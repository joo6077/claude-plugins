# {Category} 조건 검증 절차 ({Stack})

## 검증 방법

1. {어떤 도구(Glob/Read/Grep/Bash)로 무엇을 확인하는지}
2. {검증 순서 — 어떤 파일을 먼저 확인하고 어떤 관계를 추적하는지}
3. {조건 충족 판단 기준 — 코드에 어떤 요소가 있어야 PASS인지}

## 정적 검증 최소 증거

| 조건 유형 | PASS 가능한 최소 증거 |
|-----------|----------------------|
| "{조건 패턴}" | {파일:라인 수준의 구체적 증거} |
| "{조건 패턴}" | {증거 설명} |

## 런타임 검증 (선택 — MCP 사용 가능 시)

1. {MCP 도구 이름} — {확인 내용}
2. {도구} — {내용}

## 금지 패턴 (선택)

- `{코드 패턴}` — {이 패턴이 금지인 이유}
