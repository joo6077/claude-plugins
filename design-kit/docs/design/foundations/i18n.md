# Internationalization (i18n)

design-kit HTML 산출물 템플릿에 적용할 한국어/영어 다국어 패턴. Standalone HTML (빌드 도구 없이 브라우저에서 바로 열리는 단일 파일).

## 핵심 패턴: data-i18n + 인라인 딕셔너리

```html
<h1 data-i18n="hero.title">디자인 시스템</h1>
<p data-i18n="hero.desc">색상, 타이포그래피, 간격의 기초</p>
```

```js
const translations = {
  ko: { hero: { title: "디자인 시스템", desc: "색상, 타이포그래피, 간격의 기초" } },
  en: { hero: { title: "Design System", desc: "Foundations of color, typography, and spacing" } }
};

function applyTranslations(lang) {
  document.querySelectorAll("[data-i18n]").forEach(el => {
    const text = el.dataset.i18n.split(".").reduce((obj, k) => obj?.[k], translations[lang]);
    if (text) el.textContent = text;
  });
  document.documentElement.lang = lang;
  localStorage.setItem("lang", lang);
}
```

속성 번역: `data-i18n-placeholder`, `data-i18n-aria-label` 접미사.

## lang 속성

```js
document.documentElement.lang = "ko"; // 또는 "en"
```

스크린 리더 음성 엔진 선택에 직접 영향. Googlebot은 JS 동적 변경도 인식.

## 폰트 스택

Pretendard = Inter 기반 로마자 + Noto Sans KR 한글 통합 → 단일 폰트로 한/영 커버.

```css
:root {
  font-family:
    "Pretendard Variable", Pretendard,
    -apple-system, BlinkMacSystemFont, system-ui,
    "Segoe UI", Roboto,
    "Noto Sans KR", "Apple SD Gothic Neo", "Malgun Gothic",
    sans-serif;
}

/* 언어별 최적화 (선택) */
:lang(en) { font-family: Inter, system-ui, sans-serif; }
:lang(ko) { font-family: "Pretendard Variable", "Noto Sans KR", sans-serif; }
```

## 레이아웃 영향 방지

한국어 CJK 문자는 영어 대비 글자 폭 약 2배. 텍스트 길이 변화로 레이아웃 깨짐 방지:

```css
.btn { min-width: 6rem; text-align: center; }
:lang(ko) { word-break: keep-all; }
.nav { display: flex; flex-wrap: wrap; gap: 0.5rem; }
```

- 고정 픽셀 너비 대신 `min-content` 유동 레이아웃
- WCAG 권고: CJK 한 줄 최대 40자

## 날짜/숫자 포맷

Intl API — 빌드 도구 없이 모든 모던 브라우저 지원.

```js
const locale = lang === "ko" ? "ko-KR" : "en-US";
new Intl.DateTimeFormat(locale, { dateStyle: "long" }).format(new Date());
// ko-KR → "2026년 4월 9일", en-US → "April 9, 2026"
```

## 토글 UI

우상단 고정. 현재 언어가 한국어면 전환 대상인 "English" 표시, 반대도 마찬가지. 국기/국가코드 사용 금지 (문화적 오해 방지).

```html
<div class="lang-toggle" role="navigation" aria-label="언어 선택">
  <button onclick="switchLang()">
    <span lang="en">English</span>
  </button>
</div>
```

```js
function switchLang() {
  const next = document.documentElement.lang === "ko" ? "en" : "ko";
  applyTranslations(next);
  const btn = document.querySelector(".lang-toggle span");
  btn.textContent = next === "ko" ? "English" : "한국어";
  btn.lang = next === "ko" ? "en" : "ko";
}
```

## base.html 적용 방향

1. `data-i18n` + 인라인 딕셔너리 패턴
2. Pretendard 단일 폰트 + `:lang()` 선택자 최적화
3. `:lang(ko) { word-break: keep-all }` 기본 적용
4. `Intl` API로 날짜/숫자 로케일 포맷
5. 토글: 우상단, 전환 대상 언어로 표시

## Sources

- andreasremdt.com: Building a Small i18n Script in JavaScript
- USWDS: Two Languages Pattern
- Pretendard GitHub
- MDN: Intl.DateTimeFormat
- W3C: Text Size in Translation
