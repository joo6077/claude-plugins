# 기록 파일 형식 — record.json

케이스 폴더마다 `record.json` 을 하나 둔다. `scripts/build_report.py` 가 이 파일을 읽어 검사하고 보고서를 만든다.
스크립트는 아래 표에 없는 키를 오타로 보고 막는다. 표에 키를 더하려면 스크립트의 `*_FIELDS` 도 같이 고친다.

## 폴더 구조

```text
{앱 루트}/.mcp_screenshots/test-evidence/
├── index.html                          ← 스크립트가 만든다. 다시 돌리면 덮어쓴다
├── TC-001-transfer-leader-cancel/
│   ├── record.json
│   ├── 01-picker.png
│   └── 02-after-picker-cancel.png
└── TC-002-appoint-vice-leader/
    ├── record.json
    ├── 01-menu.png
    ├── 02-picker.png
    └── 02-picker-title.png              ← 확대 사진도 같은 폴더에 둔다
```

폴더 이름은 `TC-{번호}-{영문 요약}` 으로 짓는다. 사진 이름은 소문자 · 숫자 · 하이픈으로 된 `.png` 만 받는다 (`01-menu.png`). 대문자나 공백이 들어가면 HTML 경로에서 깨진다.

## 예시

```json
{
  "id": "TC-002",
  "title": "다른 사람 프로필에서 부방장으로 임명한다",
  "summary": "두 번째 시나리오에서 그룹을 고르는 창의 제목이 잘려 「어느 그룹의 부방장으로 임명할」까지만 보였다.",
  "meta": {"date": "2026-09-24 22:09", "device": "iPhone 17 시뮬레이터 (iOS 26.5)", "commit": "8df171a7"},
  "background": ["Bob2 가 PGA 알파·브라보의 방장이다", "티나의 프로필 화면을 열었다"],
  "scenarios": [
    {
      "name": "⋯ 메뉴에 관리 항목이 보인다",
      "steps": [
        {"kw": "만일", "text": "⋯ 버튼을 누른다"},
        {"kw": "그러면", "text": "부방장 임명, 방장 넘기기가 보인다", "result": "pass", "seen": "find_widget 부방장 임명 1 · 방장 넘기기 1"},
        {"kw": "하지만", "text": "부방장 해제는 없다", "result": "pass", "seen": "find_widget 부방장 해제 0"}
      ],
      "shots": [{"file": "01-menu.png", "caption": "⋯ 버튼을 누른 뒤"}]
    },
    {
      "name": "부방장으로 임명할 그룹을 고른다",
      "steps": [
        {"kw": "만일", "text": "부방장 임명을 누른다"},
        {"kw": "그러면", "text": "「어느 그룹의 부방장으로 임명할까요?」 제목이 다 보인다", "result": "fail",
         "seen": "제목이 두 줄로 접히면서 둘째 줄 「까요?」가 가려졌다.",
         "zoom": {"file": "02-picker-title.png", "caption": "잘린 제목"}}
      ],
      "shots": [{"file": "02-picker.png", "caption": "부방장 임명을 누른 뒤"}]
    },
    {
      "name": "PGA 알파를 부방장으로 임명한다",
      "steps": [
        {"kw": "만일", "text": "그룹 고르는 창에서 PGA 알파를 고른다"},
        {"kw": "그러면", "text": "부방장으로 임명했다는 안내가 뜬다"}
      ],
      "skipped": "앞 시나리오의 결함을 먼저 고치기로 해서 진행하지 않았다."
    }
  ],
  "run": [["MCP 서버", "fitpal-mobile"], ["시나리오 1", "tap_native_point 355,97 → find_widget 부방장 임명 1"]]
}
```

## 키

### 케이스

| 키 | 필수 | 형식 | 뜻 |
| --- | --- | --- | --- |
| `id` | 예 | 문자열 | 케이스 번호 (`TC-002`). 케이스끼리 겹치면 안 된다 |
| `title` | 예 | 문자열 | 기대 동작을 한 문장으로 |
| `summary` | 예 | 문자열 | 결론 한 문장. 실패가 있으면 어느 시나리오에서 무엇이 틀렸는지 쓴다 |
| `meta` | 예 | 객체 | 실행 정보 — 아래 표 |
| `background` | 아니오 | 문자열 목록 | 테스트 전 상태. 옆 칸에 항목으로 붙는다 |
| `scenarios` | 예 | 객체 목록 | 시나리오 하나 이상 — 아래 표 |
| `run` | 아니오 | `[이름, 값]` 목록 | 접어 둔 "실행 정보" 에 들어간다. 호출한 MCP 도구와 핵심 결과를 적는다 |

### meta

| 키 | 필수 | 형식 | 뜻 |
| --- | --- | --- | --- |
| `date` | 예 | 문자열 | 실행 시각 (`2026-09-24 22:09`) |
| `device` | 예 | 문자열 | 기기와 운영체제 판 |
| `commit` | 아니오 | 문자열 | 앱 커밋. 머리 줄에 `커밋 {값}` 으로 붙는다 |

### 시나리오

| 키 | 필수 | 형식 | 뜻 |
| --- | --- | --- | --- |
| `name` | 예 | 문자열 | 시나리오 이름 |
| `steps` | 예 | 객체 목록 | 단계 하나 이상 — 아래 표 |
| `shots` | 아니오 | 사진 객체 목록 | 시나리오 왼쪽에 크게 붙는 캡처 |
| `skipped` | 아니오 | 문자열 | 진행하지 못한 이유. 있으면 시나리오가 미실행이 되고, 판정한 단계가 있으면 오류다 |

### 단계

| 키 | 필수 | 형식 | 뜻 |
| --- | --- | --- | --- |
| `kw` | 예 | 문자열 | Gherkin 한국어 키워드 — `먼저` · `조건` · `만일` · `만약` · `그러면` · `그리고` · `하지만` · `단` |
| `text` | 예 | 문자열 | 단계 문장 |
| `result` | 아니오 | `pass` · `fail` | 판정. 확인 단계에만 붙인다 |
| `seen` | `result` 가 있으면 예 | 문자열 | 실제로 본 것. 도구가 돌려준 관측값을 쓴다 |
| `zoom` | 아니오 | 사진 객체 | 판정한 단계 바로 아래 붙는 확대 사진 |

### 사진 객체

| 키 | 필수 | 형식 | 뜻 |
| --- | --- | --- | --- |
| `file` | 예 | 문자열 | 케이스 폴더 안 파일 이름 |
| `caption` | 예 | 문자열 | 사진 아래 설명 |

## 판정 계산

판정은 적지 않는다. 스크립트가 단계의 `result` 로 계산한다.

- 확인 단계는 첫 `그러면` 과 그 뒤의 `그리고` · `하지만` · `단` 이다. `먼저` · `조건` · `만일` · `만약` 과 첫 `그러면` 앞의 단계에 `result` 를 붙이면 오류다.
- 시나리오: `skipped` 가 있으면 미실행, 확인 단계 중 `fail` 이 하나라도 있으면 실패, 아니면 통과. 건너뛰지 않았는데 판정한 단계가 없으면 오류다.
- 케이스: 실패한 시나리오가 있으면 실패, 통과한 시나리오가 있으면 통과, 전부 미실행이면 미실행.
- 보고서에서 실패한 케이스가 위로 온다.

## 스크립트가 막는 것

아래는 종료 코드 1 로 멈추고 `index.html` 을 쓰지 않는다. 오류 줄에 케이스 폴더와 문제 필드가 찍힌다.

- JSON 문법 오류 · 표에 없는 키 · 필수 키 누락 · 형식이 틀린 값 · 빈 문자열
- 케이스 폴더에 없는 사진 · 규칙에 안 맞는 사진 이름 · PNG 가 아닌 파일
- Gherkin 한국어 키워드가 아닌 `kw` · `pass` · `fail` 이 아닌 `result` · 확인 단계가 아닌 곳의 `result` · `seen` 없는 `result`
- 판정한 단계가 없는 시나리오 · 판정이 있는 건너뛴 시나리오 · 겹치는 `id` · `record.json` 이 있는 케이스 폴더가 하나도 없음

기록이 가리키지 않는 PNG 가 케이스 폴더에 있으면 경고만 찍고 계속한다. 다시 돌리며 이름이 바뀐 캡처가 남은 경우다 — 지우기 전에 사용자에게 묻는다.
