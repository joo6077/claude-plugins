# Flutter Toolkit

Flutter 프로젝트 공통 개발 스킬 모음.

## 스킬 목록

(마이그레이션 예정)

- `build` — codegen + analyze
- `run` — 빌드 프리미티브 (codegen, analyze, fix, test)
- `preflight` — pre-commit gate
- `audit` — 코드 품질 감사

## 요구사항

- FVM 설치
- `.harness/project.yaml`의 `stack: flutter` 설정 (harness 플러그인 연동 시)
