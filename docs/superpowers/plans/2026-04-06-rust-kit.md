# rust-kit 플러그인 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rust 전용 백엔드 개발 워크플로우 플러그인(rust-kit)을 생성한다. 17종 스킬 + 1 에이전트 + 20개 리서치 문서 + evals + docs-site HTML 페이지를 포함.

**Architecture:** flutter-toolkit 패턴(워크플로우 스킬 세트)과 backend-kit 패턴(원칙 참조 + 에이전트 감사)을 결합. 생성형 11 + 가이드 1 + 실행 3 + 감사 1 + 메타 1 = 17종. 리서치 문서(docs/rust/)가 SSOT, 스킬은 references/ 인덱스를 통해 간접 참조. 모든 생성형 스킬은 Hexagonal Architecture (Ports & Adapters) 패턴을 기본 적용하여 trait(포트) → impl(어댑터) 순서로 코드를 생성한다.

**Tech Stack:** Claude Code plugins, SKILL.md, agents, markdown, Rust(Axum+Tokio+SQLx), bash

---

## 파일 구조

| 액션 | 파일 | 역할 |
|------|------|------|
| Create | `rust-kit/.claude-plugin/plugin.json` | 플러그인 메타데이터 |
| Create | `rust-kit/references/project-detection.md` | Rust 프로젝트 환경 자동 감지 |
| Create | `rust-kit/skills/rust-init/SKILL.md` | 프로젝트 스캐폴딩 |
| Create | `rust-kit/skills/rust-feature/SKILL.md` | feature 모듈 스캐폴딩 |
| Create | `rust-kit/skills/rust-api/SKILL.md` | Axum 라우터/핸들러 + OpenAPI |
| Create | `rust-kit/skills/rust-model/SKILL.md` | SQLx 모델 + 마이그레이션 |
| Create | `rust-kit/skills/rust-service/SKILL.md` | 비즈니스 로직 서비스 레이어 |
| Create | `rust-kit/skills/rust-auth/SKILL.md` | JWT/OAuth 인증 레이어 |
| Create | `rust-kit/skills/rust-middleware/SKILL.md` | Axum 미들웨어 |
| Create | `rust-kit/skills/rust-grpc/SKILL.md` | tonic gRPC 서비스 |
| Create | `rust-kit/skills/rust-test/SKILL.md` | 테스트 코드 생성 |
| Create | `rust-kit/skills/rust-docker/SKILL.md` | Dockerfile + docker-compose |
| Create | `rust-kit/skills/rust-error/SKILL.md` | 에러 처리 패턴 가이드 |
| Create | `rust-kit/skills/rust-l10n/SKILL.md` | 백엔드 i18n |
| Create | `rust-kit/skills/rust-run/SKILL.md` | 빌드 프리미티브 개별 실행 |
| Create | `rust-kit/skills/rust-build/SKILL.md` | cargo build + clippy wrapper |
| Create | `rust-kit/skills/rust-preflight/SKILL.md` | pre-commit gate |
| Create | `rust-kit/skills/rust-audit/SKILL.md` | 코드 품질 감사 |
| Create | `rust-kit/skills/rust-audit/references/audit-criteria.md` | PASS/FAIL 체크리스트 |
| Create | `rust-kit/agents/rust-reviewer.md` | 읽기 전용 독립 평가 에이전트 |
| Create | `rust-kit/evals/evals.json` | 스킬별 테스트 케이스 |
| Create | `rust-kit/README.md` | 플러그인 문서 |
| Create | `docs/rust/fundamentals/ownership-borrowing.md` | 소유권/빌림 원칙 |
| Create | `docs/rust/fundamentals/error-handling.md` | 에러 처리 원칙 |
| Create | `docs/rust/fundamentals/async-concurrency.md` | 비동기/동시성 원칙 |
| Create | `docs/rust/fundamentals/testing.md` | 테스팅 원칙 |
| Create | `docs/rust/fundamentals/project-structure.md` | 프로젝트 구조 원칙 |
| Create | `docs/rust/fundamentals/performance.md` | 성능 원칙 |
| Create | `docs/rust/web/axum-patterns.md` | Axum 패턴 |
| Create | `docs/rust/web/middleware.md` | 미들웨어 원칙 |
| Create | `docs/rust/web/authentication.md` | 인증 원칙 |
| Create | `docs/rust/web/openapi.md` | OpenAPI 원칙 |
| Create | `docs/rust/data/sqlx-patterns.md` | SQLx 패턴 |
| Create | `docs/rust/data/migrations.md` | 마이그레이션 원칙 |
| Create | `docs/rust/data/caching.md` | 캐싱 원칙 |
| Create | `docs/rust/protocols/grpc-tonic.md` | gRPC 원칙 |
| Create | `docs/rust/protocols/graphql.md` | GraphQL 원칙 |
| Create | `docs/rust/protocols/realtime.md` | 실시간 통신 원칙 |
| Create | `docs/rust/ops/docker.md` | Docker 원칙 |
| Create | `docs/rust/fundamentals/hexagonal-architecture.md` | 헥사고날 아키텍처 원칙 |
| Create | `docs/rust/ops/ci-cd.md` | CI/CD 원칙 |
| Create | `docs/rust/ops/observability.md` | 관측성 원칙 |
| Create | `.claude/skills/rust-kaizen/SKILL.md` | 스킬 품질 개선 메타 스킬 |
| Create | `.claude/skills/rust-research/SKILL.md` | 리서치 문서 갱신 스킬 |
| Modify | `.claude-plugin/marketplace.json` | rust-kit 등록 |
| Modify | `CLAUDE.md` | rust-kit 스킬 테이블 추가 |

---

### Task 1: 리서치 문서 — fundamentals/ (7개)

Codex 에이전트에 리서치를 위임하여 docs/rust/fundamentals/ 7개 문서를 생성한다. 모든 문서는 동일한 frontmatter + 구조를 따른다.

**Files:**
- Create: `docs/rust/fundamentals/ownership-borrowing.md`
- Create: `docs/rust/fundamentals/error-handling.md`
- Create: `docs/rust/fundamentals/async-concurrency.md`
- Create: `docs/rust/fundamentals/testing.md`
- Create: `docs/rust/fundamentals/project-structure.md`
- Create: `docs/rust/fundamentals/performance.md`
- Create: `docs/rust/fundamentals/hexagonal-architecture.md`

- [ ] **Step 1: Codex에 Rust fundamentals 리서치 위임**

`codex:rescue` 에이전트에 아래 7개 주제를 리서치 요청한다:
1. Ownership & Borrowing — 소유권 규칙, 빌림 규칙, 수명(lifetime) 패턴, 일반적 안티패턴
2. Error Handling — thiserror vs anyhow 사용 기준, Result 패턴, From impl, 에러 전파 관용구
3. Async & Concurrency — Tokio 런타임 패턴, async/await 함정, blocking 회피, 동시성 프리미티브
4. Testing — cargo-nextest, mockall, sqlx::test, 통합 테스트 패턴, 테스트 구조화
5. Project Structure — workspace 구조, 모듈 레이아웃, 크레이트 분리 기준
6. Performance — 불필요한 allocation 회피, 이터레이터 최적화, 프로파일링, 벤치마크
7. Hexagonal Architecture — Ports & Adapters 패턴, Rust에서의 trait 기반 구현, 포트 목록, 어댑터 교체 패턴

각 주제에 대해: 2026년 기준 공식 문서 + 커뮤니티 권장 사항 + 수치 기준 + 안티패턴을 요청한다.

- [ ] **Step 2: ownership-borrowing.md 작성**

```markdown
---
title: 소유권과 빌림 원칙
version: 0.1.0
last_updated: 2026-04-06
---

# 소유권과 빌림 원칙

Rust의 메모리 안전을 보장하는 소유권 시스템의 핵심 원칙과 관용적 패턴.

---

## 원칙

### 1. 불필요한 clone 회피

{Codex 리서치 기반 내용}

> **출처:** [The Rust Programming Language](https://doc.rust-lang.org/book/)

### 2. 빌림 범위 최소화
### 3. Lifetime 명시 기준
### 4. Arc/Rc 사용 기준

---

## 수치 기준

| 항목 | 값 |
|------|-----|

---

## 안티패턴

| 안티패턴 | 문제 |
|----------|------|

---

## Gotchas

- **{패턴}** — {설명}
```

Codex 리서치 결과를 이 구조에 채운다. 나머지 5개 문서도 동일 구조로 작성.

- [ ] **Step 3: error-handling.md 작성**

Step 2와 동일 구조. 주제: thiserror/anyhow 구분, Result 패턴, From impl, `?` 연산자, 에러 계층화.

- [ ] **Step 4: async-concurrency.md 작성**

주제: Tokio 런타임, async/await 함정(blocking in async, select!, spawn), 동시성 프리미티브(Mutex, RwLock, channels).

- [ ] **Step 5: testing.md 작성**

주제: cargo-nextest, mockall, sqlx::test, 통합 테스트 디렉토리 구조, test fixture 패턴.

- [ ] **Step 6: project-structure.md 작성**

주제: workspace 구조, crates/ 분리, 모듈 레이아웃, mod.rs vs 파일 모듈, pub(crate) 범위.

- [ ] **Step 7: performance.md 작성**

주제: 불필요한 allocation, iterator 체이닝, String vs &str, Vec 사전할당, criterion 벤치마크.

- [ ] **Step 8: hexagonal-architecture.md 작성**

주제: Ports & Adapters 패턴, Rust에서 trait을 포트로 정의하는 방법, impl을 어댑터로 교체하는 패턴, 레이어 경계 설계.

- [ ] **Step 9: 커밋**

```bash
git add docs/rust/fundamentals/
git commit -m "docs(rust-kit): fundamentals 리서치 문서 7개 작성"
```

---

### Task 2: 리서치 문서 — web/ (4개)

**Files:**
- Create: `docs/rust/web/axum-patterns.md`
- Create: `docs/rust/web/middleware.md`
- Create: `docs/rust/web/authentication.md`
- Create: `docs/rust/web/openapi.md`

- [ ] **Step 1: Codex에 Rust web 리서치 위임**

4개 주제 리서치:
1. Axum Patterns — Router 구성, State 관리, Extractor 패턴, 에러 응답, 0.8.x API
2. Middleware — tower 레이어 구조, tower-http 크레이트, 미들웨어 순서, from_fn 패턴
3. Authentication — jsonwebtoken 사용법, Bearer 토큰 추출, Claims 구조, refresh token, OAuth/OIDC
4. OpenAPI — utoipa 5.x API, #[utoipa::path] 매크로, Swagger UI 서빙

- [ ] **Step 2: axum-patterns.md 작성**

fundamentals와 동일한 frontmatter + 구조. 주제: Router, State, Extractor, IntoResponse, 에러 처리.

- [ ] **Step 3: middleware.md 작성**
- [ ] **Step 4: authentication.md 작성**
- [ ] **Step 5: openapi.md 작성**

- [ ] **Step 6: 커밋**

```bash
git add docs/rust/web/
git commit -m "docs(rust-kit): web 리서치 문서 4개 작성"
```

---

### Task 3: 리서치 문서 — data/ (3개) + protocols/ (3개) + ops/ (3개)

**Files:**
- Create: `docs/rust/data/sqlx-patterns.md`
- Create: `docs/rust/data/migrations.md`
- Create: `docs/rust/data/caching.md`
- Create: `docs/rust/protocols/grpc-tonic.md`
- Create: `docs/rust/protocols/graphql.md`
- Create: `docs/rust/protocols/realtime.md`
- Create: `docs/rust/ops/docker.md`
- Create: `docs/rust/ops/ci-cd.md`
- Create: `docs/rust/ops/observability.md`

- [ ] **Step 1: Codex에 data/protocols/ops 리서치 위임**

9개 주제 리서치:
1. SQLx Patterns — query_as!, FromRow, 타입 매핑, 트랜잭션, connection pool
2. Migrations — sqlx migrate, 파일 네이밍, reversible migration, 오프라인 모드
3. Caching — Redis 연동 (bb8-redis, deadpool-redis), in-memory 캐시 (moka), 캐시 전략
4. gRPC (tonic) — proto 정의, build.rs, 서비스 구현, streaming, 에러 매핑
5. GraphQL — async-graphql 패턴, schema stitching, dataloader
6. Realtime — WebSocket (axum-tungstenite), SSE (axum SSE 지원)
7. Docker — cargo-chef, 멀티스테이지 빌드, musl, .dockerignore, docker-compose 패턴
8. CI/CD — GitHub Actions Rust 워크플로우, cargo-cache, 매트릭스 빌드, 릴리스 자동화
9. Observability — tracing 크레이트, OpenTelemetry 연동, metrics (prometheus), 로그 수준 설정

- [ ] **Step 2: sqlx-patterns.md 작성**
- [ ] **Step 3: migrations.md 작성**
- [ ] **Step 4: caching.md 작성**
- [ ] **Step 5: grpc-tonic.md 작성**
- [ ] **Step 6: graphql.md 작성**
- [ ] **Step 7: realtime.md 작성**
- [ ] **Step 8: docker.md 작성**
- [ ] **Step 9: ci-cd.md 작성**
- [ ] **Step 10: observability.md 작성**

- [ ] **Step 11: 커밋**

```bash
git add docs/rust/data/ docs/rust/protocols/ docs/rust/ops/
git commit -m "docs(rust-kit): data/protocols/ops 리서치 문서 9개 작성"
```

---

### Task 4: 플러그인 스캐폴딩 — plugin.json + project-detection.md + README.md

**Files:**
- Create: `rust-kit/.claude-plugin/plugin.json`
- Create: `rust-kit/references/project-detection.md`
- Create: `rust-kit/README.md`
- Create: `rust-kit/evals/evals.json` (빈 배열로 시작)

- [ ] **Step 1: plugin.json 생성**

```json
{
  "name": "rust-kit",
  "description": "Rust 전용 백엔드 개발 워크플로우 플러그인 — 프로젝트 스캐폴딩, API 생성, 모델 관리, 빌드/테스트/감사 자동화",
  "version": "0.1.0",
  "author": {
    "name": "Jackson Kim"
  },
  "repository": "https://github.com/joo6077/claude-plugins",
  "license": "MIT",
  "keywords": ["rust", "axum", "tokio", "sqlx", "backend", "api", "build", "test", "audit", "grpc", "docker"]
}
```

- [ ] **Step 2: project-detection.md 생성**

flutter-toolkit의 project-detection.md 패턴을 따르되 Rust에 맞게 조정:

```markdown
# Rust 프로젝트 감지

rust-kit 스킬이 공통으로 실행하는 프로젝트 환경 감지 절차.

---

## Step 1. Rust 프로젝트 확인

`Cargo.toml` 존재 여부 확인. 없으면 "Rust 프로젝트가 아닙니다" 안내 후 중단.

## Step 2. 툴체인 감지

| 조건 | 결과 |
|------|------|
| `rust-toolchain.toml` 존재 | 파일에서 `channel`, `components` 파싱 |
| 없음 | `rustup default` 결과 사용 |

## Step 3. Workspace 감지

`Cargo.toml`에 `[workspace]` 섹션 존재 여부:

| 조건 | 결과 |
|------|------|
| `[workspace]` 존재 | `IS_WORKSPACE = true`, `WORKSPACE_MEMBERS` = members 목록 |
| 없음 | `IS_WORKSPACE = false` |

## Step 4. 의존성 감지

`Cargo.toml`의 `[dependencies]`와 `[dev-dependencies]`에서 다음 크레이트 존재 여부를 확인하고 `HAS_*` 플래그를 설정한다.

IS_WORKSPACE이면 workspace root + 모든 member의 Cargo.toml을 합산한다.

| 크레이트 | 플래그 |
|----------|--------|
| `axum` | `HAS_AXUM` |
| `actix-web` | `HAS_ACTIX` |
| `rocket` | `HAS_ROCKET` |
| `sqlx` | `HAS_SQLX` |
| `diesel` | `HAS_DIESEL` |
| `sea-orm` | `HAS_SEAORM` |
| `tokio` | `HAS_TOKIO` |
| `async-std` | `HAS_ASYNC_STD` |
| `tonic` | `HAS_TONIC` |
| `serde` | `HAS_SERDE` |
| `utoipa` | `HAS_UTOIPA` |
| `jsonwebtoken` | `HAS_JSONWEBTOKEN` |
| `tracing` | `HAS_TRACING` |
| `rust-i18n` | `HAS_RUST_I18N` |
| `fluent` | `HAS_FLUENT` |
| `mockall` | `HAS_MOCKALL` |
| `cargo-nextest` (dev-dep 또는 설치 확인) | `HAS_NEXTEST` |

## Step 5. 아키텍처 패턴 감지

| 조건 | `ARCH` 값 |
|------|-----------|
| `IS_WORKSPACE` + `crates/` 디렉토리 (api/domain/infra 등) | `workspace_service` |
| 단일 크레이트 + `src/api/`, `src/domain/` 등 모듈 분리 | `modular` |
| `src/main.rs` + `src/lib.rs` 수준 | `flat` |
| `[lib]` only, `[[bin]]` 없음 | `library` |

## Step 6. 빌드 도구 감지

| 파일 | 플래그 |
|------|--------|
| `Makefile` | `HAS_MAKEFILE` |
| `justfile` | `HAS_JUST` |
| `Makefile.toml` (cargo-make) | `HAS_CARGO_MAKE` |
| `Cross.toml` | `HAS_CROSS` → `$CARGO = cross` |
| `build.rs` | `HAS_BUILD_SCRIPT` |

## Step 7. CI 감지

| 파일/디렉토리 | CI 플랫폼 |
|---------------|-----------|
| `.github/workflows/` | GitHub Actions |
| `.gitlab-ci.yml` | GitLab CI |
| `.circleci/` | CircleCI |

---

## 감지 결과 변수 요약

### 커맨드

| 변수 | 기본값 | 조건 |
|------|--------|------|
| `$CARGO` | `cargo` | `HAS_CROSS`이면 `cross` |
| `$RUSTFMT` | `cargo fmt` | |
| `$CLIPPY` | `cargo clippy` | |

### 프로젝트 메타

| 변수 | 설명 |
|------|------|
| `$PACKAGE` | Cargo.toml [package].name |
| `$EDITION` | 2021 \| 2024 |
| `IS_WORKSPACE` | true \| false |
| `WORKSPACE_MEMBERS` | 크레이트 목록 (IS_WORKSPACE일 때만) |
| `ARCH` | workspace_service \| modular \| flat \| library |

### 의존성 플래그 (HAS_*)

위 Step 4 테이블 참조.

### 빌드 도구

위 Step 6 테이블 참조.
```

- [ ] **Step 3: README.md 스켈레톤 생성**

```markdown
# rust-kit

Rust 전용 백엔드 개발 워크플로우 플러그인.

## 개요

Axum + Tokio + SQLx 기반 Rust 백엔드 프로젝트의 스캐폴딩, API 생성, 모델 관리, 빌드/테스트/감사를 자동화한다. flutter-toolkit과 동일한 개발 워크플로우 패턴.

<!-- AUTO:skills -->
## 스킬

| 스킬 | 용도 |
|------|------|
| `/rust-init` | 프로젝트 스캐폴딩 (workspace + toolchain + 구조) |
| `/rust-feature` | feature 모듈 스캐폴딩 |
| `/rust-api` | Axum 라우터/핸들러 + utoipa OpenAPI |
| `/rust-model` | SQLx 모델 + 마이그레이션 |
| `/rust-service` | 비즈니스 로직 서비스 레이어 |
| `/rust-auth` | JWT/OAuth 인증 레이어 |
| `/rust-middleware` | Axum 미들웨어 (CORS, logging, rate-limit) |
| `/rust-grpc` | tonic gRPC 서비스 |
| `/rust-test` | 테스트 코드 생성 (unit + integration) |
| `/rust-docker` | Dockerfile + docker-compose |
| `/rust-error` | 에러 처리 패턴 가이드 (thiserror/anyhow) |
| `/rust-l10n` | 백엔드 i18n (rust-i18n/fluent) |
| `/rust-run` | 빌드 프리미티브 개별 실행 (build, clippy, fmt, test, audit, check) |
| `/rust-build` | cargo build + clippy (rust-run wrapper) |
| `/rust-preflight` | pre-commit gate (fmt → clippy → test → audit) |
| `/rust-audit` | 코드 품질 감사 (quick/deep 모드) |
<!-- AUTO:skills -->

<!-- AUTO:agents -->
## 에이전트

| 에이전트 | 용도 |
|---------|------|
| `rust-reviewer` | rust-audit에서 호출. 읽기 전용 독립 평가 |
<!-- AUTO:agents -->

## 리서치 문서

`docs/rust/` 디렉토리에 20개 원칙 문서가 있으며, 모든 스킬이 이를 SSOT로 참조한다.

### fundamentals
- **소유권과 빌림** — ownership, borrowing, lifetime, clone 회피
- **에러 처리** — thiserror, anyhow, Result 패턴, 에러 계층화
- **비동기/동시성** — Tokio, async/await, spawn, 동시성 프리미티브
- **테스팅** — cargo-nextest, mockall, sqlx::test, 통합 테스트
- **프로젝트 구조** — workspace, 모듈 레이아웃, 크레이트 분리
- **성능** — allocation, iterator, 벤치마크, 프로파일링

### web
- **Axum 패턴** — Router, State, Extractor, IntoResponse
- **미들웨어** — tower 레이어, tower-http, 미들웨어 순서
- **인증** — JWT, OAuth, Bearer 토큰, Claims
- **OpenAPI** — utoipa, Swagger UI, 스키마 자동 생성

### data
- **SQLx 패턴** — query_as!, FromRow, 타입 매핑, 트랜잭션
- **마이그레이션** — sqlx migrate, 오프라인 모드
- **캐싱** — Redis, in-memory 캐시, 캐시 전략

### protocols
- **gRPC** — tonic, proto 정의, streaming
- **GraphQL** — async-graphql, schema, dataloader
- **실시간** — WebSocket, SSE

### ops
- **Docker** — cargo-chef, 멀티스테이지 빌드, compose

## 카이젠

- `/rust-research` — 외부 소스 크롤링으로 docs/rust/ 문서 갱신
- `/rust-kaizen` — 리서치 문서 기준으로 스킬 품질 점진 개선
```

- [ ] **Step 4: 빈 evals.json 생성**

```json
{
  "skill_name": "rust-kit",
  "evals": []
}
```

- [ ] **Step 5: 커밋**

```bash
git add rust-kit/.claude-plugin/ rust-kit/references/ rust-kit/README.md rust-kit/evals/
git commit -m "feat(rust-kit): 플러그인 스캐폴딩 — plugin.json + project-detection + README"
```

---

### Task 5: 실행형 스킬 — rust-run, rust-build, rust-preflight

실행형 스킬을 먼저 작성한다. 다른 스킬들이 `## 0. 프로젝트 감지` + `rust-run` 호출 패턴을 참조하므로 기반 스킬 우선.

**Files:**
- Create: `rust-kit/skills/rust-run/SKILL.md`
- Create: `rust-kit/skills/rust-build/SKILL.md`
- Create: `rust-kit/skills/rust-preflight/SKILL.md`

- [ ] **Step 1: rust-run/SKILL.md 작성**

```markdown
---
name: rust-run
description: >
  Rust 빌드 프리미티브 실행 (build, clippy, fmt, test, audit, check).
  "빌드해줘", "clippy 돌려줘", "포맷팅", "테스트 실행",
  "cargo build", "cargo clippy", "lint" 같은 요청 시 사용한다.
  상위 워크플로우 스킬(rust-build, rust-preflight)에서 내부적으로도 호출된다.
  코드를 직접 수정하거나 새 파일을 생성하는 작업에는 사용하지 않는다.
argument-hint: "<build|clippy|fmt|test|audit|check> [args]"
user-invocable: true
---

# Gotchas

1. **`cargo clippy`에 `-- -D warnings` 누락 금지** — 없으면 워닝이 에러로 잡히지 않아 CI와 불일치한다.
2. **workspace에서 `--workspace` 누락 금지** — 없으면 루트 크레이트만 실행되어 하위 크레이트 문제를 놓친다.
3. **`cargo nextest`가 없을 때 에러 금지** — 설치 여부 확인 후 없으면 `cargo test`로 폴백한다.
4. **`cargo audit` 미설치 시 skip** — 설치 안내만 출력하고 중단하지 않는다.

# Process

## 0. 프로젝트 감지

`references/project-detection.md`의 절차를 실행하여 프로젝트 환경을 파악한다.
이후 단계에서 감지 결과(`$CARGO`, `IS_WORKSPACE`, `HAS_NEXTEST`) 를 사용한다.

## 1. 서브커맨드 파싱

| 서브커맨드 | 실행 커맨드 | 성공 조건 |
|-----------|------------|----------|
| `build` | `$CARGO build` + (`--workspace` if `IS_WORKSPACE`) | exit 0 |
| `clippy` | `$CARGO clippy` + (`--workspace` if `IS_WORKSPACE`) + `-- -D warnings` | exit 0, 워닝 0 |
| `fmt` | `$CARGO fmt` (적용) / `$CARGO fmt -- --check` (검사만) | exit 0 |
| `test` | `HAS_NEXTEST` → `cargo nextest run` / else → `$CARGO test` + (`--workspace` if `IS_WORKSPACE`) | 전 테스트 통과 |
| `audit` | `cargo audit` (있으면) + `cargo deny check` (있으면) | 취약점 0 |
| `check` | `$CARGO check` + (`--workspace` if `IS_WORKSPACE`) | exit 0 |

추가 args가 있으면 커맨드 끝에 전달한다.

## 2. 실행 + 결과 출력

실행 결과를 아래 포맷으로 출력한다:

### rust-run {서브커맨드} 결과

**커맨드:** `{실행된 전체 커맨드}`
**상태:** PASS / FAIL
**상세:** {에러 메시지 또는 요약}

# References

- references/project-detection.md
```

- [ ] **Step 2: rust-build/SKILL.md 작성**

```markdown
---
name: rust-build
description: >
  cargo build + clippy를 순서대로 실행한다.
  내부적으로 rust-run 스킬의 build + clippy를 호출하는 thin wrapper.
  "빌드", "build", "컴파일", "rust build" 같은 요청 시 사용한다.
  개별 프리미티브만 실행하려면 rust-run을 직접 사용한다.
argument-hint: ""
user-invocable: true
---

# Gotchas

1. **build 없이 clippy만 실행하지 않는다** — build가 선행되어야 clippy가 증분 분석을 제대로 수행한다.
2. **실패 시 즉시 중단** — build 실패 시 clippy를 실행하지 않는다.

# Process

## 0. 프로젝트 감지

`references/project-detection.md`의 절차를 실행하여 프로젝트 환경을 파악한다.
이후 단계에서 감지 결과(`$CARGO`, `IS_WORKSPACE`) 를 사용한다.

## 1. Build 실행

rust-run build를 실행한다. 실패 시 에러를 출력하고 중단.

## 2. Clippy 실행

rust-run clippy를 실행한다.

## 3. 결과 리포트

| Step | Status | Details |
|------|--------|---------|
| build | {PASS/FAIL} | {상세} |
| clippy | {PASS/FAIL} | {상세} |

# References

- references/project-detection.md
```

- [ ] **Step 3: rust-preflight/SKILL.md 작성**

```markdown
---
name: rust-preflight
description: >
  Pre-commit quality gate. fmt → clippy → test → audit 순서로 실행하고
  결과를 요약 보고한다.
  "preflight", "프리플라이트", "커밋 전 검사", "pre-commit",
  "품질 게이트", "커밋 전에 확인" 같은 요청 시 사용한다.
  개별 프리미티브만 실행하려면 rust-run을 직접 사용한다.
argument-hint: ""
user-invocable: true
---

# Gotchas

1. **순서 변경 금지** — fmt → clippy → test → audit 순서는 고정이다. clippy 전에 fmt를 해야 포맷 워닝이 없다.
2. **fmt 실패 시 자동 적용** — `cargo fmt -- --check` 실패 시 `cargo fmt`를 적용한 후 재검사한다. 자동 수정 후 unstaged changes가 생기므로 `git add` 안내를 출력한다.
3. **audit은 non-blocking** — 외부 크레이트 취약점은 즉시 수정 불가할 수 있으므로 WARN으로만 표시한다.
4. **clippy 또는 test 실패 시 즉시 중단** — 이후 단계를 실행하지 않는다.

# Process

## 0. 프로젝트 감지

`references/project-detection.md`의 절차를 실행하여 프로젝트 환경을 파악한다.
이후 단계에서 감지 결과(`$CARGO`, `IS_WORKSPACE`, `HAS_NEXTEST`) 를 사용한다.

## 1. fmt 검사

rust-run `fmt --check`를 실행한다.
- PASS → Step 2로
- FAIL → rust-run `fmt`를 실행하여 자동 적용 후 재검사. 재검사도 FAIL이면 중단.
  - 자동 적용 시: "`cargo fmt`가 파일을 수정했습니다. `git add`로 변경사항을 스테이징하세요." 안내.

## 2. clippy 검사

rust-run clippy를 실행한다.
- PASS → Step 3로
- FAIL → 에러 출력 후 중단. 이후 단계 skip.

## 3. test 실행

rust-run test를 실행한다.
- PASS → Step 4로
- FAIL → 에러 출력 후 중단. 이후 단계 skip.

## 4. audit 검사

rust-run audit를 실행한다.
- PASS → 정상
- FAIL → WARN으로 표시 (non-blocking). 취약점 목록 출력.

## 5. 리포트

## Preflight Report

| Step | Status | Details |
|------|--------|---------|
| fmt | {PASS/FAIL/FIXED} | {상세} |
| clippy | {PASS/FAIL/SKIP} | {상세} |
| test | {PASS/FAIL/SKIP} | {N tests passed / failed} |
| audit | {PASS/WARN/SKIP} | {N advisories} |

**Result:** {PASS / PASS (with warnings) / FAIL}

# References

- references/project-detection.md
```

- [ ] **Step 4: 커밋**

```bash
git add rust-kit/skills/rust-run/ rust-kit/skills/rust-build/ rust-kit/skills/rust-preflight/
git commit -m "feat(rust-kit): 실행형 스킬 3종 — rust-run, rust-build, rust-preflight"
```

---

### Task 6: 생성형 스킬 — rust-init, rust-feature

프로젝트 구조를 만드는 스킬 2개.

**Files:**
- Create: `rust-kit/skills/rust-init/SKILL.md`
- Create: `rust-kit/skills/rust-feature/SKILL.md`

- [ ] **Step 1: rust-init/SKILL.md 작성**

스펙의 "1. rust-init" 섹션 내용을 SKILL.md 포맷으로 변환:

```markdown
---
name: rust-init
description: >
  Rust 백엔드 프로젝트를 스캐폴딩한다. Cargo workspace 구조, rust-toolchain.toml,
  디렉토리 레이아웃, 의존성 설정을 생성한다.
  "프로젝트 만들어줘", "rust init", "새 프로젝트", "cargo new",
  "프로젝트 생성", "프로젝트 셋업" 같은 요청 시 트리거.
  기존 프로젝트에 모듈을 추가할 때는 트리거하지 않는다 — rust-feature 사용.
argument-hint: "[project-name]"
user-invocable: true
---

# Gotchas

1. **`cargo init` vs `cargo new` 혼동 금지** — 기존 디렉토리에 초기화하면 `cargo init`, 새 디렉토리를 만들면 `cargo new`.
2. **workspace resolver 누락 금지** — edition 2021+는 `resolver = "2"`, edition 2024는 `resolver = "3"` 필수.
3. **타겟 아키텍처 고정 금지** — `.cargo/config.toml`에 `[target.x86_64-unknown-linux-gnu]` 같은 타겟을 고정하면 크로스 플랫폼이 깨진다.
4. **과도한 의존성 금지** — 사용자가 선택한 의존성만 추가한다. "나중에 필요할 수도 있으니" 추가하지 않는다.

# Process

## 1. 프로젝트 이름/설명 확인

사용자에게 프로젝트 이름을 확인한다. 미지정 시 현재 디렉토리 이름 사용.

## 2. 아키텍처 선택

사용자에게 아키텍처를 제안한다:

| 아키텍처 | 적합 규모 | 특징 |
|----------|----------|------|
| `workspace_service` (권장) | 중~대규모 | crates/api + domain + infra 분리 |
| `modular` | 소~중규모 | 단일 크레이트 내 모듈 분리 |
| `flat` | 프로토타입/소규모 | src/main.rs + lib.rs |

## 3. 의존성 선택

체크리스트로 제시:
- [x] axum (기본)
- [x] tokio (기본)
- [x] serde + serde_json (기본)
- [x] tracing + tracing-subscriber (기본)
- [ ] sqlx (PostgreSQL)
- [ ] utoipa + utoipa-swagger-ui (OpenAPI)
- [ ] jsonwebtoken (JWT 인증)
- [ ] tower-http (CORS, 로깅 등)
- [ ] rust-i18n (다국어)

## 4. 구조 생성

선택한 아키텍처에 따라 파일/디렉토리를 생성한다.

### workspace_service 구조

```
{project}/
├── Cargo.toml              # [workspace] members = ["crates/*"]
├── rust-toolchain.toml      # channel = "stable", components = ["rustfmt", "clippy"]
├── .cargo/config.toml
├── .env.example
├── crates/
│   ├── api/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs
│   │       ├── router.rs
│   │       └── handlers/
│   │           └── mod.rs
│   ├── domain/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── models/
│   │       │   └── mod.rs
│   │       ├── services/
│   │       │   └── mod.rs
│   │       └── errors.rs
│   └── infra/
│       ├── Cargo.toml
│       └── src/
│           ├── lib.rs
│           └── db/
│               └── mod.rs
├── migrations/
└── tests/
```

### modular 구조

```
{project}/
├── Cargo.toml
├── rust-toolchain.toml
├── .cargo/config.toml
├── .env.example
├── src/
│   ├── main.rs
│   ├── api/
│   │   ├── mod.rs
│   │   ├── router.rs
│   │   └── handlers/
│   │       └── mod.rs
│   ├── domain/
│   │   ├── mod.rs
│   │   ├── models/
│   │   │   └── mod.rs
│   │   └── services/
│   │       └── mod.rs
│   └── infra/
│       ├── mod.rs
│       └── db/
│           └── mod.rs
├── migrations/
└── tests/
```

### flat 구조

```
{project}/
├── Cargo.toml
├── rust-toolchain.toml
├── src/
│   ├── main.rs
│   └── lib.rs
└── tests/
```

## 5. 초기 컴파일 확인

`cargo build`를 실행하여 프로젝트가 정상 컴파일되는지 확인한다.

## After Creation

1. 생성된 파일/디렉토리 목록 출력.
2. 다음 단계 안내:
   > - API 엔드포인트 추가: `/rust-api`
   > - DB 모델 추가: `/rust-model`
   > - 인증 설정: `/rust-auth`

# References

- references/project-detection.md
- docs/rust/fundamentals/project-structure.md (프로젝트 구조 원칙 참조)
```

- [ ] **Step 2: rust-feature/SKILL.md 작성**

```markdown
---
name: rust-feature
description: >
  feature 모듈을 프로젝트 아키텍처에 맞는 구조로 스캐폴딩한다.
  handler + service + model + test 디렉토리를 생성하고 mod.rs에 등록한다.
  "모듈 추가", "feature 생성", "새 기능 모듈", "rust feature" 같은 요청 시 트리거.
  프로젝트 자체를 새로 만들 때는 트리거하지 않는다 — rust-init 사용.
argument-hint: "<feature-name>"
user-invocable: true
---

# Gotchas

1. **`mod.rs` vs 파일 모듈 혼용 금지** — 프로젝트 기존 패턴을 따른다. 한 프로젝트에서 두 방식을 섞지 않는다.
2. **workspace 순환 의존 금지** — domain 크레이트가 api나 infra를 의존하면 안 된다. 의존 방향: api → domain ← infra.
3. **`pub` 범위 최소화** — `pub(crate)` 기본, `pub`은 크레이트 경계에서만.
4. **빈 모듈 금지** — 생성하는 모든 파일에 최소한의 구조체 또는 함수를 포함한다.

# Process

## 0. 프로젝트 감지

`references/project-detection.md`의 절차를 실행하여 프로젝트 환경을 파악한다.
이후 단계에서 감지 결과(`ARCH`, `IS_WORKSPACE`, `WORKSPACE_MEMBERS`) 를 사용한다.

## 1. Feature 이름 확인

사용자에게 feature 이름을 확인한다 (snake_case).

## 2. 기존 패턴 분석

이미 존재하는 feature 모듈의 구조를 읽어 패턴을 파악한다:
- 디렉토리 구조
- mod.rs 등록 방식
- 네이밍 컨벤션

없으면 아키텍처 기본 패턴을 따른다.

## 3. 파일 생성

### ARCH = workspace_service

해당 크레이트(api/domain/infra) 내에 모듈 디렉토리를 생성한다:

```
crates/api/src/handlers/{feature}/
├── mod.rs          # pub mod 선언
├── handler.rs      # 핸들러 함수
└── dto.rs          # Request/Response 구조체

crates/domain/src/models/{feature}.rs    # 도메인 모델
crates/domain/src/services/{feature}.rs  # 서비스 로직
crates/infra/src/db/{feature}.rs         # DB 쿼리
```

### ARCH = modular

```
src/api/handlers/{feature}/
├── mod.rs
├── handler.rs
└── dto.rs

src/domain/models/{feature}.rs
src/domain/services/{feature}.rs
src/infra/db/{feature}.rs
```

### ARCH = flat

```
src/{feature}.rs    # 모든 로직을 한 파일에
```

## 4. mod.rs 등록

각 레이어의 mod.rs에 `pub mod {feature};`를 추가한다.

## 5. 라우터 연결 안내

> 라우터에 새 feature를 연결하세요:
> ```rust
> .nest("/{feature}", {feature}::router())
> ```

## After Creation

1. 생성/수정된 파일 목록 출력.
2. 다음 단계 안내:
   > - API 엔드포인트 추가: `/rust-api`
   > - DB 모델 추가: `/rust-model`
   > - 서비스 로직 추가: `/rust-service`

# References

- references/project-detection.md
```

- [ ] **Step 3: 커밋**

```bash
git add rust-kit/skills/rust-init/ rust-kit/skills/rust-feature/
git commit -m "feat(rust-kit): 생성형 스킬 — rust-init, rust-feature"
```

---

### Task 7: 생성형 스킬 — rust-api, rust-model, rust-service

코어 비즈니스 로직 스킬 3개.

**Files:**
- Create: `rust-kit/skills/rust-api/SKILL.md`
- Create: `rust-kit/skills/rust-model/SKILL.md`
- Create: `rust-kit/skills/rust-service/SKILL.md`

- [ ] **Step 1: rust-api/SKILL.md 작성**

스펙의 "3. rust-api" 섹션을 SKILL.md 포맷으로 변환. frontmatter description에 트리거 키워드 포함. Gotchas 3개(State 주입, Json 소비, Path 순서). Process: 0.감지 → 1.메서드/경로 확인 → 2.기존 패턴 읽기 → 3.핸들러+라우터 생성 → 4.OpenAPI 등록(HAS_UTOIPA) → 5.빌드 확인 안내. After Creation: 생성 파일 목록 + 다음 스킬 안내. **Hexagonal 패턴 적용: Process에 포트 정의 → 어댑터 구현 단계 포함.**

- [ ] **Step 2: rust-model/SKILL.md 작성**

스펙의 "4. rust-model" 섹션 기반. Gotchas 4개(DATABASE_URL, 오프라인 모드, 타임스탬프 겹침, nullable). Process: 0.감지 → 1.테이블/컬럼 확인 → 2.기존 모델 읽기 → 3.마이그레이션 SQL → 4.구조체+쿼리 함수 → 5.migrate 안내. **Hexagonal 패턴 적용: Process에 포트 정의 → 어댑터 구현 단계 포함.**

- [ ] **Step 3: rust-service/SKILL.md 작성**

스펙의 "5. rust-service" 섹션 기반. Gotchas 2개(async_trait vs RPITIT, 제네릭 복잡도). Process: 0.감지 → 1.서비스 이름/의존성 → 2.기존 패턴 읽기 → 3.trait+impl 생성 → 4.테스트 모듈. **Hexagonal 패턴 적용: Process에 포트 정의 → 어댑터 구현 단계 포함.**

- [ ] **Step 4: 커밋**

```bash
git add rust-kit/skills/rust-api/ rust-kit/skills/rust-model/ rust-kit/skills/rust-service/
git commit -m "feat(rust-kit): 생성형 스킬 — rust-api, rust-model, rust-service"
```

---

### Task 8: 생성형 스킬 — rust-auth, rust-middleware, rust-grpc

인프라/프로토콜 레이어 스킬 3개.

**Files:**
- Create: `rust-kit/skills/rust-auth/SKILL.md`
- Create: `rust-kit/skills/rust-middleware/SKILL.md`
- Create: `rust-kit/skills/rust-grpc/SKILL.md`

- [ ] **Step 1: rust-auth/SKILL.md 작성**

스펙의 "6. rust-auth" 섹션 기반. Gotchas 3개(하드코딩 금지, exp 중복 검사, refresh token DB 저장). Process: 0.감지 → 1.인증 방식 확인 → 2.기존 auth 패턴 읽기 → 3.토큰 유틸+미들웨어+Claims 생성 → 4.env 안내. **Hexagonal 패턴 적용: Process에 포트 정의 → 어댑터 구현 단계 포함.**

- [ ] **Step 2: rust-middleware/SKILL.md 작성**

스펙의 "7. rust-middleware" 섹션 기반. Gotchas 3개(레이어 순서, tower-http 크레이트, rate limit 상태). Process: 0.감지 → 1.미들웨어 종류 → 2.기존 스택 읽기 → 3.생성+.layer() 등록 → 4.빌드 확인. **Hexagonal 패턴 적용: Process에 포트 정의 → 어댑터 구현 단계 포함.**

- [ ] **Step 3: rust-grpc/SKILL.md 작성**

스펙의 "8. rust-grpc" 섹션 기반. Gotchas 3개(protoc 설치, proto 경로, streaming). Process: 0.감지 → 1.서비스/RPC 확인 → 2.proto 생성 → 3.build.rs → 4.서비스 impl → 5.빌드 확인. **Hexagonal 패턴 적용: Process에 포트 정의 → 어댑터 구현 단계 포함.**

- [ ] **Step 4: 커밋**

```bash
git add rust-kit/skills/rust-auth/ rust-kit/skills/rust-middleware/ rust-kit/skills/rust-grpc/
git commit -m "feat(rust-kit): 생성형 스킬 — rust-auth, rust-middleware, rust-grpc"
```

---

### Task 9: 생성형 스킬 — rust-test, rust-docker, rust-l10n + 가이드형 rust-error

나머지 생성형 3개 + 가이드형 1개.

**Files:**
- Create: `rust-kit/skills/rust-test/SKILL.md`
- Create: `rust-kit/skills/rust-docker/SKILL.md`
- Create: `rust-kit/skills/rust-l10n/SKILL.md`
- Create: `rust-kit/skills/rust-error/SKILL.md`

- [ ] **Step 1: rust-test/SKILL.md 작성**

스펙의 "9. rust-test" 섹션 기반. Gotchas 3개(sqlx::test, tokio::test flavor, automock). Process: 0.감지 → 1.대상 분석 → 2.기존 테스트 패턴 → 3.테스트 타입 결정 → 4.코드 생성 → 5.실행 안내.

- [ ] **Step 2: rust-docker/SKILL.md 작성**

스펙의 "10. rust-docker" 섹션 기반. Gotchas 3개(정적 링크, cargo-chef, .sqlx 포함). Process: 0.감지 → 1.의존 서비스 확인 → 2.Dockerfile(cargo-chef) → 3.docker-compose → 4..dockerignore.

- [ ] **Step 3: rust-l10n/SKILL.md 작성**

스펙의 "12. rust-l10n" 섹션 기반. Gotchas 3개(플레이스홀더 일관성, 키 검증 없음, Accept-Language 파싱). Process: 0.감지(HAS_RUST_I18N/HAS_FLUENT) → 1.라이브러리 미설치 시 제안 → 2.기존 로케일 읽기 → 3.키 추가/수정 → 4.미들웨어 확인.

- [ ] **Step 4: rust-error/SKILL.md 작성**

스펙의 "11. rust-error" 섹션 기반. 가이드형 — 코드를 생성하기보다 패턴을 안내. Gotchas 3개(anyhow+thiserror 혼용 OK, unwrap 금지, From 누락). Process: 0.감지 → 1.현재 에러 패턴 분석 → 2.3계층 구조 제안 → 3.예시 코드 제시 → 4.개선점 안내.

- [ ] **Step 5: 커밋**

```bash
git add rust-kit/skills/rust-test/ rust-kit/skills/rust-docker/ rust-kit/skills/rust-l10n/ rust-kit/skills/rust-error/
git commit -m "feat(rust-kit): 생성형 스킬 3종 + 가이드형 — rust-test, rust-docker, rust-l10n, rust-error"
```

---

### Task 10: 감사형 스킬 — rust-audit + audit-criteria + rust-reviewer 에이전트

backend-kit의 backend-audit + backend-reviewer 패턴을 따라 구현.

**Files:**
- Create: `rust-kit/skills/rust-audit/SKILL.md`
- Create: `rust-kit/skills/rust-audit/references/audit-criteria.md`
- Create: `rust-kit/agents/rust-reviewer.md`

- [ ] **Step 1: audit-criteria.md 작성**

```markdown
# Rust Audit Criteria

rust-reviewer 에이전트가 사용하는 유일한 감사 기준. 카테고리별 PASS/FAIL 조건을 정의한다.

---

## 1. Ownership & Borrowing

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| 불필요한 clone | `.clone()` 호출이 빌림으로 대체 가능한 곳이 없다 | Rust Book Ch.4 |
| &String / &Vec 사용 | 함수 인자에 `&String` 대신 `&str`, `&Vec<T>` 대신 `&[T]` 사용 | Clippy |
| Lifetime 명시 | 생략 가능한 lifetime을 불필요하게 명시하지 않는다 | Rust RFC 141 |

## 2. Error Handling

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| unwrap/expect 사용 | 프로덕션 코드에 `.unwrap()` / `.expect()` 없다 (테스트 제외) | Clippy |
| 에러 타입 일관성 | 하나의 모듈/크레이트 내에서 에러 타입이 일관적이다 | thiserror docs |
| From impl | 에러 변환에 수동 매핑 대신 `#[from]` 또는 `From` impl 사용 | thiserror docs |

## 3. Async

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| blocking in async | `std::thread::sleep`, `std::fs::*` 등 blocking 호출이 async 함수 내에 없다 | Tokio docs |
| 불필요한 .await | 반환값을 사용하지 않는 `let _ = foo().await;` 패턴이 없다 (fire-and-forget이면 spawn) | Clippy |
| Mutex in async | `std::sync::Mutex` 대신 `tokio::sync::Mutex` 사용 (.await를 넘겨 잡으면) | Tokio docs |

## 4. Security

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| 시크릿 하드코딩 | API 키, JWT 시크릿, DB 비밀번호가 소스코드에 없다 | OWASP |
| SQL injection | 문자열 보간 대신 파라미터 바인딩 사용 (`sqlx::query!` / `$1`) | SQLx docs |
| 입력 검증 | 외부 입력(HTTP body, query param)에 길이/형식 검증이 있다 | OWASP |

## 5. Performance

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| 불필요한 allocation | 루프 내 불필요한 `String::new()` / `Vec::new()` 없다 | Clippy |
| collect 후 iterate | `.collect::<Vec<_>>()` 후 다시 `.iter()`하지 않는다 | Clippy |
| 사전 할당 | 크기를 아는 Vec에 `Vec::with_capacity()` 사용 | Rust Book |

## 6. Testing

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| 핵심 로직 테스트 | 비즈니스 로직 함수에 최소 1개 테스트 존재 | 일반 관행 |
| 에러 경로 테스트 | 주요 에러 경로에 대한 테스트 존재 | 일반 관행 |
| 테스트 격리 | 테스트 간 상태 공유가 없다 (글로벌 변수, 공유 파일 등) | cargo test docs |

## 7. API Design

| 기준 | PASS 조건 | 출처 |
|------|-----------|------|
| HTTP 메서드 일관성 | CRUD에 적절한 메서드 사용 (GET=조회, POST=생성, PUT/PATCH=수정, DELETE=삭제) | REST 관행 |
| 응답 코드 일관성 | 성공(200/201), 없음(404), 충돌(409), 서버 에러(500) 등 적절한 상태 코드 | HTTP spec |
| OpenAPI 정합 | HAS_UTOIPA면 모든 공개 엔드포인트에 `#[utoipa::path]` 존재 | utoipa docs |
```

- [ ] **Step 2: rust-audit/SKILL.md 작성**

```markdown
---
name: rust-audit
description: >
  Rust 코드를 원칙 기준으로 체계적으로 감사한다.
  카테고리별 PASS/FAIL 판정과 근거를 포함한 리포트를 생성한다.
  rust-reviewer 에이전트를 Agent 도구로 호출하여 독립 평가한다.
  "Rust 감사", "코드 검수", "rust audit", "코드 리뷰",
  "품질 검사" 같은 요청 시 트리거.
  빌드/테스트만 실행하려면 rust-run이나 rust-preflight를 사용한다.
argument-hint: "[quick|deep] [target-path]"
user-invocable: true
---

# Gotchas

1. **Rust 이외 도메인 평가 금지** — UI 디자인, 인프라 설정은 평가 대상이 아니다.
2. **추측성 FAIL 금지** — 실제 코드를 확인한 후 판정한다. "아마 문제가 있을 것"으로 FAIL하지 않는다.
3. **보안 카테고리 생략 금지** — 항상 Security 카테고리를 포함한다.
4. **deep 모드에서만 에이전트 호출** — quick 모드는 직접 검사한다.

# Process

## 0. 프로젝트 감지

`references/project-detection.md`의 절차를 실행하여 프로젝트 환경을 파악한다.

## 1. 대상 범위 결정

| 입력 | 범위 |
|------|------|
| 파일 경로 | 해당 파일만 |
| 디렉토리 경로 | 하위 전체 |
| 미지정 | `git diff --name-only` 기준 변경 파일 |

## 2. 모드 결정

| 모드 | 동작 |
|------|------|
| `quick` (기본) | 변경 파일만, 직접 검사, 경량 리포트 |
| `deep` | 전체 프로젝트, rust-reviewer 에이전트 위임, 상세 리포트 |

미지정 시: 변경 파일 5개 이하 → quick, 초과 → deep.

## 3-A. quick 모드 실행

references/audit-criteria.md를 읽고, 대상 파일을 직접 검사하여 카테고리별 PASS/FAIL 판정.

## 3-B. deep 모드 실행

rust-reviewer 에이전트를 Agent 도구로 호출:

```
subagent_type: "rust-kit:rust-reviewer"
prompt: |
  다음 파일을 Rust 원칙 기준으로 평가하라.
  감사 기준: rust-kit/skills/rust-audit/references/audit-criteria.md
  대상 파일: [목록]
```

## 4. 리포트 생성

| 카테고리 | 판정 | 근거 |
|----------|------|------|
| Ownership & Borrowing | {PASS/FAIL} | {파일:라인 + 설명} |
| Error Handling | {PASS/FAIL} | |
| Async | {PASS/FAIL} | |
| Security | {PASS/FAIL} | |
| Performance | {PASS/FAIL} | |
| Testing | {PASS/FAIL} | |
| API Design | {PASS/FAIL} | |

**최종 판정:** {APPROVE / REJECT}
**FAIL 수:** {N}개

# References

- references/audit-criteria.md — 카테고리별 PASS/FAIL 체크리스트
```

- [ ] **Step 3: rust-reviewer.md 에이전트 작성**

```markdown
---
name: rust-reviewer
description: >
  Rust 코드를 원칙 기준으로 독립 평가한다.
  rust-audit 스킬에서 Agent 도구로 위임받아 실행된다.
  카테고리별 PASS/FAIL 판정과 근거를 반환한다.
  단독 실행하지 않는다 — 반드시 rust-audit을 통해 호출.
tools: Read, Grep, Glob
model: sonnet
---

# Rust Reviewer

Rust 코드를 원칙 기준으로 평가하는 읽기 전용 에이전트.
코드를 수정하지 않는다. 결함을 찾는 것이 유일한 역할이다.

## 핵심 규칙

1. **Rust 원칙만 판정** — UI 디자인, 코드 스타일(fmt가 처리)은 평가 대상이 아니다.
2. **이진 판정** — PASS 또는 FAIL만 존재한다. "부분적 준수", "거의 통과" 없음.
3. **근거 필수** — 모든 FAIL에 `파일:라인` + 출처(원칙명)를 명시한다.
4. **칭찬 금지** — 긍정적 평가는 하지 않는다. PASS면 비고란을 비운다.
5. **1 FAIL = REJECT** — 하나라도 FAIL이면 전체 판정은 REJECT.

## 평가 카테고리

7개 카테고리를 아래 순서대로 평가한다. 세부 rule은 **반드시 `rust-kit/skills/rust-audit/references/audit-criteria.md`를 읽고 그 기준만 사용한다.**

1. Ownership & Borrowing
2. Error Handling
3. Async
4. Security
5. Performance
6. Testing
7. API Design

## 출력 포맷

| 카테고리 | 판정 | 파일:라인 | 근거 | 출처 |
|----------|------|-----------|------|------|

**최종 판정:** APPROVE / REJECT
**FAIL 수:** N개
```

- [ ] **Step 4: 커밋**

```bash
git add rust-kit/skills/rust-audit/ rust-kit/agents/
git commit -m "feat(rust-kit): 감사 스킬 + 에이전트 — rust-audit, audit-criteria, rust-reviewer"
```

---

### Task 11: 카이젠/리서치 스킬 (.claude/skills/)

**Files:**
- Create: `.claude/skills/rust-kaizen/SKILL.md`
- Create: `.claude/skills/rust-research/SKILL.md`

- [ ] **Step 1: rust-kaizen/SKILL.md 작성**

backend-kaizen 패턴을 따르되 경로만 rust-kit으로 치환:

```markdown
---
name: rust-kaizen
description: >
  rust-kit 스킬 품질을 docs/rust/ 리서치 문서 기준으로 주기적으로 개선한다.
  이 레포 개발용 스킬이며, rust-kit 플러그인에 포함되지 않는다.
  harness-kaizen, flutter-kaizen, design-kaizen과 동일한 패턴.
  "/rust-kaizen", "Rust 카이젠", "rust-kit 개선" 같은 요청 시 트리거.
  단순 버그 수정이나 기능 구현 요청에는 트리거하지 않는다.
argument-hint: ""
user-invocable: true
---

# Gotchas

1. **리서치 문서 없이 개선 금지** — docs/rust/ 문서를 먼저 읽고, 그 기준으로만 개선한다.
2. **스킬 삭제 금지** — 기존 스킬을 삭제하지 않는다. 개선만 한다.
3. **한 번에 1~2개 스킬만 개선** — 전체 스킬을 한 번에 수정하면 품질이 떨어진다.

# Process

## Step 1: 현황 분석

rust-kit/skills/ 디렉토리의 모든 SKILL.md를 읽고 현재 상태를 파악한다.

## Step 2: 리서치 문서 비교

docs/rust/ 리서치 문서와 스킬의 Gotchas, Process, 코드 예시를 비교한다.
차이가 있는 부분을 목록화한다.

## Step 3: 개선 우선순위

| 우선순위 | 기준 |
|----------|------|
| 높음 | 잘못된 정보, deprecated API 사용, 안티패턴 포함 |
| 중간 | 누락된 Gotchas, 불완전한 Process |
| 낮음 | 코드 예시 개선, 트리거 키워드 보완 |

## Step 4: 개선 실행

상위 1~2개 스킬을 개선한다. 각 개선마다:
1. 변경 전 내용
2. 변경 후 내용
3. 변경 근거 (리서치 문서 출처)

## Step 5: 검증

변경된 스킬의 evals를 확인하고, 필요하면 evals.json도 업데이트한다.

# References

- docs/rust/ — 리서치 문서 (SSOT)
- rust-kit/skills/ — 개선 대상 스킬
- rust-kit/evals/evals.json — 테스트 케이스
```

- [ ] **Step 2: rust-research/SKILL.md 작성**

backend-research 패턴을 따르되 경로만 치환:

```markdown
---
name: rust-research
description: >
  Rust 레퍼런스 소스를 크롤링/분석하여 docs/rust/ 문서를 갱신한다.
  이 레포 개발용 스킬이며, rust-kit 플러그인에 포함되지 않는다.
  "Rust 리서치", "rust research", "Rust 문서 갱신" 같은 요청 시 트리거.
argument-hint: "[category]"
user-invocable: true
---

# Gotchas

1. **기존 문서 구조 유지** — frontmatter, 섹션 순서를 바꾸지 않는다. 내용만 갱신.
2. **출처 없는 내용 금지** — 모든 원칙과 수치에 출처(URL 포함)를 명시한다.
3. **한 번에 전체 갱신 금지** — category 인자로 특정 카테고리만 갱신한다. 미지정 시 사용자에게 확인.

# Process

## Step 1: 대상 카테고리 결정

| 인자 | 대상 |
|------|------|
| `fundamentals` | docs/rust/fundamentals/ (6개 문서) |
| `web` | docs/rust/web/ (4개 문서) |
| `data` | docs/rust/data/ (3개 문서) |
| `protocols` | docs/rust/protocols/ (3개 문서) |
| `ops` | docs/rust/ops/ (3개 문서) |
| 미지정 | 사용자에게 확인 |

## Step 2: 리서치 실행

Codex 에이전트에 해당 카테고리의 최신 정보를 리서치 위임한다:
- 공식 문서 (docs.rs, Rust Book)
- 크레이트 최신 버전 및 변경사항
- 커뮤니티 추천 사항 (Rust subreddit, This Week in Rust)

## Step 3: 문서 갱신

리서치 결과를 기존 문서에 반영한다:
- `last_updated` 날짜 갱신
- 새 원칙/안티패턴 추가
- deprecated 내용 제거 또는 대체
- 수치 기준 업데이트
- 출처 URL 갱신

## Step 4: 커밋

갱신된 문서를 커밋한다.

# References

- docs/rust/ — 갱신 대상 문서
```

- [ ] **Step 3: 커밋**

```bash
git add .claude/skills/rust-kaizen/ .claude/skills/rust-research/
git commit -m "feat(rust-kit): 카이젠/리서치 스킬 — rust-kaizen, rust-research"
```

---

### Task 12: 레지스트리 등록 — marketplace.json + CLAUDE.md

**Files:**
- Modify: `.claude-plugin/marketplace.json`
- Modify: `CLAUDE.md`

- [ ] **Step 1: marketplace.json에 rust-kit 추가**

`plugins` 배열에 추가:

```json
{
  "name": "rust-kit",
  "source": "./rust-kit",
  "description": "[v0.1.0 · 2026-04-06] Rust 전용 백엔드 개발 워크플로우 플러그인 — 17종 스킬 + 에이전트"
}
```

- [ ] **Step 2: CLAUDE.md에 rust-kit 스킬 테이블 추가**

`## Repository Overview` 섹션에 rust-kit 설명 추가:
```markdown
- **rust-kit** — Rust 전용 백엔드 개발 워크플로우 플러그인 (17종 스킬 + 에이전트)
```

`## Skills Reference` > "이 레포 스킬 (플러그인 소속)" 테이블에 추가:

```markdown
**rust-kit — Rust 백엔드 개발 워크플로우 (17종)**

| 스킬/에이전트 | 용도 |
|---------------|------|
| `/rust-init` | 프로젝트 스캐폴딩 (workspace + toolchain + 구조) |
| `/rust-feature` | feature 모듈 스캐폴딩 |
| `/rust-api` | Axum 라우터/핸들러 + utoipa OpenAPI |
| `/rust-model` | SQLx 모델 + 마이그레이션 |
| `/rust-service` | 비즈니스 로직 서비스 레이어 |
| `/rust-auth` | JWT/OAuth 인증 레이어 |
| `/rust-middleware` | Axum 미들웨어 (CORS, logging, rate-limit) |
| `/rust-grpc` | tonic gRPC 서비스 |
| `/rust-test` | 테스트 코드 생성 (unit + integration) |
| `/rust-docker` | Dockerfile + docker-compose |
| `/rust-error` | 에러 처리 패턴 가이드 (thiserror/anyhow) |
| `/rust-l10n` | 백엔드 i18n (rust-i18n/fluent) |
| `/rust-run` | 빌드 프리미티브 개별 실행 (build, clippy, fmt, test, audit, check) |
| `/rust-build` | cargo build + clippy (rust-run wrapper) |
| `/rust-preflight` | pre-commit gate (fmt → clippy → test → audit) |
| `/rust-audit` | 코드 품질 감사 (quick/deep 모드) |
| `rust-reviewer` (에이전트) | rust-audit에서 호출. 읽기 전용 독립 평가 |
```

"이 레포 전용 스킬 (.claude/skills/)" 테이블에 추가:

```markdown
| `/rust-research` | Rust 레퍼런스 크롤링 → docs/rust/ 문서 갱신 |
| `/rust-kaizen` | rust-kit 스킬 개선 |
```

- [ ] **Step 3: 커밋**

```bash
git add .claude-plugin/marketplace.json CLAUDE.md
git commit -m "feat(rust-kit): marketplace.json + CLAUDE.md 등록"
```

---

### Task 13: evals 작성

**Files:**
- Modify: `rust-kit/evals/evals.json`

- [ ] **Step 1: evals.json에 17개 테스트 케이스 작성**

```json
{
  "skill_name": "rust-kit",
  "evals": [
    {
      "id": 1,
      "skill": "rust-init",
      "prompt": "Rust 백엔드 프로젝트 새로 만들어줘",
      "expected_output": "프로젝트 스캐폴딩 + Cargo.toml + rust-toolchain.toml",
      "assertions": [
        {"text": "rust-toolchain.toml 생성됨", "type": "output"},
        {"text": "Cargo.toml에 [workspace] 또는 [package] 존재", "type": "output"},
        {"text": "사용자에게 아키텍처 선택을 제안", "type": "behavior"},
        {"text": "cargo build 성공 확인 실행", "type": "behavior"}
      ]
    },
    {
      "id": 2,
      "skill": "rust-feature",
      "prompt": "users feature 모듈 추가해줘",
      "expected_output": "feature 모듈 파일 생성 + mod.rs 등록",
      "assertions": [
        {"text": "프로젝트 감지 실행", "type": "behavior"},
        {"text": "기존 feature 패턴 분석", "type": "behavior"},
        {"text": "mod.rs에 pub mod 등록", "type": "output"},
        {"text": "ARCH에 따른 구조 생성", "type": "behavior"}
      ]
    },
    {
      "id": 3,
      "skill": "rust-api",
      "prompt": "GET /users 엔드포인트 추가해줘",
      "expected_output": "핸들러 함수 + 라우터 등록",
      "assertions": [
        {"text": "프로젝트 감지 실행", "type": "behavior"},
        {"text": "기존 핸들러 패턴 읽기", "type": "behavior"},
        {"text": "async fn 핸들러 생성", "type": "output"},
        {"text": ".route() 등록", "type": "output"}
      ]
    },
    {
      "id": 4,
      "skill": "rust-model",
      "prompt": "users 테이블 모델 만들어줘",
      "expected_output": "구조체 + 마이그레이션 SQL + 쿼리 함수",
      "assertions": [
        {"text": "sqlx::FromRow derive", "type": "output"},
        {"text": "마이그레이션 SQL 파일 생성", "type": "output"},
        {"text": "DATABASE_URL 확인 안내", "type": "behavior"}
      ]
    },
    {
      "id": 5,
      "skill": "rust-service",
      "prompt": "UserService 만들어줘",
      "expected_output": "서비스 trait + impl",
      "assertions": [
        {"text": "trait 정의 생성", "type": "output"},
        {"text": "impl 생성", "type": "output"},
        {"text": "테스트 모듈 스켈레톤", "type": "output"}
      ]
    },
    {
      "id": 6,
      "skill": "rust-auth",
      "prompt": "JWT 인증 추가해줘",
      "expected_output": "토큰 유틸 + 미들웨어 + Claims",
      "assertions": [
        {"text": "Claims 구조체 생성", "type": "output"},
        {"text": "FromRequestParts impl", "type": "output"},
        {"text": "환경변수에서 시크릿 로드 (하드코딩 없음)", "type": "behavior"}
      ]
    },
    {
      "id": 7,
      "skill": "rust-middleware",
      "prompt": "CORS 미들웨어 추가해줘",
      "expected_output": "CorsLayer 설정 + .layer() 등록",
      "assertions": [
        {"text": "tower-http 크레이트 사용", "type": "output"},
        {"text": ".layer() 등록", "type": "output"},
        {"text": "레이어 순서 고려", "type": "behavior"}
      ]
    },
    {
      "id": 8,
      "skill": "rust-grpc",
      "prompt": "UserService gRPC 서비스 추가해줘",
      "expected_output": ".proto + build.rs + 서비스 impl",
      "assertions": [
        {"text": ".proto 파일 생성", "type": "output"},
        {"text": "build.rs tonic-build 설정", "type": "output"},
        {"text": "protoc 설치 안내", "type": "behavior"}
      ]
    },
    {
      "id": 9,
      "skill": "rust-test",
      "prompt": "UserService 테스트 만들어줘",
      "expected_output": "테스트 코드 생성",
      "assertions": [
        {"text": "대상 분석 (공개 함수, trait 목록)", "type": "behavior"},
        {"text": "#[cfg(test)] mod tests 또는 tests/ 파일", "type": "output"},
        {"text": "기존 테스트 패턴 따름", "type": "behavior"}
      ]
    },
    {
      "id": 10,
      "skill": "rust-docker",
      "prompt": "Dockerfile 만들어줘",
      "expected_output": "멀티스테이지 Dockerfile + docker-compose",
      "assertions": [
        {"text": "cargo-chef 기반 캐싱", "type": "output"},
        {"text": "멀티스테이지 빌드 (builder + runtime)", "type": "output"},
        {"text": ".dockerignore 생성", "type": "output"}
      ]
    },
    {
      "id": 11,
      "skill": "rust-error",
      "prompt": "에러 처리 패턴 알려줘",
      "expected_output": "3계층 에러 구조 가이드",
      "assertions": [
        {"text": "thiserror/anyhow 구분 설명", "type": "output"},
        {"text": "IntoResponse impl 예시", "type": "output"},
        {"text": "현재 프로젝트 에러 패턴 분석", "type": "behavior"}
      ]
    },
    {
      "id": 12,
      "skill": "rust-l10n",
      "prompt": "다국어 지원 추가해줘",
      "expected_output": "i18n 설정 + 로케일 파일 + 미들웨어",
      "assertions": [
        {"text": "HAS_RUST_I18N/HAS_FLUENT 감지", "type": "behavior"},
        {"text": "로케일 파일 생성", "type": "output"},
        {"text": "Accept-Language 미들웨어 설정", "type": "output"}
      ]
    },
    {
      "id": 13,
      "skill": "rust-run",
      "prompt": "clippy 돌려줘",
      "expected_output": "cargo clippy 실행 결과",
      "assertions": [
        {"text": "cargo clippy -- -D warnings 실행", "type": "behavior"},
        {"text": "IS_WORKSPACE면 --workspace 추가", "type": "behavior"},
        {"text": "결과 포맷 출력", "type": "output"}
      ]
    },
    {
      "id": 14,
      "skill": "rust-build",
      "prompt": "빌드해줘",
      "expected_output": "build + clippy 순서 실행",
      "assertions": [
        {"text": "build 먼저 실행", "type": "behavior"},
        {"text": "build 성공 후 clippy 실행", "type": "behavior"},
        {"text": "결과 리포트 테이블", "type": "output"}
      ]
    },
    {
      "id": 15,
      "skill": "rust-preflight",
      "prompt": "커밋 전 검사해줘",
      "expected_output": "fmt → clippy → test → audit 순서 실행 + 리포트",
      "assertions": [
        {"text": "fmt → clippy → test → audit 순서 고정", "type": "behavior"},
        {"text": "fmt 실패 시 자동 적용 후 재검사", "type": "behavior"},
        {"text": "audit은 non-blocking (WARN)", "type": "behavior"},
        {"text": "Preflight Report 테이블 출력", "type": "output"}
      ]
    },
    {
      "id": 16,
      "skill": "rust-audit",
      "prompt": "코드 리뷰해줘",
      "expected_output": "카테고리별 PASS/FAIL 감사 리포트",
      "assertions": [
        {"text": "audit-criteria.md 기준 사용", "type": "behavior"},
        {"text": "7개 카테고리 평가", "type": "output"},
        {"text": "deep 모드 시 rust-reviewer 에이전트 호출", "type": "behavior"},
        {"text": "최종 판정 APPROVE/REJECT", "type": "output"}
      ]
    },
    {
      "id": 17,
      "skill": "rust-kaizen",
      "prompt": "rust-kit 개선해줘",
      "expected_output": "리서치 문서 기준 스킬 개선",
      "assertions": [
        {"text": "docs/rust/ 문서를 먼저 읽음", "type": "behavior"},
        {"text": "한 번에 1~2개 스킬만 개선", "type": "behavior"},
        {"text": "변경 근거에 리서치 문서 출처 포함", "type": "output"}
      ]
    }
  ]
}
```

- [ ] **Step 2: 커밋**

```bash
git add rust-kit/evals/evals.json
git commit -m "feat(rust-kit): evals.json — 17개 스킬 테스트 케이스"
```

---

### Task 14: docs-site HTML 페이지 (17개)

`/docs-site` 스킬을 호출하여 docs/rust/ 리서치 문서 17개에 대응하는 HTML 페이지를 생성한다.

**Files:**
- Create: `docs/rust-kit/` 디렉토리 하위에 17개 HTML 파일
- Modify: `docs/index.html` (rust-kit 섹션 추가)

- [ ] **Step 1: /docs-site 스킬 호출**

```
/docs-site rust-kit
```

docs/rust/ 리서치 문서 17개를 읽어 standalone HTML로 변환하고 docs/rust-kit/ 디렉토리에 생성한다. docs/index.html에 rust-kit 섹션을 추가한다.

- [ ] **Step 2: 커밋**

```bash
git add docs/rust-kit/ docs/index.html
git commit -m "docs(rust-kit): docs-site HTML 페이지 17개 생성"
```

---

### Task 15: sync-docs 실행 + 최종 검증

**Files:**
- Modify: `rust-kit/README.md` (sync-docs가 AUTO 마커 갱신)

- [ ] **Step 1: sync-docs 실행**

```bash
python scripts/sync-docs.py rust-kit
```

README.md의 `<!-- AUTO:skills -->`, `<!-- AUTO:agents -->` 마커 사이를 자동 갱신한다.

- [ ] **Step 2: 전체 구조 검증**

```bash
# 필수 파일 존재 확인
ls rust-kit/.claude-plugin/plugin.json
ls rust-kit/references/project-detection.md
ls rust-kit/agents/rust-reviewer.md
ls rust-kit/skills/rust-*/SKILL.md | wc -l  # 16개여야 함
ls rust-kit/skills/rust-audit/references/audit-criteria.md
ls rust-kit/evals/evals.json
ls docs/rust/**/*.md | wc -l  # 20개여야 함
ls .claude/skills/rust-kaizen/SKILL.md
ls .claude/skills/rust-research/SKILL.md
```

- [ ] **Step 3: marketplace.json 검증**

```bash
python -c "import json; d=json.load(open('.claude-plugin/marketplace.json')); names=[p['name'] for p in d['plugins']]; assert 'rust-kit' in names, 'rust-kit not found'"
```

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "feat(rust-kit): 최종 검증 + sync-docs 완료"
```

---

## Self-Review 체크리스트

### 1. Spec Coverage
| 스펙 항목 | Task |
|-----------|------|
| 리서치 문서 20개 | Task 1-3 |
| plugin.json + project-detection | Task 4 |
| 실행형 스킬 3종 | Task 5 |
| 생성형 스킬 (init, feature) | Task 6 |
| 생성형 스킬 (api, model, service) | Task 7 |
| 생성형 스킬 (auth, middleware, grpc) | Task 8 |
| 생성형 스킬 (test, docker, l10n) + 가이드 (error) | Task 9 |
| 감사 스킬 + 에이전트 | Task 10 |
| 카이젠/리서치 스킬 | Task 11 |
| marketplace + CLAUDE.md | Task 12 |
| evals | Task 13 |
| docs-site HTML | Task 14 |
| sync-docs + 검증 | Task 15 |

### 2. Placeholder Scan
- TBD/TODO 없음 확인
- "Similar to Task N" 없음 확인
- 모든 코드 블록에 실제 내용 포함

### 3. Type Consistency
- 스킬 이름: rust-{name} 일관
- 감지 변수: $CARGO, IS_WORKSPACE, HAS_*, ARCH 일관
- audit-criteria 경로: `rust-kit/skills/rust-audit/references/audit-criteria.md` 일관
- 에이전트 이름: rust-reviewer 일관
