# onboarding-kit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** fit-pal의 setup-guide 스킬을 스택 무관 onboarding-kit 플러그인으로 격상. 그 시점 최신 정보 기반 외부 서비스 셋업 가이드 자동 생성.

**Architecture:** 1스킬 킷 (bambu-kit 패턴) + 이 레포 개발용 `/onboarding-kaizen`. 스킬 자체는 services/별 골격 없이 매 호출마다 WebFetch → Context7 → Codex 순서로 최신 정보 fetch. 누적된 feedback 메모리 3개(`setup-guide-stack-first`, `setup-guide-console-ui-fetch`, `setup-guide-site-distinction`)를 Gotchas로 반영.

**Tech Stack:** Markdown (SKILL.md, references/), JSON (plugin.json, marketplace.json, evals.json), Python (sync-docs.py, validate-plugin.py 검증).

**Spec:** `docs/onboarding-kit/design-2026-05-18.md`

---

## File Structure

플러그인 신규 생성:

```text
onboarding-kit/
├── .claude-plugin/plugin.json
├── skills/setup-guide/
│   ├── SKILL.md
│   ├── references/
│   │   ├── project-detection.md
│   │   ├── format-checklist.md
│   │   └── search-strategy.md
│   └── evals/evals.json
└── README.md
```

이 레포 개발용 스킬 신규 생성:

```text
.claude/skills/onboarding-kaizen/
├── SKILL.md
└── references/research-sources.md
```

기존 파일 수정:

- `.claude-plugin/marketplace.json` — plugins 배열에 onboarding-kit 추가
- `.claude/skills/kaizen-orchestrator/SKILL.md` — Phase 13 등록
- `.claude/skills/kaizen-orchestrator/references/phase-dependencies.md` — Phase 13 추가
- `.claude/skills/kaizen-orchestrator/references/phase-research-templates.md` — Phase 13 리서치 소스 테이블 추가
- `CLAUDE.md` — Repository Overview에 onboarding-kit 한 줄 추가

---

## Task 1: 사전 조사 — fit-pal 원본 + 기존 ~-kit 패턴 정리

**Files:**

- Read: `/Users/jackson/Hub/10_Dev/fit-pal/.claude/skills/setup-guide/SKILL.md`
- Read: `bambu-kit/skills/bambu-print-profile/SKILL.md`
- Read: `.claude/skills/kaizen-orchestrator/SKILL.md`
- Read: `.claude/skills/kaizen-orchestrator/references/phase-research-templates.md`

- [ ] **Step 1: fit-pal setup-guide 원본 읽기**

```bash
cat /Users/jackson/Hub/10_Dev/fit-pal/.claude/skills/setup-guide/SKILL.md
```

확인: 기존 Process(Phase 1~3), 가이드 포맷, 검색 전략 골격 파악.

- [ ] **Step 2: bambu-kit SKILL.md 패턴 확인 (1스킬 킷의 SKILL 형식)**

```bash
head -80 bambu-kit/skills/bambu-print-profile/SKILL.md
```

확인: frontmatter, Process 구조, references 참조 방식.

- [ ] **Step 3: kaizen-orchestrator 현재 Phase 목록 확인**

```bash
grep -n "### Step\|Phase " .claude/skills/kaizen-orchestrator/SKILL.md | head -30
```

확인: 마지막 Phase 번호 (예상 Phase 12 reflect-kit), Phase 13으로 onboarding-kit 추가 위치.

- [ ] **Step 4: phase-research-templates.md의 기존 Phase 행 한 개 확인 (Phase 13 추가 시 동일 형식)**

```bash
grep -A 10 "Phase 12" .claude/skills/kaizen-orchestrator/references/phase-research-templates.md | head -20
```

조사 결과 별도 커밋 없음. 다음 task로 이동.

---

## Task 2: onboarding-kit 폴더 + plugin.json 생성

**Files:**

- Create: `onboarding-kit/.claude-plugin/plugin.json`

- [ ] **Step 1: 폴더 구조 생성**

```bash
mkdir -p onboarding-kit/.claude-plugin onboarding-kit/skills/setup-guide/references onboarding-kit/skills/setup-guide/evals
```

- [ ] **Step 2: plugin.json 작성**

```json
{
  "name": "onboarding-kit",
  "description": "스택 무관 외부 서비스 셋업 가이드 자동 생성 — 그 시점 최신 정보 기반 step-by-step 가이드 (Firebase, GCP, AWS, FCM, OAuth, Stripe 등)",
  "version": "0.1.0",
  "author": {
    "name": "Jackson Kim"
  },
  "repository": "https://github.com/joo6077/claude-plugins",
  "license": "MIT",
  "keywords": [
    "onboarding",
    "setup-guide",
    "firebase",
    "fcm",
    "gcp",
    "aws",
    "oauth",
    "external-service",
    "step-by-step"
  ]
}
```

저장 경로: `onboarding-kit/.claude-plugin/plugin.json`

- [ ] **Step 3: 검증**

```bash
python3 -c "import json; print(json.load(open('onboarding-kit/.claude-plugin/plugin.json'))['name'])"
```

Expected: `onboarding-kit`

---

## Task 3: references/project-detection.md 작성

**Files:**

- Create: `onboarding-kit/skills/setup-guide/references/project-detection.md`

- [ ] **Step 1: 파일 작성**

내용:

```markdown
# Project Detection — 스택 무관 탐지 패턴

`/setup-guide` 스킬이 프로젝트 스택을 자동 감지할 때 참조한다. 단일 스택부터 멀티 스택 모노레포까지 커버한다.

## 의존성 파일 → 스택 매핑

| 파일 (글로브 탐색) | 스택 | 비고 |
|------------------|------|------|
| `pubspec.yaml` | Flutter / Dart | `flutter:` 키 존재 시 Flutter 확정 |
| `Cargo.toml` | Rust | `[workspace]` 있으면 워크스페이스 |
| `package.json` | Node.js / JS | `dependencies` 안의 `react`/`next`/`vue` 등으로 세분 |
| `requirements.txt` / `pyproject.toml` | Python | poetry, pip, uv 등 |
| `go.mod` | Go | |
| `Gemfile` | Ruby | Rails 여부는 `rails` gem 포함 확인 |
| `composer.json` | PHP | Laravel 여부 별도 확인 |
| `build.gradle` / `build.gradle.kts` | JVM (Android/Kotlin/Java) | |
| `Podfile` | iOS 네이티브 (Swift/Obj-C) | Flutter도 ios/Podfile 보유 — 우선순위 낮음 |
| `*.xcodeproj` / `*.xcworkspace` | iOS 네이티브 | |
| `.csproj` / `.sln` | .NET | |

## 멀티스택 모노레포 탐지

- 루트에 위 파일 여러 개가 있으면 모노레포로 판단
- 하위 디렉토리(예: `app/`, `server/`, `web/`)별로 다시 스캔
- 사용자에게 명시적으로 어느 스택의 가이드를 만들지 확인

## 외부 서비스 의존성 grep 패턴

코드 안의 SDK 사용 흔적을 찾아 활성 서비스 목록 도출:

| 서비스 | grep 패턴 |
|--------|----------|
| Firebase | `firebase_core`, `firebase_messaging`, `FirebaseApp.configure`, `import { initializeApp } from "firebase/app"` |
| GCP | `google-cloud-`, `from google.cloud`, `GOOGLE_APPLICATION_CREDENTIALS` |
| AWS | `boto3`, `aws-sdk`, `@aws-sdk/`, `import software.amazon.awssdk` |
| Stripe | `stripe.api_key`, `import Stripe from "stripe"`, `stripe.Client(` |
| Sentry | `Sentry.init`, `sentry-sdk`, `sentry_dsn` |
| OAuth | `oauth2`, `passport-`, `next-auth`, `Sign in with` |

## 인프라/환경 파일

- `.env*` — 환경변수에서 외부 서비스 키 흔적 (`FIREBASE_*`, `AWS_*`, `STRIPE_*`)
- `docker-compose*.yml`, `Dockerfile` — 컨테이너 의존성
- `terraform/*.tf` — IaC 리소스
- `k8s/*.yaml`, `helm/` — Kubernetes 매니페스트

## 탐지 결과 0건일 때

사용자에게 명시적 질문: "프로젝트 스택이 자동 감지되지 않습니다. 어떤 환경에서 진행하시나요? (Flutter / 네이티브 iOS / React Native / Node 백엔드 / 기타)"
```

저장: `onboarding-kit/skills/setup-guide/references/project-detection.md`

- [ ] **Step 2: 검증**

```bash
wc -l onboarding-kit/skills/setup-guide/references/project-detection.md
```

Expected: 50+ 라인.

---

## Task 4: references/format-checklist.md 작성

**Files:**

- Create: `onboarding-kit/skills/setup-guide/references/format-checklist.md`

- [ ] **Step 1: 파일 작성**

내용:

```markdown
# Format Checklist — 가이드 MD 포맷 표준

생성되는 가이드 MD는 아래 11개 섹션을 **반드시 이 순서로** 포함한다. 사용자가 따라가다 막히지 않도록 각 섹션의 누락 방지 체크리스트.

## 필수 섹션 순서

### 1. 헤더

- 가이드 제목 (`# <서비스명> 설정 가이드`)
- `> 작성일: YYYY-MM-DD`
- `> 기준 버전: <SDK/CLI 버전>` (가능한 한 정확한 버전 명시)
- `> 공식 문서: <URL>` (1차 출처)

### 2. 사전 요구사항

- 필요한 계정/권한 (예: Apple Developer Program 가입)
- 설치해야 할 CLI 도구 (버전 포함)
- 프로젝트 전제조건

> **사이트 혼동 주의 박스** (Apple/Google/AWS 가이드는 필수): 진입할 사이트 URL과 다른 비슷한 사이트의 차이를 표로 명시.

### 3. 설정 단계 (Step 1~N)

각 Step은 3단 구조:

```
**어디서:** <URL 또는 터미널 위치>

**무엇을:**

1. <구체적 액션 — 클릭 경로 또는 명령어>
2. ...

**확인 방법:** <성공 검증 방법 — 명령어, 콘솔 위치, 예상 출력>
```

각 Step에 필요시:

- ⚠️ 주의 박스 — 흔한 실수
- ❌ Deprecated / ✅ 현재 권장 박스 — 더 이상 안 쓰는 방법

### 4. 프로젝트 코드 변경

스택별로 변경할 파일과 코드를 구체적으로:

- 파일 경로 명시 (예: `lib/main.dart`, `ios/Runner/AppDelegate.swift`)
- 추가/변경할 코드 블록 (언어 라벨 필수)
- 설정 파일 변경 (예: `pubspec.yaml`, `.env`)

### 5. 권한 / IAM

| 시스템 | 필요한 권한 | 누구에게 |
|--------|------------|----------|

최소 권한 원칙. 셋업용 권한과 운영용 권한 분리 권장.

### 6. 비용

- 무료 한도 (예: FCM 무제한, Firebase Spark plan)
- 유료 진입점 (어느 시점부터 과금)
- 예상 월비용 범위

### 7. 환경 분기 (dev / staging / prod)

- 환경별로 별도 프로젝트가 필요한지
- 환경별 키/설정 파일 분리 패턴
- Build Configuration 별 차이

### 8. Rollback / 정리

- 잘못 설정했을 때 되돌리는 방법 (구체적 명령어 또는 콘솔 경로)
- 사용 안 할 때 리소스 정리 (과금 방지)

### 9. 보안 체크리스트

- [ ] 비밀 키 (`.p8`, JSON service account 등) 안전 저장소 보관
- [ ] `.gitignore`에 비밀 파일 패턴 추가
- [ ] 클라이언트 노출 가능 키와 서버 전용 키 구분
- [ ] FCM 등록 토큰/사용자 토큰의 서버 측 격리

### 10. 검증 체크리스트

- [ ] <검증 항목 1> — 확인 명령어/위치
- [ ] <검증 항목 2>
- [ ] <검증 항목 3>

### 11. 트러블슈팅

각 항목:

```
**증상:** <에러 메시지 또는 현상>
**원인:** <원인 분석>
**해결:**

1. <구체적 단계>
2. ...
```

## 톤 / 표기 규칙

- 모든 클릭 경로는 `→` 화살표로 (`프로젝트 설정 ⚙️ → 클라우드 메시징 탭 → Apple 앱 구성`)
- 명령어는 코드 블록 + 언어 라벨 (`bash`, `swift`, `dart` 등)
- 한국어 라벨과 영어 라벨 병기 (`업로드 (Upload)`) — 콘솔이 다국어 지원 시 사용자가 화면에서 매칭 가능하게
- 외부 도메인은 마크다운 링크로 (`[Identifiers 페이지](https://...)`) — bare URL 금지
- 어려운 용어는 괄호 풀이 (`Service Account (서비스 계정 — 자동화용 가상 사용자)`)
```

저장: `onboarding-kit/skills/setup-guide/references/format-checklist.md`

- [ ] **Step 2: 검증**

```bash
grep -c "^### " onboarding-kit/skills/setup-guide/references/format-checklist.md
```

Expected: 11+ (11개 섹션 + 톤 규칙).

---

## Task 5: references/search-strategy.md 작성

**Files:**

- Create: `onboarding-kit/skills/setup-guide/references/search-strategy.md`

- [ ] **Step 1: 파일 작성**

내용:

```markdown
# Search Strategy — 최신 정보 수집 우선순위

`/setup-guide`가 그 시점 최신 정보를 수집할 때 따르는 순서. 학습 데이터 추측은 금지.

## 우선순위 (위에서 아래로)

### 1. WebFetch — 콘솔 UI / 공식 help 페이지

가장 빠르고 정확. 콘솔 클릭 경로, 메뉴 라벨, + 버튼 옵션 리스트는 매번 WebFetch.

주요 출처:

- **Apple**: `developer.apple.com/help/account/` 하위 (identifiers/keys/devices/profiles)
- **Firebase**: `firebase.google.com/docs/`
- **Google Cloud**: `cloud.google.com/docs/`
- **AWS**: `docs.aws.amazon.com/`
- **Stripe**: `stripe.com/docs/`
- **Sentry**: `docs.sentry.io/`

WebFetch 프롬프트 예시: "Apple Developer Portal에서 새 App ID를 등록하는 정확한 클릭 경로를 step-by-step으로 추출해줘. 좌측 메뉴 위치, + 버튼 이후 옵션, Capabilities 체크리스트 위치, 최종 등록 버튼 라벨까지. 한국어로 정리."

### 2. Context7 — SDK API 문서

라이브러리/SDK의 메서드 시그니처, 초기화 코드, 마이그레이션 가이드.

흐름:

1. `mcp__context7__resolve-library-id` — 라이브러리 ID 검색
2. `mcp__context7__query-docs` — 구체 API 질의

대상: firebase-ios-sdk, FlutterFire, AWS SDK, Stripe SDK 등.

### 3. Codex 위임 — 정책 / 교차검증 / 깊은 분석

`codex-rescue` 에이전트에 `MODE=research`, `--write` 없이 위임.

언제 쓸지:

- 정책 사실 검증 (예: Apple Bundle ID 변경 정책)
- 여러 출처 교차검증 필요
- WebFetch 한 페이지로 답이 안 나올 때
- 깊은 분석/판단 필요

언제 안 쓸지:

- 단순 페이지 fetch (WebFetch가 더 빠름)
- API 시그니처 (Context7가 더 정확)

`~/.claude/codex-prompt-template.md` 템플릿 필수.

### 4. WebSearch — fallback

위 세 가지로 안 잡힐 때만. 검색어에 **현재 연도 필수** (`2026 Firebase setup`). 연도 없으면 deprecated 결과가 상위 노출됨.

## Deprecated 감지 규칙

매번 검색 결과에서 아래 확인:

- 문서/페이지의 **마지막 업데이트 날짜** — 2년+ 자동 ⚠️ 경고 박스
- 본문에 "deprecated", "legacy", "old method", "will be removed", "removed in" 키워드
- API/SDK 메이저 버전 변경 안내
- "use X instead" 같은 대체 권장

감지 시 가이드에 명시:

```
> **❌ Deprecated:** <옛 방법 + 비추천 이유>
> **✅ 현재 권장:** <새 방법 + 이유>
```

## 검색 결과 검증 체크리스트

- [ ] 공식 도메인 (developer.apple.com, firebase.google.com 등) URL인지 확인
- [ ] 문서 마지막 업데이트 날짜 추출 (없으면 추정 메모)
- [ ] 본문에 deprecated 경고 키워드 grep
- [ ] 여러 페이지에서 교차 확인 (한 페이지 정보를 절대적으로 신뢰하지 않기)
```

저장: `onboarding-kit/skills/setup-guide/references/search-strategy.md`

- [ ] **Step 2: 검증**

```bash
grep -c "### " onboarding-kit/skills/setup-guide/references/search-strategy.md
```

Expected: 5+ 섹션.

---

## Task 6: skills/setup-guide/SKILL.md 작성

**Files:**

- Create: `onboarding-kit/skills/setup-guide/SKILL.md`

- [ ] **Step 1: SKILL.md frontmatter + 본문 작성**

내용:

```markdown
---
name: setup-guide
description: "프로젝트의 외부 서비스(Firebase, GCP, AWS, FCM, OAuth, Stripe 등) 설정 가이드를 그 시점 최신 정보 기준으로 step-by-step MD 문서로 생성한다. 콘솔/대시보드 클릭 경로, 인증 키 발급, 프로젝트 코드 연동까지 매번 1차 출처(공식 docs)를 fetch하여 deprecated 정보로 사용자가 헤매지 않게 한다. 사용자가 'FCM 설정해야 해', 'Firebase 어떻게 연동해?', 'GCP 설정 가이드', '외부 서비스 설정', 'push notification 세팅', 'OAuth 설정', '서드파티 연동 방법', '셋업 가이드 만들어줘' 등을 언급하면 이 스킬을 사용한다. 설정/세팅/연동/가이드 키워드와 외부 서비스명이 함께 나오면 반드시 트리거."
argument-hint: "[서비스명] 또는 빈값(전체 스캔)"
user-invocable: true
---

프로젝트를 분석하고 그 시점 최신 정보로 1차 출처를 fetch하여, 외부 서비스 설정 가이드 MD를 step-by-step으로 생성한다.

## Input

`$ARGUMENTS`:

- 특정 서비스: `/setup-guide firebase`, `/setup-guide fcm ios`, `/setup-guide stripe`
- 전체 스캔: `/setup-guide` (인자 없음) → 프로젝트 전체를 분석하여 외부 서비스 목록 도출 + 사용자에게 선택지 제시

## 핵심 원칙

이 스킬의 존재 이유는 **deprecated된 정보로 사용자가 헤매지 않도록** 하는 것이다.

- 학습 데이터의 콘솔 UI 정보는 자주 outdated → **매번 WebFetch로 공식 help 페이지 직접 조회**
- 검색 시 반드시 **현재 연도** 포함 — 연도 없이 검색하면 2~3년 전 deprecated 결과가 상위 노출
- 버전 번호, CLI 명령어, 콘솔 UI 경로는 **구체적으로** — 모호한 안내는 콘솔 UI 변경 시마다 사용자가 헤맴
- deprecated 발견 시 **명시적 경고** + 현재 권장 방법

## Gotchas (반복 실수 방지)

### Gotcha 1: 스택 확정 없이 가이드 작성 금지

가이드 작성을 시작하기 전에 **반드시 프로젝트 스택을 확정**한다. Flutter / 네이티브 iOS / React Native / Node 백엔드는 같은 서비스라도 SDK 설치 명령, 초기화 코드, CLI 자동화 도구가 완전히 다르다.

탐지 방법은 `references/project-detection.md` 참조.

스택 확정 실패 시 → 사용자에게 명시적으로 묻기 ("Flutter iOS 기준? 네이티브 Swift 기준?")

### Gotcha 2: 콘솔 UI 클릭 경로는 학습 데이터 추측 금지

Apple Developer Portal, Firebase Console 등의 + 버튼 옵션, 메뉴 라벨, 화면 순서는 자주 바뀐다. 학습 데이터로 답하면 사용자가 화면에서 못 찾는다.

**WebFetch 우선** — Codex 위임보다 빠르고 정확. `references/search-strategy.md` 참조.

### Gotcha 3: 사이트 혼동 (Apple)

Apple 셋업은 두 사이트가 완전히 다르다:

- **App Store Connect** (`appstoreconnect.apple.com`) — 앱 출시·심사 관리. 셋업 단계엔 거의 안 들어감
- **Apple Developer Portal** (`developer.apple.com/account`) — App ID·인증서·키 발급. 셋업의 99%가 여기

생성하는 가이드 사전 요구사항 섹션 맨 위에 두 사이트 차이 표 박기.

같은 패턴이 다른 플랫폼에도 있음:
- Google Cloud: GCP Console vs Firebase Console
- AWS: AWS Console vs AWS Marketplace

### Gotcha 4: 글로벌 유니크 식별자 충돌

Bundle ID (Apple), Package Name (Android), Project ID (GCP/Firebase) 등은 **전 세계 모든 계정에 걸쳐 유니크**해야 한다. 흔한 조합(`com.fitpal.app`)은 누가 선점했을 확률 높음.

가이드 트러블슈팅에 "not available" 에러 케이스 자동 포함. 해결책: 조직/도메인 reverse prefix 강조.

### Gotcha 5: 등록 ≠ 활성화

App ID 등록과 Provisioning Profile 생성은 별개. Firebase 프로젝트 생성과 APNs Key 업로드는 별개. 단계별 분리 명시.

### Gotcha 6: 출시 후 변경 불가 정책

Bundle ID는 빌드 업로드 후 변경 불가. Firebase Project ID도 생성 후 변경 불가. 가이드 사전 요구사항에 경고.

## Process

### Phase 1: 스택 + 외부 서비스 탐지

1. **스택 확정** — `references/project-detection.md`의 의존성 파일 매핑 따라 스캔. 멀티스택 모노레포면 사용자에게 명시적 확인.
2. **외부 서비스 의존성 탐지** — 의존성 파일, 설정 파일(`.env*`, `docker-compose*.yml`, `terraform/`), 코드 grep (SDK 초기화 패턴) 종합.
3. **인자 처리**:
   - 특정 서비스 지정 → 해당 서비스 집중
   - 빈 인자 → 탐지한 서비스 목록 사용자에게 제시 + 어떤 가이드 만들지 선택받기

### Phase 2: 최신 정보 수집

`references/search-strategy.md`의 우선순위에 따라:

1. **WebFetch** — 콘솔 UI/help 페이지 직접 fetch
2. **Context7** — SDK API 문서 (`resolve-library-id` → `query-docs`)
3. **Codex 위임** — 정책 검증, 교차검증, 깊은 분석 필요 시
4. **WebSearch** — fallback, 검색어에 현재 연도 필수

**Deprecated 감지**: 문서 마지막 업데이트 2년+, "deprecated/legacy/will be removed" 키워드, 메이저 버전 변경 안내 → 가이드에 ❌/✅ 박스로 명시.

### Phase 3: 가이드 MD 생성

`references/format-checklist.md`의 11개 섹션을 **순서대로** 채운다.

저장 위치: 기본 `docs/setup/<서비스명>/<기능명>.md`. 사용자가 위치 지정하면 그쪽.

여러 하위 기능(예: Firebase → FCM, Analytics, Crashlytics)이면 각각 별도 파일로 분리.

### Phase 4: 검증 + 완료 안내

1. 생성된 가이드의 모든 외부 URL이 공식 도메인인지 확인 (`references/search-strategy.md`)
2. 11개 섹션 누락 확인
3. 사용자에게 파일 경로 + "막히는 부분 알려주세요" 안내. 사용자가 코드 변경에 도움 필요하면 직접 도와줄 수 있음 안내.

## References

- `references/project-detection.md` — 스택 탐지 패턴
- `references/format-checklist.md` — 가이드 MD 11개 섹션 체크리스트
- `references/search-strategy.md` — WebFetch → Context7 → Codex → WebSearch 우선순위
```

저장: `onboarding-kit/skills/setup-guide/SKILL.md`

- [ ] **Step 2: SKILL.md 형식 검증**

```bash
head -10 onboarding-kit/skills/setup-guide/SKILL.md
```

Expected: frontmatter (`name:`, `description:`, `argument-hint:`, `user-invocable:`) 정상.

```bash
grep -c "^### Gotcha" onboarding-kit/skills/setup-guide/SKILL.md
```

Expected: 6 (Gotcha 1~6).

```bash
grep -c "^### Phase" onboarding-kit/skills/setup-guide/SKILL.md
```

Expected: 4 (Phase 1~4).

---

## Task 7: evals/evals.json 작성

**Files:**

- Create: `onboarding-kit/skills/setup-guide/evals/evals.json`

- [ ] **Step 1: 4개 시나리오 작성**

내용:

```json
{
  "skill": "setup-guide",
  "version": "0.1.0",
  "cases": [
    {
      "id": "flutter-fcm-ios",
      "description": "Flutter 프로젝트에서 FCM iOS 가이드 생성. flutterfire CLI 흐름이 정확히 포함되어야 함",
      "prompt": "FCM iOS 설정해줘",
      "setup": {
        "project_files": ["pubspec.yaml", "ios/Podfile", "lib/main.dart"],
        "pubspec_contains": ["flutter:", "dependencies:"]
      },
      "assertions": [
        "stack_detected == 'flutter'",
        "guide_includes('flutterfire configure')",
        "guide_includes('firebase_messaging')",
        "guide_includes('FirebaseApp.configure()')",
        "guide_does_not_include('Swift Package Manager 검색창에 firebase-ios-sdk') — Flutter는 SPM 흐름 아님",
        "guide_includes('App Store Connect vs Apple Developer Portal 사이트 차이')",
        "guide_includes_section('보안 체크리스트')",
        "guide_includes_section('환경 분기')"
      ]
    },
    {
      "id": "stack-not-detected",
      "description": "스택 탐지 0건 → 사용자에게 명시적 확인 요청",
      "prompt": "Firebase 설정 가이드 만들어줘",
      "setup": {
        "project_files": []
      },
      "assertions": [
        "asks_user_for_stack_confirmation == true",
        "does_not_generate_guide_before_confirmation == true"
      ]
    },
    {
      "id": "apple-site-distinction",
      "description": "Apple 관련 가이드는 사전 요구사항에 App Store Connect vs Developer Portal 차이 표 포함",
      "prompt": "iOS Sign in with Apple 설정 가이드",
      "setup": {
        "project_files": ["ios/Runner.xcodeproj"]
      },
      "assertions": [
        "guide_includes_section('사전 요구사항')",
        "guide_includes('appstoreconnect.apple.com')",
        "guide_includes('developer.apple.com/account')",
        "guide_includes('사이트 혼동 주의')"
      ]
    },
    {
      "id": "deprecated-api-warning",
      "description": "검색 결과에 deprecated API가 있으면 ❌ Deprecated / ✅ 현재 권장 박스 자동 삽입",
      "prompt": "Firebase Cloud Messaging APNs 인증 설정",
      "setup": {
        "expected_deprecated_keywords": ["APNs Certificate (.p12)"]
      },
      "assertions": [
        "guide_includes('❌ Deprecated')",
        "guide_includes('✅ 현재 권장')",
        "guide_includes('.p8')",
        "guide_includes_recommended_alternative == true"
      ]
    }
  ]
}
```

저장: `onboarding-kit/skills/setup-guide/evals/evals.json`

- [ ] **Step 2: JSON 형식 검증**

```bash
python3 -c "import json; d=json.load(open('onboarding-kit/skills/setup-guide/evals/evals.json')); print(len(d['cases']))"
```

Expected: `4`

---

## Task 8: README.md 작성

**Files:**

- Create: `onboarding-kit/README.md`

- [ ] **Step 1: README.md 작성 (AUTO 마커 포함)**

내용:

```markdown
# onboarding-kit

스택 무관 외부 서비스 셋업 가이드 자동 생성 플러그인.

## 개요

프로젝트의 외부 서비스(Firebase, GCP, AWS, FCM, OAuth, Stripe, Sentry 등)를 셋업할 때 **그 시점 최신 정보 기준 step-by-step 가이드 MD**를 자동 생성한다. 학습 데이터의 outdated 정보가 아니라 매 호출마다 WebFetch → Context7 → Codex 순서로 1차 출처를 fetch하여 deprecated 정보로 사용자가 헤매지 않게 한다.

bambu-kit과 같은 도구형 1스킬 킷. 스택 무관 — Flutter / 네이티브 / React Native / Node / Python / Rust 등 어떤 프로젝트에서도 동작.

## 스킬

<!-- AUTO:skills:start -->
| 스킬 | 용도 |
|------|------|
| `/setup-guide` | 그 시점 최신 정보 기준 외부 서비스 셋업 가이드 step-by-step MD 자동 생성 |
<!-- AUTO:skills:end -->

트리거 키워드: "FCM 설정해야 해", "Firebase 어떻게 연동해?", "GCP 설정 가이드", "OAuth 설정", "셋업 가이드 만들어줘".

## 사용 흐름

1. 프로젝트 스택 자동 탐지 (`pubspec.yaml` / `Cargo.toml` / `package.json` / `requirements.txt` / `Podfile` 등)
2. 외부 서비스 의존성 grep + 사용자 확인
3. WebFetch로 공식 docs/help 페이지 직접 조회 → 최신 콘솔 UI 경로 확보
4. Context7으로 SDK API 문서 조회
5. 11개 섹션 표준 포맷(헤더 / 사전 요구사항 / 설정 단계 / 코드 변경 / 권한 / 비용 / 환경 분기 / Rollback / 보안 / 검증 / 트러블슈팅)으로 가이드 MD 생성
6. 사용자에게 파일 경로 + 다음 단계 안내

## 핵심 가치

- **그 시점 최신 정보** — 학습 데이터 추측 금지, 매 호출 시 1차 출처 fetch
- **스택 무관** — 의존성 파일 자동 탐지, 멀티스택 모노레포는 사용자에게 확인
- **사이트 혼동 사전 박기** — Apple/Google/AWS의 두 콘솔 차이를 가이드 사전 요구사항에 표로
- **deprecated 자동 경고** — 2년+ 문서, 키워드 grep, ❌/✅ 박스 자동 삽입

## 카이젠

이 레포 개발용으로 `.claude/skills/onboarding-kaizen/`이 존재한다 (플러그인에는 포함 X). bambu-kaizen 패턴과 동일하게 SKILL.md/references/evals를 주기적으로 개선한다.

- 수동 호출: `/onboarding-kaizen`
- 전체 카이젠: `/kaizen` (Phase 13으로 자동 실행)
- 사용자 피드백 메모리(`feedback_setup_guide_*`)가 3개 이상 누적되면 자동 트리거 후보

## License

MIT
```

저장: `onboarding-kit/README.md`

- [ ] **Step 2: README.md 형식 검증**

```bash
grep -c "AUTO:" onboarding-kit/README.md
```

Expected: `2` (AUTO:skills:start + AUTO:skills:end).

---

## Task 9: .claude/skills/onboarding-kaizen/ 생성

**Files:**

- Create: `.claude/skills/onboarding-kaizen/SKILL.md`
- Create: `.claude/skills/onboarding-kaizen/references/research-sources.md`

- [ ] **Step 1: SKILL.md 작성**

내용:

```markdown
---
name: onboarding-kaizen
description: onboarding-kit 스킬(setup-guide) 품질을 외부 docs/help 페이지 변경, 사용자 피드백 메모리, marketplace 트렌드 기반으로 주기적으로 개선한다. 이 레포 개발용 스킬이며, onboarding-kit 플러그인에는 포함되지 않는다. bambu-kaizen / rust-kaizen / flutter-kaizen 패턴과 동일. "/onboarding-kaizen", "온보딩 카이젠", "onboarding-kit 개선", "셋업 가이드 스킬 개선" 같은 요청 시 트리거. 단순 버그 수정이나 기능 구현 요청에는 트리거하지 않는다.
argument-hint: ""
user-invocable: true
---

onboarding-kit의 `/setup-guide` SKILL.md, references/ 3종, evals/evals.json을 주기적으로 개선한다.

## 트리거

- 주 1회 cron 자동 실행 (kaizen-orchestrator Phase 13)
- 수동 호출: `/onboarding-kaizen`
- 이벤트: `feedback_setup_guide_*` 메모리가 3개 이상 누적되면 자동 트리거 후보
- 이벤트: setup-guide 호출 결과의 사용자 부정 피드백 누적

## Process

### Phase 1: 데이터 수집

1. **사용자 피드백 메모리 스캔** — `~/.claude/projects/-Users-jackson-Hub-10-Dev-claude-plugins/memory/feedback_setup_guide_*.md`
2. **외부 출처 변경 확인** — `references/research-sources.md`의 출처 목록을 WebFetch / Codex로 최신화 확인
3. **marketplace 트렌드** — claude-plugins 마켓플레이스에 새 onboarding/setup 관련 플러그인 등장 여부

### Phase 2: 개선 후보 도출

- SKILL.md Gotchas에 추가할 새 패턴 (메모리 기반)
- references/ 3종에 추가/수정할 항목
- evals/evals.json에 추가할 시나리오

### Phase 3: Sprint Contract 작성 + 구현

`harness/skills/sprint-contract`로 완료 조건 정의 후 구현. 단순 텍스트 수정에서 큰 구조 변경까지 모두 contract 기반.

### Phase 4: QA Evaluator

`harness/agents/qa-evaluator`로 APPROVE/REJECT 판정. REJECT 시 수정 후 재평가.

### Phase 5: 커밋 + 릴리스

개선이 의미 있으면 onboarding-kit 버전 bump + marketplace.json 갱신 (`/release onboarding-kit patch`).

## References

- `references/research-sources.md` — 외부 출처 목록 + 폴링 빈도
```

저장: `.claude/skills/onboarding-kaizen/SKILL.md`

- [ ] **Step 2: references/research-sources.md 작성**

내용:

```markdown
# Research Sources — onboarding-kit kaizen

`/onboarding-kaizen` Phase 1 데이터 수집 시 폴링하는 외부 출처 목록.

## 1차 출처 (공식 docs/help)

| 서비스 | URL | 폴링 빈도 |
|--------|-----|-----------|
| Apple Developer Account Help | https://developer.apple.com/help/account/ | 월 1회 |
| Apple Bundle ID 정책 | https://developer.apple.com/help/app-store-connect/reference/app-information/ | 분기 1회 |
| Firebase iOS docs | https://firebase.google.com/docs/cloud-messaging/ios/client | 월 1회 |
| FlutterFire docs | https://github.com/firebase/flutterfire/tree/main/docs | 월 1회 |
| Google Cloud Console help | https://cloud.google.com/docs/ | 분기 1회 |
| AWS Console help | https://docs.aws.amazon.com/ | 분기 1회 |
| Stripe iOS / Web docs | https://stripe.com/docs/ | 월 1회 |
| Sentry SDK docs | https://docs.sentry.io/ | 분기 1회 |

## 2차 출처 (커뮤니티 / 변경 시그널)

| 출처 | 신호 |
|------|------|
| Firebase 공식 블로그 | https://firebase.blog/ — 메이저 SDK 릴리스, deprecated 공지 |
| Apple Developer Forums | https://developer.apple.com/forums/ — Push/FCM 트러블슈팅 트렌드 |
| FlutterFire GitHub Releases | https://github.com/firebase/flutterfire/releases — 호환 매트릭스 변경 |
| Reddit /r/iOSProgramming | 커뮤니티에서 자주 막히는 단계 |
| Stack Overflow firebase-cloud-messaging 태그 | 최근 질문 트렌드 |

## 사용자 피드백 메모리

`~/.claude/projects/-Users-jackson-Hub-10-Dev-claude-plugins/memory/feedback_setup_guide_*.md` 스캔 → SKILL.md Gotchas에 추가/수정할 패턴 도출.

## kaizen 트리거 체크

1. 1차 출처의 페이지 마지막 업데이트 날짜 비교 — 변경이 있으면 해당 섹션 재검토
2. 2차 출처에서 deprecated/breaking change 시그널 grep
3. 피드백 메모리 3개+ 누적 시 자동 트리거
```

저장: `.claude/skills/onboarding-kaizen/references/research-sources.md`

- [ ] **Step 3: 검증**

```bash
ls .claude/skills/onboarding-kaizen/ && cat .claude/skills/onboarding-kaizen/SKILL.md | head -5
```

Expected: SKILL.md + references/ 디렉토리. frontmatter 정상.

---

## Task 10: marketplace.json에 onboarding-kit 등록

**Files:**

- Modify: `.claude-plugin/marketplace.json`

- [ ] **Step 1: marketplace.json의 plugins 배열 끝에 onboarding-kit 추가**

현재 plugins 배열의 마지막 항목인 bambu-kit 뒤에 추가. 정확한 Edit:

```json
    {
      "name": "bambu-kit",
      "source": "./bambu-kit",
      "description": "[v0.2.0 · 2026-05-16] Bambu Lab H2S 자동 process+filament JSON 생성 — MakerWorld URL/모델 분석 → 소재 추천 → seam 전략 → Bambu Studio import 번들 (Codex 8회 리서치 + 실측 dogfood 2건 검증)"
    }
```

→

```json
    {
      "name": "bambu-kit",
      "source": "./bambu-kit",
      "description": "[v0.2.0 · 2026-05-16] Bambu Lab H2S 자동 process+filament JSON 생성 — MakerWorld URL/모델 분석 → 소재 추천 → seam 전략 → Bambu Studio import 번들 (Codex 8회 리서치 + 실측 dogfood 2건 검증)"
    },
    {
      "name": "onboarding-kit",
      "source": "./onboarding-kit",
      "description": "[v0.1.0 · 2026-05-18] 스택 무관 외부 서비스 셋업 가이드 자동 생성 — 그 시점 최신 정보(WebFetch → Context7 → Codex) 기준 step-by-step MD (Firebase/GCP/AWS/FCM/OAuth/Stripe, 11개 섹션 표준 포맷)"
    }
```

- [ ] **Step 2: JSON 형식 검증**

```bash
python3 -c "import json; d=json.load(open('.claude-plugin/marketplace.json')); print([p['name'] for p in d['plugins']])"
```

Expected: 마지막 원소가 `onboarding-kit`.

---

## Task 11: kaizen-orchestrator에 Phase 13 등록

**Files:**

- Modify: `.claude/skills/kaizen-orchestrator/SKILL.md`
- Modify: `.claude/skills/kaizen-orchestrator/references/phase-research-templates.md`
- Modify: `.claude/skills/kaizen-orchestrator/references/phase-dependencies.md`

- [ ] **Step 1: 기존 마지막 Phase(12 reflect-kit) 항목 찾기**

```bash
grep -n "Phase 12\|reflect-kit" .claude/skills/kaizen-orchestrator/SKILL.md | head -10
```

위치 확인 후 그 다음에 Phase 13 항목 삽입.

- [ ] **Step 2: SKILL.md에 Phase 13 entry 추가**

기존 Phase 12 항목 형식과 동일하게 Phase 13 entry 작성. 항목 내용 예시:

```markdown
### Step <N>: Phase 13 — onboarding-kit 카이젠

`/onboarding-kaizen` 호출. onboarding-kit `/setup-guide` SKILL.md, references/ 3종, evals/evals.json을 외부 docs/help 변경 + 사용자 피드백 메모리 + marketplace 트렌드 기반으로 개선.

의존성: Phase 1 (스킬 설계 가이드) 결과 반영 필수. 다른 Phase 결과에는 영향받지 않음.
```

정확한 step 번호와 형식은 기존 Phase 12 항목과 매칭해서 작성.

- [ ] **Step 3: phase-research-templates.md에 Phase 13 행 추가**

기존 Phase 12 행 형식을 따라 Phase 13 추가. 리서치 소스: `.claude/skills/onboarding-kaizen/references/research-sources.md`의 1차/2차 출처 reference.

- [ ] **Step 4: phase-dependencies.md에 Phase 13 의존성 추가**

```text
Phase 13 (onboarding-kit) depends on:
- Phase 1 (skill-design-guide) — 스킬 설계 원칙 반영
독립: 다른 Phase 결과에 영향받지 않음
```

- [ ] **Step 5: 검증**

```bash
grep -c "Phase 13" .claude/skills/kaizen-orchestrator/SKILL.md
grep -c "Phase 13" .claude/skills/kaizen-orchestrator/references/phase-research-templates.md
grep -c "Phase 13" .claude/skills/kaizen-orchestrator/references/phase-dependencies.md
```

Expected: 각각 1+ (최소 한 번씩 등장).

---

## Task 12: CLAUDE.md Repository Overview 업데이트

**Files:**

- Modify: `CLAUDE.md`

- [ ] **Step 1: Repository Overview 섹션의 플러그인 목록 끝에 onboarding-kit 한 줄 추가**

기존 마지막 항목(bambu-kit) 다음에 추가. Edit:

```markdown
- **bambu-kit** — Bambu Lab H2S용 자동 process+filament JSON 생성 — MakerWorld URL/모델 분석 → 소재 추천 → seam 전략 → Bambu Studio import 번들
```

→

```markdown
- **bambu-kit** — Bambu Lab H2S용 자동 process+filament JSON 생성 — MakerWorld URL/모델 분석 → 소재 추천 → seam 전략 → Bambu Studio import 번들
- **onboarding-kit** — 스택 무관 외부 서비스 셋업 가이드 자동 생성 — 그 시점 최신 정보(WebFetch → Context7 → Codex) 기준 step-by-step MD (Firebase, GCP, AWS, FCM, OAuth, Stripe 등)
```

- [ ] **Step 2: Skills Reference 섹션에 onboarding-kit 표 추가**

flutter-toolkit, design-kit, ... 표 다음에 onboarding-kit 표 한 줄짜리 추가:

```markdown
**onboarding-kit — 외부 서비스 셋업 가이드**

| 스킬 | 용도 |
|------|------|
| `/setup-guide` | 그 시점 최신 정보 기준 외부 서비스 셋업 가이드 step-by-step MD 자동 생성 |
```

- [ ] **Step 3: 이 레포 전용 스킬 표에 onboarding-kaizen 추가**

기존 kaizen 표에 한 줄 추가:

```markdown
| `/onboarding-kaizen` | onboarding-kit 스킬 개선 |
```

- [ ] **Step 4: 검증**

```bash
grep -c "onboarding-kit" CLAUDE.md
grep -c "onboarding-kaizen" CLAUDE.md
```

Expected: onboarding-kit 3+, onboarding-kaizen 1+.

---

## Task 13: sync-docs.py 실행

**Files:**

- Modify (auto): `onboarding-kit/README.md` (AUTO 마커 사이)

- [ ] **Step 1: 전체 sync 실행**

```bash
python scripts/sync-docs.py onboarding-kit
```

Expected: AUTO 마커 사이 갱신 또는 "no changes". 에러 없음.

- [ ] **Step 2: 결과 확인**

```bash
python scripts/sync-docs.py --check-only
```

Expected: "all synced" 또는 "no changes needed".

---

## Task 14: validate-plugin.py 실행

**Files:** (read-only validation)

- [ ] **Step 1: onboarding-kit 검증**

```bash
python3 scripts/validate-plugin.py onboarding-kit
```

Expected: PASS for all 7 categories (or specific FAIL 출력).

- [ ] **Step 2: FAIL 있으면 fix 후 재실행**

자동 수정 가능한 항목:

```bash
python3 scripts/validate-plugin.py --fix
```

수동 수정 필요한 항목은 출력의 안내에 따라 fix 후 다시 validate-plugin.py 실행.

- [ ] **Step 3: 전체 레포 검증 (회귀 방지)**

```bash
python3 scripts/validate-plugin.py
```

Expected: 모든 킷 PASS. onboarding-kit 추가로 기존 킷 검증이 깨지지 않았는지 확인.

---

## Task 15: 변경 사항 검토 + 커밋

**Files:** (git only)

- [ ] **Step 1: git status로 변경 파일 목록 확인**

```bash
git status
```

Expected 파일 목록:

- `onboarding-kit/` (전체 신규)
- `.claude/skills/onboarding-kaizen/` (전체 신규)
- `.claude-plugin/marketplace.json` (modified)
- `.claude/skills/kaizen-orchestrator/SKILL.md` (modified)
- `.claude/skills/kaizen-orchestrator/references/phase-research-templates.md` (modified)
- `.claude/skills/kaizen-orchestrator/references/phase-dependencies.md` (modified)
- `CLAUDE.md` (modified)
- `docs/onboarding-kit/design-2026-05-18.md` (이미 존재, 신규)
- `docs/onboarding-kit/plan-2026-05-18.md` (이미 존재, 신규)
- `fcm-ios-setup-guide.md` (dogfood 산출물)

확인: 의도하지 않은 파일 변경 없는지 검토.

- [ ] **Step 2: git diff로 핵심 변경 검토**

```bash
git diff CLAUDE.md
git diff .claude-plugin/marketplace.json
git diff .claude/skills/kaizen-orchestrator/SKILL.md
```

- [ ] **Step 3: 단일 커밋으로 onboarding-kit v0.1.0 출시**

```bash
git add onboarding-kit/ .claude/skills/onboarding-kaizen/ .claude-plugin/marketplace.json .claude/skills/kaizen-orchestrator/ CLAUDE.md docs/onboarding-kit/
git commit -m "$(cat <<'EOF'
feat(onboarding-kit): v0.1.0 — 스택 무관 외부 서비스 셋업 가이드 자동 생성 플러그인

핵심:
- 1스킬 킷 (bambu-kit 패턴) — /setup-guide 단일 진입점
- 그 시점 최신 정보: WebFetch → Context7 → Codex → WebSearch 우선순위
- 스택 무관 자동 탐지 (pubspec/Cargo/package.json/Podfile 등)
- 11개 섹션 표준 포맷 (헤더/사전요구/단계/코드/IAM/비용/환경/Rollback/보안/검증/트러블슈팅)
- Gotchas 6종 — 사이트 혼동, 글로벌 식별자 충돌, 등록≠활성화, 출시후 변경불가 등
- references/ 3종: project-detection / format-checklist / search-strategy
- evals/evals.json 4개 시나리오

부가:
- .claude/skills/onboarding-kaizen — 이 레포 개발용 스킬 자체 카이젠
- kaizen-orchestrator Phase 13으로 등록
- marketplace.json + CLAUDE.md 업데이트

기반: fit-pal/.claude/skills/setup-guide 격상 + 본 세션 사용자 피드백 3건 메모리화 (스택 확정 / 콘솔 UI fetch / 사이트 구분) + FCM iOS dogfood 가이드 검증.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: 커밋 확인**

```bash
git log -1 --stat
```

Expected: 커밋 성공, 변경 파일 목록 출력.

---

## Self-Review (작성자 본인 체크)

- [x] **Spec coverage**: design-2026-05-18.md의 모든 섹션이 Task로 매핑됨 (구조→Task 2, SKILL.md→Task 6, references→Task 3~5, evals→Task 7, onboarding-kaizen→Task 9, orchestrator→Task 11, CLAUDE.md→Task 12, marketplace→Task 10)
- [x] **Placeholder scan**: TBD/TODO/"appropriate handling" 같은 빈말 없음. 모든 step에 actual content
- [x] **Type consistency**: SKILL.md `name: setup-guide` ↔ marketplace `onboarding-kit` (스킬과 플러그인 이름 구분 일관). references 3종 파일명이 SKILL.md 본문 참조와 일치
- [x] **No 외부 라이브러리 API 호출 코드**: 플러그인 자체는 markdown + JSON만 — Context7/Codex 조회 불필요

---

## Execution Handoff

Plan complete and saved to `docs/onboarding-kit/plan-2026-05-18.md`. Two execution options:

1. **Subagent-Driven (recommended)** — 각 Task마다 fresh subagent 디스패치 + Task 사이 리뷰. fast iteration.
2. **Inline Execution** — 이 세션에서 batch + 체크포인트로 직접 실행.

Which approach?
