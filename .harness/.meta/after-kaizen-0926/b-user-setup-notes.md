# B 사용자 설정 P6~P10 — 구현 기록

계약: `.harness/sprint-contract-user-setup-p6-p10.md` (봉인 `af6ae6ab308af2bf`, 봉인 커밋 `0751d74`).
바뀐 파일은 전부 레포 밖이다. 이 레포에는 계약 · 결과 · 이 기록만 들어간다.

## 바꾼 것

| 키 | 파일 | 한 일 |
| --- | --- | --- |
| P6 | `~/.claude/skills/handoff/SKILL.md` | 틀에 `## 폐기·거절한 결정 (되살리기 전에 사용자에게 묻기)` 절, 5 단계를 `git commit -o` 로. 같은 파일에 원래 있던 마크다운 경고 7 건(목록 앞 빈 줄 · 틀 안 코드 블록이 바깥 블록을 닫던 것 · 같은 제목)도 고쳤다 — 틀 바깥 블록을 백틱 4 개로 |
| P6 | `~/.claude/hooks/next-session-handoff.sh` | 복붙 블록에 `폐기한 결정: <없으면 없음>` 한 줄 |
| P7 | `~/.claude/hooks/_lib-hook-payload.sh` | `strip_heredoc_bodies` 를 자동 수정 훅에서 옮겨 왔다(실패 모드 설명 주석 함께) |
| P7 | `~/.claude/hooks/block-dirwide-autofixer.sh` | 함수 정의를 지우고 공용 함수를 부른다 |
| P7 | `~/.claude/hooks/parallel-session-guard.sh` | 앞말 변수 대입 · `env` 를 벗겨 커밋 판별, heredoc 본문 제외, `GIT_INDEX_FILE` 이 보이면 그 파일 기준 목록(못 풀면 조용히), 커밋 뒤 알림에 「지운 파일 N 개」 |
| P8 | `~/.claude/rules/flutter-playwright-verification.md` | 56 행 끝에 반 문장 |
| P9 | `~/.claude/CLAUDE.md` | 22 행 끝 한 문장, 136 행 「작은 변경이니까」 문장을 예외 문장으로 |
| P10 | fit-pal 기억 둘 | 한 줄 추가, 목차 여섯 줄을 한 줄로 (파일 여섯은 그대로) |

## 잰 값

- `b-test.sh` 설치본 결과(`b-result-after.txt`)가 사본 구현 결과와 37 줄 같다. SC · ER · SK02 · AF 줄은 계약 기대값 그대로.
- 실제 세션: `df9837e0-3896-4de8-85da-6c5c678a1463`(개인 인덱스 커밋) · `342372e2-417a-4f52-bd90-35e69c336cf9`(세션 마감 문장) · 훅을 마지막으로 고친 뒤 다시 돈 `2c2ba3c3-33ee-415e-849c-c9e4e26d3055` — 안내에 `mine.txt` 1 · `shared.txt` 0, 커밋 파일 `mine.txt` 하나, 네 훅 오류 첨부 0.
- 마크다운 경고(편집기와 같은 설정, markdownlint-cli2 0.23.2 · MD013 끔): `CLAUDE.md` 11 → 11, 플러터 규칙 6 → 6. 두 파일은 「새 줄 없이」 가 조건이라 원래 있던 경고(빈 줄 부족)를 고칠 수 없다.
- 관찰(조건 아님) `OBS-dup kit=1 user_del51=1` — 51 개 삭제 커밋에서 킷 `commit-guard.sh post` 와 이 훅이 각각 알린다. 이 훅의 커밋 뒤 알림은 고치기 전부터 모든 커밋에 떴으니 새로 생긴 겹침이 아니다.

## 구현 중 고친 것

- 처음 사본은 병렬 세션 훅에 대체 함수를 같은 이름으로 다시 정의했다 → AR-01 · RE-02 「훅에 정의 0」 과 어긋나 `declare -F` 로 있으면 부르고 없으면 원문을 쓰게 바꿨다.
- 공용 도우미 첫 줄 주석을 바꿨다가 AR-01 「기존 줄 삭제 0」 과 어긋나 되돌리고 새 줄만 남겼다.
- 자동 수정 훅 주석 한 줄의 「~에 대해서」 를 「~만 본다」 로.

## 톤 대조 (tone-guide 5 단계)

대상: 위 셸 네 파일 · 문서 넷의 더한 줄. 어댑터 없음(셸) — 코어 규칙 · 한국어 규칙만.

| 규칙 | 건수 | 판정 |
| --- | --- | --- |
| K-02 번역투 여섯 | 0 | 통과 (자동 수정 훅 한 줄 고친 뒤) |
| C-04 · F 구분선 | 0 | 통과 |
| C-13 자찬 | 0 | 통과 |
| N-08 한 글자 이름 | 0 | 통과 (`index_file` · `index_word` · `deleted_line`) |
| N-07 fallback 접두사 | 0 | 통과 |
| H 보존 | — | 옮긴 함수의 실패 모드 두 줄(자기 차단 · 함수 없으면 검사 꺼짐)을 공용 도우미에 남겼다 |
| C-07 해설 3 줄 초과 | 1 | 공용 도우미 함수 위 8 줄 — 전부 H(실패 모드)라 보존. 옮기기 전 12 줄에서 줄였다(S-14) |
| S-04 · S-06 | 0 | 새 함수 없음. 대체 함수 정의를 없애 헬퍼 이중 정의도 없다 |

## 다음에 합칠 것

- `~/.claude/hooks/enforce-codex-stdin.sh` 에 같은 heredoc 제거 awk 가 인라인으로 남아 있다(범위 밖). 공용 함수로 바꿀 후보.
- `~/.claude/skills/handoff/SKILL.md` 5 단계 커밋 예시의 공동 작성자 줄이 옛 모델 이름이다(범위 밖).
- 한 명령에서 `GIT_INDEX_FILE=` 을 `git add` 에만 붙이고 `git commit` 은 공용 인덱스로 하는 드문 형태는 개인 인덱스 목록을 보여준다(마지막 값을 쓰는 규칙의 한계).
