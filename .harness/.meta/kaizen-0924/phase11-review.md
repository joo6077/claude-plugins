# 카이젠 2026-09-24 Phase 11 (planning-kit) — 계약 초안 독립 검토

- 대상: `.harness/sprint-contract-kaizen-0924-p11-planning-kit.md` (봉인 전, 작업 폴더에 추적 안 된 파일, 28 조건 · 기능 조건 18)
- 개정 파일 `.harness/sprint-amendments-kaizen-0924-p11-planning-kit.md` 은 아직 없다 — 봉인 전이라 맞다
- 검토자: REVIEW 에이전트 (사용자 승인 대신, 러닝북 「사용자 승인(5 단계) 대체」)
- 검토일: 2026-09-25

## 결론

계약은 거의 그대로 봉인해도 된다. 측정 정의를 내 사본에서 다시 돌려 초안에 적힌 값이 전부 그대로 나왔다.
다만 0 을 기대하는 칸 하나(AR-03 (c))가 제목이 바뀌거나 줄이 지워지면 조용히 0 을 내어 통과시킨다 — 내 대조에서 실제로 그랬다.
그리고 F20 가운데 이 Phase 가 다루지 않는 부분(한 나라 우선 판단)이 notes 의 미반영 절에 남는지를 재는 칸이 없다.
이 두 곳을 고치면 APPROVE 다.

## 다시 돌려 본 것

모두 스크래치 `p11r/` 아래에서만 돌렸다. 작업 폴더는 이 파일 하나만 썼다.

1. 계약에서 `common.sh` · `m.sh` · `new-warnings.sh` 를 DRAFT 의 `extract.py` 로 뽑아 새 폴더에 두고, DRAFT 의 `rehearse.sh` 로 시작 커밋 `d88c35a` 에서 새 예행 저장소를 만들었다(`mock.py` sha256 앞 16 자리 `27e81dbc370e001f` — 계약과 같다).
2. 조건 스물셋(SK-01 ~ SK-10 · ER-01 ~ ER-03 · AR-01 ~ AR-03 · RE-02 · AP-01 · AP-03 · AP-04 · DG-02 · DG-05 · DG-06)을 bash 5.3.9 로 전부 다시 쟀다.
   계약 `봉인 전 실측` 표의 예행 판 값과 한 글자도 다르지 않았다(예: SK-03 `prfaq=1` · `shapeup=1 old=0` · `linear=1 1` · `rows=1 1 heads=3` · `1 1 1`, AR-01 `0` · `0 10` · `0` · `SEAL_OK` · `scope_same=1` · `1`, DG-06 `scope-isolation: PASS` · `doc-contracts: PASS` · `doc_checked=2 doc_mine=0` · `violators=0 mine=0`).
3. 시작 커밋 판(`base` 변형)으로 SK-01 · SK-02 · SK-03 · SK-07 · SK-09 · SK-10 · AR-02 · RE-02 를 다시 쟀다 — 계약에 적힌 「시작 커밋 판」 값과 같다. 새 문장이 편집 전에는 0 이라는 확인이 맞다.
4. 내 대조 일곱 개(끝 판 사본 한 군데를 바꾸고 되돌림):

| 바꾼 것 | 조건 | 결과 | 판단 |
| --- | --- | --- | --- |
| plan-data-model Step 0 로드 줄 끝에 「겹치는 필드는 넣지 않는다」 | SK-05 | 첫 줄 `1 1 1` | 떨어진다 — 규칙이 Step 0 에 들어가면 잡는다 |
| reviewer 새 절을 Step 5 뒤로 옮김 | SK-08 | 셋째 줄 `0` | 떨어진다 |
| Gotcha 14 를 13 줄 끝에 붙여 번호 줄을 없앰 | SK-02 | `0` 열둘 · `1 … 13` | 떨어진다 |
| plan-audit 카테고리 3 행에서 「planning-reviewer Step 3 확인」 구절만 지움 | SK-07 | `1 1 0 1 1` | 떨어진다 |
| plan-audit 에 「흔적은 치울 목록이다」 한 줄 | RE-02 | 첫 칸에 plan-audit 가 더해짐 | 떨어진다 |
| **plan-prd `## Step 0: 자동 로드 (독립 단계)` 제목을 `## Step 0: 로드` 로 바꿈** | AR-03 | 셋째 줄 `0 0 0 1 1` 그대로 | **통과해 버린다** |
| **plan-audit Step 0 의 3 번 줄을 지움** | AR-03 | 셋째 줄 `0 0 0 1 1` 그대로 | **통과해 버린다** |

마지막 두 줄이 아래 필수 1 의 근거다. `sect` 가 제목을 못 찾으면 끝 판 쪽이 빈 글이 되고, `diff` 는 편집 전 줄을 `<` 로만 내는데 측정이 `^> .` 만 세서 0 이 된다.

## 조건마다 FAIL 을 한 문장으로 쓸 수 있는가

전부 쓸 수 있다. 몇 개만 적는다.

- SK-01 ~ SK-10: 정해진 절 · 줄에서 토큰 하나라도 0 이거나, 순서 비교 값이 0 이거나, 머리 설정 값이 다르면 FAIL. 문장 삭제 대조 109 가운데 103 이 떨어졌고, 안 떨어진 넷은 del.sh 가 같은 문자열의 다른 자리(Gotcha 10 · 카테고리 2 행 등)를 지운 경우라 측정 쪽 구멍이 아니다 — 측정은 그 줄 · 그 절로 좁혀져 있다(내가 SK-02 · SK-07 에서 따로 확인)
- ER-01: 새 URL 가운데 근거 파일에 없는 것이 1 개 이상이거나 notes 가 끝 판에 없으면 FAIL
- ER-03: notes 가 `$END` 에 없거나, 세어야 할 문자열 하나라도 그 절에서 0 이거나, 공유 · 다른 Phase 경로를 건드린 서명 없는 커밋이 1 개 이상이면 FAIL
- AR-01: 여섯 값 가운데 하나라도 기대값과 다르면 FAIL (`SEAL_ABSENT` 도 FAIL 로 적혀 있다)
- DG-06: 다른 Phase 몫으로 가르는 규칙(위반 커밋 목록을 1 개 이상 읽었고 그 가운데 이 Phase 서명이 0)이 적혀 있어 PASS · FAIL 이 기계로 갈린다. 러닝북 「FAIL 이면 안 된다」 보다 느슨하지만, 여러 Phase 가 한 가지에 쌓이는 구조 때문에 필요한 가르기이고 판정 규칙이 명시돼 있어 받아들인다
- N/A 다섯(SC-00 · DG-01 · DG-03 · DG-04 · RE-01): 사유마다 재는 명령과 양성 대조가 붙어 있다

## 처리 배정표 Phase 11 행

- 배정 칸이 Phase 11 인 행은 `F20` · `backend-family:P1` 둘뿐이다(처리 배정표 77 · 123 행, 적용 힌트 167 행). 둘 다 `배경` 표에서 다뤘다
- `F20` 의 「한 나라 우선 판단」 은 `user-setup:P10`(킷 밖), 「시간대를 설정값으로」 는 `backend-family:P2`(Phase 7)로 간다는 계약 설명이 처리 배정표와 맞다. 다만 이 사실이 notes 에 남는지를 재는 칸이 없다 — 필수 2
- 앞 Phase 넘김(Phase 4 · 6 · 7)은 모두 `배경` 표에 있다. Phase 8 notes 의 「일곱 킷 reviewer 미검증 사본은 다음 사이클 Phase 3 뒤」 도 GAP 표 · ER-03 (b) 에 반영돼 있다. Phase 1 notes 가 짚은 `plan-audit/SKILL.md:25` 는 「맞게 남았다」 는 기록이라 할 일이 없다
- 러닝북 `Phase 별 추가 과제` 에 Phase 11 줄은 없다 — 확인했다
- 근거 파일 §3 셋째 행은 `plan-data-model/SKILL.md:24`(Gotcha 8, Mermaid 버전 적지 않음)와 `docs/planning/data-modeling.md` 를 함께 짚었다. 계약은 문서 쪽만 고치고 스킬 쪽을 미반영이라고 적지 않았다 — 권고 5

## 범위 · 공유 파일

- 고치는 열 파일이 러닝북 표 Phase 11 칸(`planning-kit/` · `docs/planning/`) 안이다. `.harness/` 는 계약 · 개정 · notes · 이 검토 파일 · QA 피드백만 쓴다
- 공유 파일(`marketplace.json` · plugin.json 버전 · 루트 README · 루트 CLAUDE.md · `docs/` HTML · 처리 배정표 · 감사 기록 · 실패 횟수 · CI · stale-values)과 `design-kit/` · `harness/` · `scripts/` · `.claude/skills/` 를 고치지 않는다고 적었고, ER-03 (d) 가 경로로 직접 센다. 경로 지정 `docs/planning` 이 `docs/planning-kit` 에 걸리지 않는 것도 확인했다(`git ls-files -- docs/planning` 에 planning-kit 0 건)
- 범위 선언 블록(`# sprint-scope`)이 `harness/README.md` 가 정한 자리 · 꼴과 맞다
- frontmatter 를 건드리지 않아 README AUTO 구간이 바뀔 일이 없다(AP-04 · DG-05 (b))

## 조건끼리 부딪히는가

부딪히는 곳은 없다. 맞물림을 확인한 곳:

- SK-05 · SK-06 이 요구하는 두 문장(로드 문장 · 「이 단계에서 … 그리지 않는다.」)과 AR-03 (c) 의 「Step 0 에 1 줄」 은 두 문장이 한 줄에 있어야 함께 선다. `mock.py` 가 한 줄로 넣으니 지금은 맞다 — 권고 3
- RE-02 (c) 는 plan-audit 을 빼고 잰다. plan-audit 카테고리 3 행은 판정 기준이라 칸 이름을 적어야 하고 SK-07 이 그 문장을 요구한다 — 둘은 부딪히지 않는다. 다만 RE-02 머리 문장 「규칙 본문은 킷 안에서 plan-prd 한 곳」 이 이 예외를 말하지 않는다 — 권고 2
- SK-03 `heads=3` 과 RE-02 (b) 「표 머리는 plan-prd 에만」 이 맞물린다

## 고칠 것 (필수)

### 필수 1 — AR-03 (c): 지운 줄과 사라진 제목도 센다

`m.sh` `AR-03)` 갈래의 끝 두 줄에서 `grep -c '^> .'` 를 `grep -c '^[<>] .'` 로 바꾼다(두 곳).

```bash
    for f in "$PRD" "$ST" "$AU"; do printf '%s ' "$(diff <(sect "$T/B/$f" '## Step 0: 자동 로드 (독립 단계)') <(sect "$E/$f" '## Step 0: 자동 로드 (독립 단계)') | grep -c '^[<>] .')"; done
    for f in "$MO" "$FW"; do printf '%s ' "$(diff <(sect "$T/B/$f" '## Step 0: 리서치 문서 로드') <(sect "$E/$f" '## Step 0: 리서치 문서 로드') | grep -c '^[<>] .')"; done; echo ;;
```

내 사본에서 이 판으로 잰 값: 예행 판 `0 0 0 1 1`(기대값 그대로) · 시작 커밋 판 `0 0 0 0 0` · plan-prd Step 0 제목을 바꾼 사본 `6 0 0 1 1` ·
plan-audit Step 0 의 3 번 줄을 지운 사본 `0 0 1 1 1` · DRAFT 의 `step0-rule` 대조(3 번 항목 자리에 한 줄을 끼워 번호가 밀림) `3 0 0 1 1`.

AR-03 조건 줄 (c) 와 측정 괄호를 이렇게 고친다:

- (c) 앞부분: 「(c) Step 0 절에서 바뀐(더하거나 지운) 비지 않은 줄이 plan-prd · plan-stories · plan-audit 는 0, plan-data-model · plan-flow 는 1 (로드 문장 하나 — SK-05 · SK-06 이 그 문장을 잰다). 제목이 바뀌어 절을 못 찾으면 편집 전 줄이 전부 지운 줄로 세어져 0 이 되지 않는다」
- 양성 대조 괄호: 「… plan-prd Step 0 의 3 번 항목 자리에 한 줄을 끼운 사본에서 셋째 줄 `3 0 0 1 1` · plan-prd Step 0 제목을 `## Step 0: 로드` 로 바꾼 사본에서 `6 0 0 1 1` · plan-audit Step 0 의 3 번 줄을 지운 사본에서 `0 0 1 1 1` · plan-audit 표에 행을 하나 더한 사본에서 둘째 줄 `13 12`」
- `회귀 게이트` 절 표의 AR-03 행 「대조」 칸도 같은 값으로 바꾼다

### 필수 2 — ER-03 (c): F20 에서 이 Phase 가 다루지 않는 부분을 notes 미반영 절에 남긴다

`F20` 은 notes 의 「반영한 처리 배정표 키」 에 들어가는데, 그 행의 「한 나라 우선 판단」 은 처리 배정표가 `user-setup:P10`(킷 밖)으로 보냈다.
notes 만 읽는 Final 이 F20 을 다 끝난 것으로 읽지 않게 미반영 절에 그 줄이 있는지 잰다.

- `m.sh` `ER-03)` 갈래: `toks "$(sect "$E/$NOTES" '## 미반영 키와 사유' 2>/dev/null)" '2026-03-10' 'Design Sprint'` 끝에 `'user-setup:P10'` 을 더한다
- ER-03 조건 줄 (c): 「(c) `## 미반영 키와 사유` 절 안에 `2026-03-10` (GitHub 문서 버전 날짜) · `Design Sprint` (틀 없음) · `user-setup:P10` (F20 의 한 나라 우선 판단 — 처리 배정표가 킷 밖으로 보냈다) 이 각각 1 줄 이상이고」
- ER-03 Then: 「넷째 줄 세 값 모두 `1` 이상」
- 봉인 전 실측(내 사본): 지금 notes 모의본 그대로 넷째 줄 `1 1 0`, 미반영 절에 「`F20` 의 한 나라 우선 판단 — 처리 배정표가 `user-setup:P10`(킷 밖)으로 보냈다」 한 줄을 더한 사본 `1 1 1`, 같은 낱말을 `## 다음 사이클 메모` 절에만 둔 사본 `1 1 0`. `notes-mock.md` 에 그 줄을 더하고 예행 판 값을 다시 적는다
- `배경` 표 `F20` 행 「이번 처리」 칸 끝에 「notes 미반영 절에 적는다(ER-03)」 를 더한다

두 곳을 고친 뒤 Step 6.2 두 명령(28 · 18 — 조건 수는 안 바뀐다)과 Step 6.5 를 다시 돌리고, AR-03 · ER-03 두 조건의 예행 판 · 대조 값을 표에 다시 적는다.

## 권고 (선택 — 판정에 넣지 않는다)

1. `배경` 의 「Gotcha 4 의 1-2 개씩에 맞춰 관심사를 둘로 묶는다」 는 글자 그대로 읽으면 맞지 않는다 — 고치는 킷 파일이 여섯이다. 「파일 수는 여섯으로 1-2 개를 넘는다. 뒤 단계 셋과 감사 둘은 규칙 본문 없이 한두 줄만 더하고(RE-02), 근거 파일 §2 가 감사 두 파일을 함께 바꾸라고 해서 나누지 않았다」 처럼 넘는다는 사실과 이유를 적는 편이 정직하다
2. RE-02 머리 문장에 「plan-audit 카테고리 3 행은 판정 기준이라 칸 이름을 적는다(SK-07) — (c) 에서 뺀다」 를 더한다
3. SK-05 · SK-06 에 「두 문장은 한 줄이다(AR-03 (c) 가 1 줄을 센다)」 를 더한다 — BUILD 가 두 줄로 나누면 SK 는 통과하고 AR-03 만 떨어져 원인을 찾기 어렵다
4. `회귀 게이트` 절에 「`m` 의 종료 코드는 판정하지 않는다 — 마지막 `grep -c` 가 0 을 세면 1 이다. 도우미가 없을 때만 2 다」 한 줄. 내 사본에서 ER-01 · ER-03 · AP-03 이 PASS 값인데 종료 코드 1 을 냈다
5. `배경` 표 「근거 파일 §3 현행화」 행에 「`plan-data-model/SKILL.md:24`(Gotcha 8) 는 고치지 않는다 — Step 0 이 `data-modeling.md` 를 읽으니 버전 사실은 문서 한 곳에 둔다」 를 더한다
6. `GAP 분석` 「감사 쪽」 에 한 줄: reviewer Gotcha 8(카테고리 독립 판정)과의 관계 — 새 확인은 다른 카테고리의 판정이 아니라 뒤 단계 파일을 읽을 뿐이라 독립 판정을 어기지 않는다
7. `docs/planning/flows.md` 새 문장 「Mermaid 최신 안정판은 12.0.0」 은 시간이 지나면 틀린 말이 된다. 「2026-09-25 확인 기준 최신 안정판은」 처럼 확인 날짜를 붙이면 오래 간다(바꾸면 SK-09 토큰과 `mock.py` 를 같이 고친다)

## 기록

- 내 예행 저장소 · 도우미: `/private/tmp/claude-501/-Users-jackson-Hub-10-Dev-claude-plugins/de8c7935-a5b6-4df5-9106-fafa73c288a0/scratchpad/p11r/` (`rh` · `rh-base` · `k` · `k5`(필수 1 판) · `k6`(필수 1 · 2 판) · `myctl.sh` · `ar03.sh` · `er03.sh`)
- 작업 폴더에서 쓴 파일: 이 파일 하나

VERDICT: CHANGES

## 2 회차

- 대상: 같은 계약 초안(봉인 전, 추적 안 된 파일, 28 조건 · 기능 조건 18). 개정 파일은 아직 없다 — 봉인 전이라 맞다
- 검토자: REVIEW 에이전트(2 회차, 1 회차와 따로 띄움). 검토일 2026-09-25
- 비교 기준: 1 회차가 본 판은 스크래치 `p11d2/contract.v1.md` 다 — 1 회차가 뽑은 `p11r/k/m.sh` 와 그 판의 `m.sh` 가 한 글자도 다르지 않다

### 결론

필수 둘과 권고 일곱이 전부 들어갔다. 바뀐 곳은 그 아홉 자리와 예행 기록 줄뿐이고(옛 판과 줄 단위 비교 60 줄), 다른 조건 문구는 그대로다.
측정 정의를 내가 따로 뽑아 새 예행 저장소 일곱에서 다시 쟀고, 계약 표의 값이 전부 그대로 나왔다. 새 결함은 찾지 못했다. 봉인해도 된다.

### 1 회차 CHANGES 반영 확인

| 항목 | 계약 자리 | 확인 |
| --- | --- | --- |
| 필수 1 — AR-03 (c) 지운 줄 · 사라진 제목도 센다 | `m.sh` `AR-03)` 끝 두 줄 `grep -c '^[<>] .'` · 조건 줄 (c) · 측정 괄호 · 봉인 전 실측 표 AR-03 행 | 들어갔다. 대조값 `3 0 0 1 1` · `6 0 0 1 1` · `0 0 1 1 1` · `13 12` 가 세 자리에 같게 적혔다 |
| 필수 2 — ER-03 (c) `user-setup:P10` | `m.sh` `ER-03)` 넷째 `toks` 끝 · 조건 줄 (c) · Then 「세 값」 · 봉인 전 실측 괄호와 표 · `배경` 표 `F20` 행 끝 · notes 모의본 미반영 절 한 줄 | 들어갔다 |
| 권고 1 — 킷 파일 여섯이 1-2 개를 넘는다는 사실과 이유 | `배경` 32 ~ 33 줄 | 들어갔다 |
| 권고 2 — RE-02 에서 plan-audit 카테고리 3 행을 빼는 까닭 | RE-02 머리 문장 | 들어갔다 |
| 권고 3 — 로드 문장과 「그리지 않는다」 는 한 줄 | SK-05 · SK-06 조건 줄 | 들어갔다 |
| 권고 4 — `m` 종료 코드는 판정하지 않는다 | 측정 공통 정의 절 머리 240 ~ 241 줄 | 들어갔다. 내 예행에서도 ER-01 · ER-03 · AP-03 만 PASS 값에서 종료 코드 1 이었다 |
| 권고 5 — `plan-data-model/SKILL.md:24` 는 고치지 않는다 | `배경` 표 「근거 파일 §3 현행화」 행 | 들어갔다. 근거 파일 146 행이 그 줄과 `data-modeling.md` 를 함께 짚은 것과 맞다 |
| 권고 6 — reviewer Gotcha 8 과 부딪히지 않는 까닭 | `GAP 분석` 「감사 쪽」 | 들어갔다 |
| 권고 7 — 「최신 안정판」 에 확인 날짜 | SK-09 토큰 · `mock.py` 119 행 · 개선안 초안 8 번 | 들어갔다. 날짜를 내가 적은 2026-09-25 대신 근거 파일 `collected: 2026-09-24`(4 행)로 쓴 것은 조회한 날이라 더 맞다 |

`mock.py` 는 옛 판(`mock.v1.py`, 앞 16 자리 `27e81dbc370e001f`)과 비교해 119 행 한 곳(날짜)만 바뀌었고, 새 판 앞 16 자리가 계약에 적힌 `593fb476788192fb` 와 같다.
notes 모의본도 미반영 절 한 줄만 더해졌다.

### 다시 돌려 본 것

모두 스크래치 `p11r2/` 에서만 돌렸다. 작업 폴더에서는 이 파일 끝에 이 절을 덧붙인 것 말고 아무것도 쓰지 않았다.

1. 계약의 bash 세 블록을 DRAFT 의 `extract.py` 가 아니라 내 awk 로 뽑았다. `common.sh` · `m.sh` · `new-warnings.sh` 가 DRAFT 의 `p11d/k5` 와 같다
2. DRAFT 의 `rehearse.sh` 로 지금 계약 사본에서 예행 저장소 여섯(`none` · `base` · 변형 넷)을 새로 만들었다
3. 조건 스물셋을 bash 5.3.9 로 쟀다 — 끝 판 · 시작 커밋 판 모두 계약 표와 한 글자도 다르지 않다. `/bin/bash` 3.2.57 끝 판 출력은 bash 5 출력과 바이트 단위로 같다
4. 변형 넷 — AR-01 ① `1`(unsigned-mine) · ② `1 10`(signed-outside) · ② `2 10`(cross-phase), ER-03 다섯째 줄 넷 모두 `1`, DG-06 cross-phase `scope-isolation: FAIL` · `violators=1 mine=1`, N/A 줄 signed-outside `SC-00=1` — 표와 같다
5. Step 6.2 두 명령 `28` · `18`, Step 6.5 (1) 헤더 열둘이 허용 목록 안 · (2) 조건 줄이 조건 절에만 · (3) `OK conditions=28` · (5) `OK 미실측 0 건`. (4) 커버리지 검출기가 낸 `UNCOVERED` 여덟(SK-01 · SK-02 · SK-05 · SK-09 · ER-01 · ER-03 · AR-01 · AR-02)은 모두 `범위 경계` 절 「커버리지 해소」 줄이 다룬다
6. 지금 가지 끝(`82b2493`)을 복제하고 `end_sha` 를 거기 둔 사본에서 범위 조건을 쟀다 — 시작 커밋 뒤 커밋 여덟이 전부 Phase 9 · 10 서명이라 AR-01 ① `0` · ER-03 (d) `0` · DG-06 `PASS` 넷 · DG-05 `10 0 rc=0` 이 나온다. 이미 올라간 남의 커밋이 이 계약을 떨어뜨리지 않는다
7. 열 파일은 시작 커밋과 지금 가지 끝 사이에 바뀐 것이 없고 작업 폴더에도 미커밋 변경이 없다. 지금 가지 끝 판에 `mock.py` 를 돌리면 `mock applied 27` 이 나온다

### 내 대조 (끝 판 사본 한 군데를 바꾸고 되돌림)

| 바꾼 것 | 조건 · 줄 | 결과 | 판단 |
| --- | --- | --- | --- |
| 그대로 | AR-03 셋째 줄 | `0 0 0 1 1` | 기대값 |
| plan-prd Step 0 제목을 `## Step 0: 로드` 로 | AR-03 셋째 줄 | `6 0 0 1 1` | 떨어진다 — 1 회차에 통과해 버리던 경우 |
| plan-audit Step 0 의 3 번 줄 지움 | AR-03 셋째 줄 | `0 0 1 1 1` | 떨어진다 — 1 회차에 통과해 버리던 경우 |
| plan-prd Step 0 3 번 자리에 한 줄 끼움 | AR-03 셋째 줄 | `3 0 0 1 1` | 떨어진다 |
| plan-stories Step 0 PRD 로드 줄 끝에 「겹치면 묻는다.」 | AR-03 셋째 줄 | `0 2 0 1 1` | 떨어진다 (새로 해 본 것) |
| plan-data-model Step 0 제목을 바꿈 | AR-03 셋째 줄 | `0 0 0 2 1` | 떨어진다 (새로 해 본 것 — 기대값 1 쪽도 제목이 사라지면 1 이 아니다) |
| plan-flow 로드 문장과 「그리지 않는다」 를 두 줄로 나눔 | AR-03 셋째 줄 · SK-06 첫 줄 | `0 0 0 1 2` · `1 1 0` | AR-03 만 떨어진다 — 권고 3 이 조건 줄에 적어 둔 그대로다 |
| plan-audit 표에 행 하나 | AR-03 둘째 줄 | `13 12` | 떨어진다 |
| notes 미반영 절의 `user-setup:P10` 줄 지움 | ER-03 넷째 줄 | `1 1 0` | 떨어진다 |
| 그 줄을 notes 맨 끝(`## 다음 사이클 메모`)으로 옮김 | ER-03 넷째 줄 | `1 1 0` | 떨어진다 |
| 그 줄을 `## 넘기는 것` 표로 옮김 | ER-03 넷째 줄 | `1 1 0` | 떨어진다 (새로 해 본 것) |
| flows 버전 문장에서 「2026-09-24 에 확인한 」 만 지움 | SK-09 둘째 줄 | `1 0 1` | 떨어진다 |
| 확인 날짜를 2026-09-25 로 바꿈 | SK-09 둘째 줄 | `1 0 1` | 떨어진다 (새로 해 본 것) |

### 새로 본 것 — 판정에 넣지 않는다

1. `범위 경계` 절의 검토 결과 줄이 1 회차 `VERDICT: CHANGES` 만 적고 있다. 봉인할 때 「2 회차 `VERDICT: APPROVE`」 를 그 줄에 더하면 이 계약만 읽는 QA 가 승인 대체 근거를 바로 찾는다. 서술 줄이라 조건 줄 요약값(`conditions_digest`)에는 영향이 없다
2. 작업 폴더에 다른 Phase 계약 열 개(p01 ~ p10)의 미커밋 변경이 있다. 이 Phase 몫이 아니다. BUILD 는 러닝북대로 `git commit -o <내 경로>` 로만 커밋하면 섞이지 않는다
3. ER-03 (c) 는 `user-setup:P10` 이 미반영 절에 있는지만 보고 그 줄이 `F20` 과 같은 줄인지는 보지 않는다. 조건 문구가 괄호로 뜻을 적어 두었고 QA 가 그 줄을 읽으면 되므로 고칠 것까지는 아니다

### 기록

- 내 예행 저장소 · 도우미: `/private/tmp/claude-501/-Users-jackson-Hub-10-Dev-claude-plugins/de8c7935-a5b6-4df5-9106-fafa73c288a0/scratchpad/p11r2/` (`k` · `rh-none` · `rh-base` · `rh-unsigned-mine` · `rh-unsigned-shared` · `rh-signed-outside` · `rh-cross-phase` · `rh-real` · `runm.sh` · `runall.sh` · `ctl.sh` · `real.sh` · `cov.sh` · `run-e.txt` · `run-b.txt` · `run-e32.txt`)
- 작업 폴더에서 쓴 파일: 이 파일 하나(끝에 이 절을 덧붙임. 앞 119 줄은 그대로)

VERDICT: APPROVE
