# 카이젠 2026-09-24 Phase 15 (tone-kit) — 계약 초안 독립 검토

- 대상: `.harness/sprint-contract-kaizen-0924-p15-tone-kit.md` (봉인 전, 추적 안 된 파일, 23 조건 · 기능 조건 15)
- 개정 파일 `.harness/sprint-amendments-kaizen-0924-p15-tone-kit.md` 은 아직 없다 — 봉인 전이라 맞다
- 검토자: REVIEW 에이전트 (사용자 승인 대신, 러닝북 「사용자 승인(5 단계) 대체」)
- 검토일: 2026-09-25

## 결론

두 군데를 고치면 봉인할 수 있다. 측정 정의는 계약에서 새로 뽑아 DRAFT(초안 담당 에이전트)의 도우미 폴더와 글자 그대로 같았고,
예행 저장소에서 내가 다시 잰 값도 계약 표와 같았다. 처리 배정표 · 범위 · 공유 파일 · 조건끼리의 충돌에서는 막을 문제를 못 찾았다.

1. **ER-01 은 이 Phase 가 고칠 수 있는 파일을 기준으로 잰다.** 근거 파일 `.harness/.meta/evidence/phase15.md` 를 끝 판(`$END`)에서 읽는데,
   이 파일은 `.harness/` 안이라 AR-01 이 이 Phase 의 편집을 허용한다. 지어낸 URL 을 research-log 에 넣고 같은 URL 을 끝 판 근거 파일에도 한 줄 덧붙인
   사본에서 `m ER-01` 첫 값이 `1` 에서 `0` 으로 돌아갔다. 러닝북이 「이 Phase 의 유일한 외부 근거」 라 한 파일을 스스로 늘려 조건을 통과할 수 있다.
   근거 파일은 시작 커밋보다 앞선 `f000dab` 에 들어왔고 `499cc12..HEAD` 사이에 이 파일을 건드린 커밋은 0 이다 — 시작 커밋 판을 읽으면 된다.
2. **모의 편집이 이 Phase 가 새로 넣는 K-11 을 스스로 어긴다.** `core-naming.md` §8 의 새 안내 문장과 research-log 의 검증 문단이 정규식의 `|`
   (둘 중 하나에 맞추는 기호)를 「대안 기호」 라고 부른다. 시작 커밋 판 `tone-kit` · `docs` · `harness` 어디에도 없는 말이고(`git grep` 0 줄),
   K-11 이 막으려는 「글쓴이가 새로 붙인 이름」 그대로다. 같은 `core-naming.md` 문장은 「매치가 조용히 0 건이 된다」 로 끝나는데, 계약 자신의
   봉인 전 실측대로 G-6 은 0 건이 아니라 fixture(시험 입력 폴더)의 모든 줄을 낸다. research-log 문단은 둘을 맞게 가른다 — 킷 파일 쪽 문장만 틀렸다.

둘 다 고칠 문구를 아래에 적었고, 고친 모의 편집과 고친 측정으로 예행 저장소를 새로 만들어 조건 스물셋을 다 쟀다. 값은 ER-01 에 셋째 줄
`evid_same=1` 이 붙은 것 말고 DRAFT 의 `out-final.txt` 와 같다. 고치면 `APPROVE` 다.

## 다시 돌려 본 것

모두 스크래치 `p15rv/` 아래에서만 돌렸다. 작업 폴더에는 이 파일 하나만 썼다.

1. 계약 `회귀 게이트` 절의 bash 블록 셋을 새로 뽑아 DRAFT 의 `p15d/k/` 와 비교했다 — `common.sh` · `m.sh` · `lintcmp.sh` 셋 다 `diff` 0 줄.
   `mock.py` 지문(sha256 앞 16 자리)은 계약 155 줄 값 `8b061cc707645d0c` 와 같다. `contract.draft.md` 는 지금 계약 파일과 바이트 단위로 같다.
2. DRAFT 의 예행 저장소 `p15d/rh-none` 에 내 도우미 폴더로 SK-01 · SC-01 · SC-02 · ER-04 · AR-01 · AR-02 · RE-02 와 `V=B` SC-01 · SC-02 를 쟀다 — 계약 536 ~ 581 줄 표와 같다
   (예: SC-01 `bash=1 zsh=1 lines=8`, 시작 커밋 판 `bash=0 zsh=0 lines=3`, SC-02 시작 커밋 판 `comment_bash=8/8 comment_zsh=0/8`).
3. `NAMING_ANSWER` 여덟 줄을 fixture 열한 줄에 손으로 대 봤다. G-3 의 `AppBarX` · G-4 의 `MessageBoxY` · G-6 의 `for` 줄은 걸러져야 하는 줄이고 답에 없다 —
   기대값이 구현 출력을 옮겨 적은 것이 아니라 바깥에서 센 값이다.
4. 계약이 적지 않은 양성 대조를 끝 판 사본에서 더 돌렸다(`p15rv/mut.sh` · `mut2.sh`). 전부 값이 요구값에서 벗어났다:
   - AP-03 research-log 에 언어 힌트 없는 펜스 → `0 0 0 0 0 1 0`
   - AR-02 K-11 목록 한 줄에 `(etc_seq=663)` → `cite663=1`
   - SC-01 G-6 에서 `| grep -vE 'for \('` 를 뺌 → `bash=0 zsh=0 lines=9`
   - SC-02 core-comment 를 배열로 두고 `"$X"` 로 펼침 → `bare=0 8 0 arr=7 0 3` · `comment_bash=0/8 comment_zsh=0/8`
   - SK-01 K-11 행 강도를 `SHOULD` 로 → `row=0` · SK-04 강도 줄을 `MUST` 로 → 열째 값 `0`
   - DG-05 core-naming 끝에 언어 힌트 없는 펜스 → `10 1 rc=2` (V6 줄 `1 bare`) · 사례 4 의 assertion 을 비움 → `run_evals_rc=1 0`, 같은 사본 SK-03 `case4=0`
5. `sync-docs.py tone-kit --check-only` 가 이 README 를 실제로 읽는지 봤다 — AUTO 표의 `tone-guide` 를 `tone-BROKEN` 으로 바꾼 사본에서 `변경 필요` · 종료 코드 1.
   Phase 14 에서 본 표지 문제(`:start` · `:end`)는 이 킷에 없다.
6. 나머지 검사 명령 25 종이 살아 있다는 주장(연구 기록에 들어가는 문장)을 확인했다. `p15/gs/run.sh` 를 bash 5 · zsh 로 다시 돌려 계약 596 줄 값과 같았고,
   그 파일의 명령 25 개를 시작 커밋 판 문서의 명령 줄과 글자로 대 봤다 — 25 개 모두 같다.
7. 세션 `8fa13c90` 기록을 열어 「표시훅」 과 사용자의 되물음을 확인했다. 새 말은 2026-09-19 04:54(세계 표준시, 이하 같음) 훅 시험 출력과 04:55 답변에 쓰였고,
   다음 사용자 메시지가 2026-09-22 01:40 의 「표시 훅은 뭐야?」 다. 「되물었다」 는 맞다(날짜는 아래 참고 3).
8. `harness/skills/sprint-contract/SKILL.md` Step 6.5 (1) ~ (3) · (5) — 제목 열둘이 허용 목록 안이고, 조건 줄은 전부 카테고리 절(Skill 5 · Script 2 · Error 4 · Architecture 2 ·
   Anti-patterns 2 · Reusability 2 · Diagnostics 6)에 있으며, `FM=23 N=23`, 기능 조건 계산 15, `[미실측]` 0.
9. 시작 커밋 판과 지금 `HEAD` 사이에 `tone-kit` · `docs/tone` 변경이 0 이고, `HEAD` 를 푼 새 사본에 `mock.py` 를 돌리면 `mock applied 8` · 종료 코드 0 이다.
   `499cc12..HEAD` 커밋 셋(전부 Phase 14)은 모두 `Kaizen-Phase: kaizen-0924-p14-onboarding-kit` 서명을 달았다.

## 고칠 것 (조건 ID 별)

### ER-01 — 근거 파일을 시작 커밋 판에서 읽는다

재현(`p15rv/mut4.sh`, 고친 측정): research-log 에 `<https://example.com/not-in-evidence>` → `1 0 evid_same=1`. 같은 URL 을 끝 판 근거 파일에도 덧붙임 →
`1 0 evid_same=0`(계약 판 측정은 이 경우 `0 0`). research-log 만 되돌림 → `0 0 evid_same=0`. 둘 다 되돌림 → `0 0 evid_same=1`.

(1) 조건 줄을 아래로 바꾼다. 경로 토큰을 새로 더하지 않아 `범위 경계` 의 ER-01 커버리지 해소 줄은 그대로 둬도 된다.

> - [ ] ER-01: 여덟 파일에 새로 생긴 URL 과 `.harness/.meta/kaizen-0924/phase15-notes.md` 의 URL 이 전부 시작 커밋 판의 외부 근거 파일 `.harness/.meta/evidence/phase15.md` 에 있고, 끝 판 근거 파일이 시작 커밋 판과 같다 — 근거 파일은 .harness 폴더 안이라 이 Phase 가 고칠 수 있으므로 끝 판에서 읽지 않는다. 여덟 파일은 파일마다 편집 전 판과 비교한다. 양성 대조: 근거 파일에 없는 URL 한 줄을 research-log 사본에 더하면 첫 값 1, 같은 URL 을 끝 판 근거 파일 사본에도 덧붙이면 첫 값은 그대로 1 이고 셋째 값이 `evid_same=0` (러닝북 — 근거 파일에 없는 URL 을 지어내지 마라 · notes 킷 로그의 출처 URL 은 근거 파일에서만) — `m ER-01` 세 줄이 `0` · `0` · `evid_same=1` [exact, enumerated]

(2) `m.sh` 의 `ER-01)` 갈래를 아래로 바꾼다.

```bash
  ER-01)  # 새로 생긴 URL 이 근거 파일에 있다 — 근거 파일은 시작 커밋 판에서 읽는다 (끝 판은 .harness/ 안이라 이 Phase 가 고칠 수 있다)
    for f in "${FILES[@]}"; do comm -13 <(url < "$T/B/$f") <(url < "$E/$f"); done | sort -u | comm -23 - <(url < "$T/B/$EVID") | grep -c .
    if [ -f "$E/$NOTES" ]; then url < "$E/$NOTES" | comm -23 - <(url < "$T/B/$EVID") | grep -c .; else echo NOTES_MISSING; fi
    cmp -s "$T/B/$EVID" "$E/$EVID" && echo evid_same=1 || echo evid_same=0 ;;
```

(3) `봉인 전 실측` 절의 값을 함께 고친다: 끝 판 표 `ER-01  0 | 0` → `ER-01  0 | 0 | evid_same=1`, 시작 커밋 판 표 `ER-01  0 | NOTES_MISSING` →
`ER-01  0 | NOTES_MISSING | evid_same=1`, 양성 대조 표에 `ER-01  같은 URL 을 끝 판 근거 파일에도 → 1 · evid_same=0` 한 줄.
BUILD(구현 담당 에이전트)는 봉인 전에 이 값을 다시 재서 적는다.

### SC-01 · SK-05 가 가리키는 모의 편집 — 「대안 기호」 를 풀어 쓰고 G-6 설명을 맞춘다

조건 줄은 안 바뀐다. 두 문장 모두 SC-01 · SK-05 가 요구하는 조각이 아니다(SK-05 조각 열은 같은 문단의 다른 문장이다). 바꾸는 것은 `p15d/mock.py` 의 글 두 곳과
계약 155 줄의 `mock.py` 지문이다.

- `tone-kit/references/core-naming.md` §8 안내 문장 (mock.py 안에서는 `\|` 가 `\\|` 로 적혀 있다)
  - 지금: 명령은 코드 블록에만 둔다. 표 칸에서는 `|` 를 `\|` 로 적어야 해서, 칸의 글을 그대로 붙여 넣으면 대안 기호가 글자 `|` 로 읽혀 매치가 조용히 0 건이 된다.
  - 고칠 글: 명령은 코드 블록에만 둔다. 표 칸에서는 `|` 를 `\|` 로 적어야 하는데, 칸의 글을 그대로 붙여 넣으면 `\|` 가 정규식의 '또는' 이 아니라 글자 `|` 로 읽힌다. 그러면 매치가 조용히 0 건이 되거나(G-1 · G-2 · G-5) 파이프 자리가 인자로 넘어가 파일의 모든 줄이 나온다(G-6).
- `docs/tone/research-log.md` 검증 문단
  - 지금: `\|` 가 대안 기호가 아니라 글자 `|` 로 읽힌다.
  - 고칠 글: `\|` 가 정규식의 '또는' 이 아니라 글자 `|` 로 읽힌다.

확인한 것: 위 두 곳을 바꾼 `p15rv/mock2.py`(따옴표만 「」 로 둔 판, 지문 `45961427e021e2dd`)로 DRAFT 의 `rehearse.sh` 를 돌려 예행 저장소 `p15rv/rh2` 를 만들고,
고친 ER-01 측정으로 `runall.sh` 스물한 갈래를 다 쟀다. DRAFT 의 `out-final.txt` 와 다른 줄은 ER-01 셋째 줄 `evid_same=1` 하나다 —
ER-02 `added=121 k02=0 names=0`, DG-02 `rules_up=0` 일곱, SC-01 `bash=1 zsh=1 lines=8`, SK-05 `1` 열 그대로다.
BUILD 는 따옴표를 정한 뒤 시작 커밋 판을 푼 새 사본에서 `mock applied 8` 을 다시 보고 새 지문을 155 줄에 적는다.
계약 34 줄 배경 문장의 「대안 기호」 도 같은 말로 바꾸면 계약과 킷 문장이 맞는다(서술 줄이라 봉인과 상관없다).

## 점검표

### 조건마다 불합격(FAIL) 상태를 한 문장으로

- SK-01: 규칙표 K-11 행이 정해진 글로 한 줄이 아니거나 ID 가 K-01 ~ K-11 로 안 이어지거나, §4 의 K-11 조각 여덟 가운데 하나라도 한 줄이 아니거나, §4 제목 · 목차 줄이 바뀌었거나, 「사전에 없는 한국어 합성어」 가 남았거나 §8 에 K-11 이 있으면 불합격
- SK-02: §10 출처 넷 · 로케일 표 여섯 조각 · `확인됨` 정의 행 가운데 하나라도 정해진 글로 한 줄이 아니면 불합격
- SK-03: `evals.json` 이 JSON 으로 안 읽히거나 사례가 넷이 아니거나, 사례 4 가 `want` 와 한 글자라도 다르거나, 사례 1 ~ 3 이 바뀌었으면 불합격
- SK-04: 원칙 9 제목이 하나가 아니거나 자리가 원칙 8 뒤 · `## 규칙 강도` 앞이 아니거나, 조각 열 · 출처 줄 · 잡는 것 줄 · 안티패턴 행 가운데 하나라도 요구 수가 아니거나, 원칙 번호가 1 ~ 9 로 안 이어지면 불합격
- SK-05: 새 절 제목이 하나가 아니거나 같은 제목이 겹치거나 옛 절이 바뀌었거나, 조각 열 가운데 하나라도 한 줄이 아니면 불합격
- SC-01: 표 행에 명령이나 `\|` 가 남았거나, §8 블록을 bash 나 zsh 로 돌린 출력이 손으로 센 여덟 줄과 한 글자라도 다르면 불합격
- SC-02: 세 블록 가운데 하나라도 따옴표 없는 `$INC` · `$X` 가 있거나 배열 펼침 수가 7 · 8 · 3 이 아니거나, 둘째 확장자를 더했을 때 bash · zsh 가운데 하나라도 `.ts` 파일을 못 찾으면 불합격
- ER-01 (고친 뒤): 새로 생긴 URL 이나 notes 의 URL 가운데 시작 커밋 판 근거 파일에 없는 것이 하나라도 있거나, 근거 파일이 바뀌었으면 불합격
- ER-02: 더한 줄에 번역투 6 종이나 앱 · 도구 서버 이름이 한 건이라도 있거나, 더한 줄이 0 이면 불합격
- ER-03: notes 가 끝 판에 커밋되지 않았거나, 키 둘 · 절 제목 일곱 · 넘김 다섯 · 다음 사이클 몫 여덟 가운데 하나라도 0 이거나, 금지 경로를 건드린 커밋 가운데 다른 Phase 서명이 없는 것이 있으면 불합격
- ER-04: §9 명령이 끝 판 사본에서 bash · zsh 가운데 하나라도 한 줄 이상을 내거나, 번역투를 넣은 사본에서 1 을 못 내면 불합격
- AR-01: `tone-kit` · `docs/tone` 을 건드린 비서명 커밋이 있거나, 서명 커밋이 여덟 밖의 `.harness/` 아닌 경로를 고쳤거나 여덟을 다 담지 않았거나, 봉인이 깨진 계약이 있거나, 범위 블록이 여덟 경로와 `.harness/` 한 줄이 아니면 불합격
- AR-02: 세 자리의 K-11 URL 집합이 서로 다르거나 셋이 아니거나, K-11 문단 · 원칙 9 에 `etc_seq=663` · `linecorp` 가 있거나, 스킬 · 템플릿이 바뀌었으면 불합격
- AP-01: 더한 줄에 킷 버전 값이 한 건이라도 있으면 불합격
- AP-03: 마크다운 일곱 파일 가운데 하나라도 언어 힌트 없는 여는 펜스가 있으면 불합격
- RE-01 N/A(해당 없음): 사유 측정이 0 이 아니면 불합격 — 다만 지금 측정은 늘 0 이다(참고 1)
- RE-02: 옮긴 명령 넷 가운데 하나라도 시작 커밋 표 칸 글에 두 가지 바꿈만 한 줄 그대로 끝 판 블록에 없으면 불합격
- DG-01 · DG-03 N/A: 서명 커밋이 `scripts/release.sh` 를 건드렸으면 불합격
- DG-02: 일곱 파일 가운데 하나라도 규칙별 경고 수가 편집 전보다 늘었거나 린터가 안 돌았으면 불합격
- DG-04 N/A: 서명 커밋이 `.harness/` 밖에서 `.md` · `.json` 아닌 파일을 건드렸으면 불합격
- DG-05: V1 ~ V10 열 줄이 아니거나 OK 아닌 줄이 있거나 종료 코드가 0 이 아니거나, `sync-docs` · `sync-evals` · `run-evals` 가운데 하나라도 종료 코드 0 과 정해진 한 줄을 못 내면 불합격
- DG-06: `doc-contracts` 가 FAIL · ERROR 이거나, `scope-isolation` 이 FAIL · ERROR 인데 위반 커밋 목록을 못 읽었거나 그 안에 이 Phase 서명 커밋이 있으면 불합격

### 처리 배정표

`배정` 칸이 `Phase 15` 인 행은 `F19` · `reflect-collector:P6` 둘이고(`awk` 로 배정 칸 집계 · 76 줄 · 132 줄), 둘 다 SK-01 ~ SK-05 로 반영된다.
끝의 적용 힌트 171 줄(「관측 컨벤션 강도로」)과 강도가 맞는다. 다른 행의 비고에 Phase 15 · K-11 · tone 을 가리키는 것은 없고,
앞 Phase notes 열세 개에서 tone-kit 을 넘긴 줄도 없다. 근거 파일 권장안 1 ~ 4 는 반영, 5 ~ 8 은 notes 「다음 사이클 메모」 로 넘긴다(ER-03 이 여덟 낱말로 잰다).

### 범위 · 공유 파일

고치는 여덟 파일이 전부 Phase 표의 `tone-kit/` · `docs/tone/` 안이다. 러닝북 공유 파일 목록 전부와 `docs/tone-kit/` · `docs/index.html` · `.claude/skills/` · `harness/` · `scripts/` ·
`tone-kit/.claude-plugin/plugin.json` 을 ER-03 넷째 값이 막는다. 문서 사이트 거울 두 쪽은 Final F2 로 넘긴다 — `scripts/detect-docs-drift.py` 매핑으로 보면 이번 여덟 파일에서
만들어진 페이지는 `docs/tone-kit/korean-technical-writing.html` · `overview.html` 둘뿐이고(`core-naming.html` · `core-comment.html` · `research-log.html` 은 없다), 계약 넘김 목록과 같다.

### 조건끼리

- ER-02 · ER-04 가 막는 번역투가 SK-01 · SK-04 요구 조각에 없다 — 예행 값 `k02=0` · `bash=0 zsh=0`
- AP-01 이 막는 `0.1.0` 이 요구 조각 어디에도 없다 — `18.0.1` · `3.47.5` 도 부분 문자열로 안 걸린다
- SC-02 의 `arr=7 8 3` · RE-02 `reused=4/4` · SC-01 답 여덟 줄이 같은 블록을 두고 서로 맞는다
- SK-05 `dup=0` 과 DG-02 의 MD024(같은 제목 겹침 규칙)가 같은 방향이다
- 오케스트레이터 Phase 15 지시(강도 안 올림 · 탐지 문헌 안 살림 · 스킬 셋 · `references/` 하위 폴더 없음 · bash · zsh 와 합성 양성 케이스)와 부딪히는 조건이 없다

## 참고 (막지 않는다)

1. **RE-01 측정은 늘 0 이다.** `m RE-01` 이 실제 커밋이 아니라 `common.sh` 의 고정 목록 `FILES` 를 센다. 사유가 거짓이 되는 경우(스크립트 파일을 더함)는
   AR-01 둘째 줄과 `m NA` 의 `DG-04` 가 커밋 기준으로 잡으니 구멍은 아니다. 고치려면 갈래를 `echo "code_files=$(my | grep -v '^\.harness/' | grep -cvE '\.(md|json)$')"` 로 바꾸면 된다
   (Phase 11 에서 같은 모양을 QA 가 받아들였다).
2. **DG-05 (b) · (c) 종료 코드는 저장소 전체를 본다.** 동시에 도는 Phase 16 · 17 이 끝 판 전에 자기 README 동기화를 빠뜨린 채 커밋하면 이 조건이 남 탓으로 떨어질 수 있다.
   떨어지는 쪽이라 거짓 통과는 없다. 그렇게 되면 출력에서 `변경 필요` 줄이 tone-kit 경로가 아닌지 보고 개정 파일에 적으면 된다.
3. **세션 날짜.** 「세션 관찰 (2026-09-19)」 은 세션 시작일이자 새 말을 쓴 날이다. 사용자가 되물은 것은 2026-09-22 다. 데이터 풀도 세션을 시작일로 적으니 틀린 표기는 아니다.
   SK-02 · SK-04 조각에 날짜가 박혀 있어 바꾸려면 조건 줄을 고쳐야 하므로 그대로 두는 편이 싸다.
4. `sources.md` 의 「위 표의 마지막 세 행」 은 행이 더해지면 틀려진다. 다음 사이클에 세 출처 이름으로 바꾸면 된다.
5. 측정 코드(`common.sh` · `m.sh`)는 서술 절에 있어 봉인 지문이 덮지 않는다(harness 쪽 남은 과제 「봉인이 측정 명령 덮기」). QA 는 봉인 커밋 판과 끝 판 계약의
   `회귀 게이트` 절을 `diff` 해서 측정이 안 바뀌었는지 먼저 본다.
6. 계약 596 줄 「문서의 명령 글을 `eval` 로 그대로 돌려 쟀다」 는 정확히는 손으로 옮긴 명령이다. 위 6 에서 25 개 모두 문서 글과 같음을 확인했으니 내용은 맞다.

VERDICT: CHANGES

## 2 회차

- 대상: 계약 초안 `.harness/sprint-contract-kaizen-0924-p15-tone-kit.md` (2026-09-25 11:26 저장, 692 줄, 23 조건 · 기능 조건 15, 봉인 전)
- 개정 파일은 아직 없다 — 봉인 전이라 맞다
- 검토일: 2026-09-25. 작업 폴더에는 이 절을 덧붙인 것 말고 아무것도 쓰지 않았다

### 결론

1 회차의 고칠 것 둘이 모두 들어갔고, 새로 막을 결함은 못 찾았다. 1 회차 참고 1 · 5 · 6 도 반영됐다.
고친 측정과 고친 모의 편집으로 예행 저장소를 새로 만들어 조건 스물셋을 다 쟀고, 계약 `봉인 전 실측` 절의 표와 전부 같았다.

### 1 회차 고칠 것이 들어갔는지

1. **ER-01.** 조건 줄(665 줄)이 1 회차에 적은 글과 글자 그대로 같다. `m.sh` `ER-01)` 갈래(439 ~ 442 줄)는 근거 파일을 `$T/B` 에서 읽고 셋째 줄로 `cmp` 를 돈다.
   `봉인 전 실측` 의 끝 판 표(553 줄) · 시작 커밋 판 표(584 줄) · 양성 대조 표(609 ~ 611 줄)도 고쳐졌다. 조건 줄의 `.harness 폴더` 는 백틱 없이 적혀
   커버리지 검출기 결과가 1 회차와 같다 — ER-01 에 걸리는 토큰은 여전히 notes · 근거 파일 둘이고 220 줄 해소 줄이 덮는다.
2. **모의 편집의 「대안 기호」.** `mock.py` 123 줄(`core-naming.md` §8 안내)과 287 줄(research-log 검증 문단)이 `\|` 를 정규식의 "또는" 으로 부른다.
   1 회차 제안과 따옴표 모양만 다르고, G-6 설명은 「파이프 자리의 `\|` 가 인자로 넘어가」 로 제안보다 원인을 한 단계 더 정확히 적었다.
   곧은 큰따옴표로 한국어 낱말을 감싸는 모양은 같은 파일들에 이미 있다(`core-naming.md` 4 줄 · `korean-technical-writing.md` 7 줄). `locale-korean.md` 에는 따옴표 규칙이 없다.
   155 줄 지문 `42dde3050ac3accf` 는 지금 `p15d/mock.py` 의 sha256 앞 16 자리와 같다. 계약 34 · 35 줄 배경 문장도 같은 말로 바뀌었다.
3. **참고 1 (RE-01).** 측정이 고정 목록 대신 서명 커밋 경로를 센다(486 줄). 조건 줄(682 줄) 사유와 맞고, 양성 대조가 618 줄에 더해졌다.
4. **참고 5 · 6.** 231 ~ 232 줄(QA 가 봉인 커밋 판과 끝 판의 `## 회귀 게이트` 절을 먼저 비교) · 603 줄(손으로 옮긴 명령 25 개가 문서 글과 같다)에 들어갔다.

### 다시 돌려 본 것

모두 스크래치 `p15rv2/` 아래에서만 돌렸다.

1. 계약 `## 회귀 게이트` 절의 bash 블록 셋을 새로 뽑아(`extract.sh`) DRAFT 의 `p15d/k/` 와 비교했다 — `common.sh` · `m.sh` · `lintcmp.sh` 셋 다 `diff` 0 줄.
   `p15d/contract.draft.md` 와 작업 폴더의 계약은 바이트 단위로 같다.
2. 레포 파일 `harness/skills/sprint-contract/SKILL.md` 의 Step 6.2 · 6.5 명령을 그대로 돌렸다 — 조건 23, 기능 조건 15, `FM=23 N=23`, `## ` 제목 열둘,
   조건 줄 자리 Skill 5 · Script 2 · Error 4 · Architecture 2 · Anti-patterns 2 · Reusability 2 · Diagnostics 6, `[미실측]` 0.
   `harness/references/contract-schema.md` §측정 커버리지 표기 의 검출기는 SK-02 · ER-01 · ER-03 · AR-01 넷을 낸다 — 1 회차와 같은 넷이고 217 ~ 222 줄의 해소 줄이 각각 덮는다.
3. `mock.py` 를 지금 가지 끝(`3a348d6`)을 푼 새 사본에 돌렸다 — `mock applied 8` · 종료 코드 0, 같은 사본에 두 번째는 `MOCK_FAIL start-sha tone-kit/references/locale-korean.md` · 종료 코드 3.
4. DRAFT 의 `rehearse.sh` 를 내 폴더의 `mock.py` · `notes-mock.md` 를 읽게 `S=` 한 줄만 바꿔 예행 저장소 `rh-none` · `rh-base` 를 새로 만들고, `runall.sh` 스물한 갈래와
   `V=B` 아홉 갈래를 쟀다. 계약 545 ~ 566 줄(끝 판) · 576 ~ 587 줄(시작 커밋 판)과 한 값도 다르지 않다. `runall` 종료 코드 0.
   예: ER-01 `0` · `0` · `evid_same=1`(시작 커밋 판 `0` · `NOTES_MISSING` · `evid_same=1`), SC-01 `bash=1 zsh=1 lines=8`, SK-05 둘째 줄 `1` 열, ER-02 `added=121 k02=0 names=0`,
   DG-05 `10 0 rc=0` · `sync_docs_rc=0 1` · `sync_evals_rc=0 1` · `run_evals_rc=0 1`.
5. 양성 대조를 커밋한 사본(`mut.sh` — 바꿀 때마다 서명 커밋과 `end_sha:` 줄을 더함)에서 쟀다:
   - research-log 에 근거 밖 URL → `1 0 evid_same=1`
   - 같은 URL 을 끝 판 근거 파일에도 → `1 0 evid_same=0`
   - research-log 만 되돌림 → `0 0 evid_same=0`
   - 둘 다 되돌림 → `0 0 evid_same=1`
   - notes 에 근거 밖 URL → `0 1 evid_same=1` (계약이 적지 않은 둘째 값 양성 대조)
   - 서명 커밋으로 `tone-kit/scripts/a.sh` 를 더함 → `code_files=1` · 같은 사본 `m NA` → `DG-01=0 DG-04=1`

   계약 609 ~ 611 줄 · 618 줄 값과 같다.
6. `rh-none` 에서 `대안 기호` 는 `tone-kit` · `docs` · `harness` 에 0 줄이다. 1 회차 뒤 모의 편집에서 바뀐 것은 위 두 문장뿐이다(1 회차의 `mock2.py` 와 비교해 두 줄).
   두 문장의 「파이프 자리」 · 「표 칸」 은 시작 커밋 판에 없는 말이지만, 흔한 낱말 둘을 이어 자리를 설명한 것이지 무언가를 부르는 새 이름이 아니다. K-11 위반으로 보지 않는다.
7. 시작 커밋 뒤 `tone-kit` · `docs/tone` · 근거 파일을 건드린 커밋은 0 이다. 그 사이 `89f0ef8`(Phase 13 봉인)이 새로 올라왔고 이 킷과 상관없다.
   notes 는 이제 열세 개(Phase 14 가 더해짐)이고, 계약 15 줄의 `grep -l 'tone-kit\|Phase 15'` 는 여전히 0 파일이다.

### 참고 (막지 않는다)

1. 계약 15 · 63 줄 「앞 Phase notes 열두 개」 는 초안을 쓸 때의 수다. 지금은 열세 개이고 결과는 같다 — 서술 줄이라 봉인과 상관없다. BUILD 가 봉인 전에 「열세 개」 로 고치면 맞는다.
2. 648 줄 「지금 가지 끝(`3a348d6`)」 뒤로 `89f0ef8` 이 올라왔다. `tone-kit` · `docs/tone` 변경이 0 이라 `mock applied 8` 결과는 같다.
3. 1 회차 참고 2(DG-05 (b) · (c) 가 저장소 전체를 본다) · 3(세션 날짜) · 4(「위 표의 마지막 세 행」 — notes 틀의 다음 사이클 메모에 들어가 있다)는 그대로 남는다.

VERDICT: APPROVE
