# Phase 2 계약 초안 검토 — kaizen-0924-p02-contract

- 대상: `.harness/sprint-contract-kaizen-0924-p02-contract.md` (봉인 전 초안, 조건 28 줄 · 기능 조건 19,
  내용 sha256 `1acb55e8…`)
- 역할: 카이젠 2026-09-24 Phase 2 REVIEW (사용자 승인 5 단계를 대신하는 독립 검토자)
- 검토일: 2026-09-24 · 작업 폴더 `HEAD` = `43fc9ee` (계약의 시작 커밋 `76cfb37` 뒤에 Phase 15 · 16 · 17 근거 커밋 둘.
  네 대상 파일은 시작 커밋과 같다)
- 레포 파일은 이 파일 하나만 썼다. 임시 사본과 시험 스크립트는
  `/private/tmp/claude-501/-Users-jackson-Hub-10-Dev-claude-plugins/de8c7935-a5b6-4df5-9106-fafa73c288a0/scratchpad/p2review/` 에 있다.
  검토 전후로 `git status --short` 가 같다

## 결론

고쳐서 다시 내야 한다(CHANGES). 뼈대는 튼튼하다. 조건 28 개 모두 실패 상태를 한 문장으로 쓸 수 있고, 계약 본문에서
기계로 떼어 낸 공통 정의 · 시험 스크립트로 봉인 전 실측 값을 다시 돌려 편집 전 · 모의본 양쪽 모두 계약 표와 같게 나왔다.
처리 배정표 Phase 2 행 11 개는 전부 조건이나 넘김 사유로 다뤄졌고, 고치는 파일 넷은 러닝북 Phase 2 범위 안이며 공유 파일은
건드리지 않는다.

막는 이유는 여섯이다.

1. 러닝북 계약 규칙 「특정 문장이 있어야 한다는 조건은 그 문장만 지운 사본에서 FAIL 이 나는지 돌려 본다」를 하지 않았다.
   직접 돌려 보니 다섯 곳이 핵심 문장을 지워도 통과한다 (SK-02 · SK-03 · SK-05 · AR-03)
2. 모의본이 AR-01 을 통과하고도 `contract-schema.md` 표준형 (1) 상태 전제 정의가 옛 두 선택지만 적은 채 남는다 — 같은 절의 새
   문단은 그 둘이 커밋 뒤 빈 출력이라고 말하고, 예시는 정의에 없는 세 번째 전제를 쓴다
3. `SKILL.md` Gotchas 셋째 줄(메타 이슈 F1 의 스킬 쪽 안내 · F3)은 배경 표가 반영한다고 적었는데 재는 조건이 없다
4. SK-06 의 멈춤 조건 「셸 실행 도구와 파일 쓰기 도구가 없는 세션」은 셸만 없는 세션을 놓친다. 근거 파일(§2 F21 · §4-7 · §5)이
   요구한 「대체 경로가 있는 도구는 멈추지 말 것」 구분도 없다
5. 반대편 목록에서 평가자 쪽 세 자리가 빠졌다. 그중 `qa-evaluation-guide.md:1785` 는 AR-01 이 틀렸다고 고치는 `--cached` 권고를
   평가자에게 그대로 가르친다. 평가자 가이드 parity 표에 16 행이 없는 것도 contract-kaizen Gotcha 「Cross-Surface Parity 전파 누락
   금지」가 Phase 3 넘김으로 적으라고 한 항목이다
6. DG-06 의 `scope-isolation` 예외 분기(다른 Phase 커밋 때문에 FAIL 일 때)에 재는 명령이 없다

## 다시 돌려 본 것

공통 정의 블록 · `f3test.sh` · `fence.py` · `dg07.py` · `new-warnings.sh` 다섯 블록을 계약 본문에서 기계로 떼어 냈다.

- 공통 정의를 그대로 bash 로 실행하면 개정 파일이 아직 없어 `END_UNRESOLVED` 를 찍고 종료 코드 2 로 멈춘다 — 설계대로다
- `END` 자리와 `$T` 채우기만 바꾼 사본으로 편집 전(시작 커밋 판) · 모의본(초안의 `mock.py` 를 시작 커밋 판에 다시 적용)을 쟀다.
  28 조건의 측정 값이 계약 `회귀 게이트` 절 표와 전부 같다. 예: SK-02 (d) 편집 전 `[1 2 3 11 12 13 14 ]` · 모의본
  `[1 2 3 11 12 13 14 15 16 ]`, SK-07 (a) 편집 전 옛 문구 12 · 이름 23 → 모의본 0 · 25 · 잃은 이름 0, AR-05 (c) `SV/AV` = `1.6.0/1.7.0`,
  RE-02 `SC=15 SK=6 GD=18 RF=1` 양쪽 같음, DG-07 편집 전 `3 · 19 · 2 · 2 · 1 · 2` → 모의본 `… 2 · 2`
- 알려진 답 두 곳: SK-03 (d) 계산식 × Phase 1 봉인 계약(`51a22b4`) bash `16` · zsh `16`. 손으로도 셌다 — SK 4 · ER 5 · AR 4 ·
  DG-05~07 3 = 16. 같은 계산식을 이 초안에 돌리면 19 로 배경 문구와 같다. AR-02 (b) 스니펫은 bash · zsh 둘 다
  `mine OK [a.md b.md ]` · `unsigned_on OK (c4)`
- 음성 대조 둘도 다시 만들었다: `--grep` 의 `^` `$` 를 뺀 변이 → `mine NG [a.md b.md other.md ]`, `grep -qxF … || echo` 를 `true`
  로 바꾼 변이 → `unsigned_on NG []`
- 양성 대조: ER-01 1 · ER-02 UTF-8 2 / C 로케일 1 · AP-01 1 · AP-03 `bare_open_total=1`. 이 맥에 `C.UTF-8` 로케일이 있다
  (`locale -a`)
- DG-02: 고친 `new-warnings.sh` 로 모의본 네 파일 `new_warnings=0`. 편집 전 경고 줄 수 4 · 10 · 5 · 1 이 `범위 경계` 절과 같다
  (markdownlint-cli2 0.23.2 · MD013 끔)
- DG-05: 지금 작업 폴더 `validate-plugin.py harness` V1~V10 모두 OK, `check-stale-values.py` 종료 코드 0. 모의본 네 파일을 얹은
  스크래치 사본에서도 V6 · V9 · V10 포함 10 검사 OK
- DG-06: 지금 `--since 76cfb37 --verbose` → `15 checks — 12 PASS / 0 FAIL / 0 ERROR / 3 SKIP`
- AR-06 ③: 계약 봉인 정의 네 함수를 `contract-schema.md` 에서 떼어 돌리면 `SEAL_OK 51 · SEAL_ABSENT 11 · SEAL_BROKEN 0` —
  계약 값과 같다. 작업 트리에서 수정 중인 Phase 1 계약도 `SEAL_OK` 다
- sprint-contract Step 6.5 (1)(2)(3)(5): 제목은 허용 목록 안, 조건 체크박스는 조건 절에만, `conditions: 28` = 실제 28, `[미실측]` 0 건
- 인용된 사실 대조: `fc29b58` 커밋 시각 `20:41:11 +0900` · 그 판 개정 파일 `created: "2026-09-24 20:50"`, 2026-09-22 REJECT 두 건
  (`jq` 가 `/usr/bin/jq` · `find` 가 3 개 더), facets `8fa13c90` · `858c246f` · `d204ea78` 내용 모두 데이터 풀과 맞는다

## 항목별 판단

- **처리 배정표**: `배정` 칸이 Phase 2 인 `F11` `F12` `F13` `F17` `F21` `F27` `harness:P03` `harness:P05` `harness:P06` `harness:P08`
  `user-setup:P5` 11 개 모두 배경 표에 있고, 넘기는 부분(`/sprint` 두 자리 · `Evaluated` · 계약 검사기)은 Counterpart 표와 ER-03 에
  사유가 있다. `Phase 별 적용 힌트` Phase 2 줄과도 맞는다. 러닝북 추가 과제 둘(메타 이슈 1 F1~F4 · F11 크기 규칙)도 다뤘다.
  F1 이 2026-09-23 절로 이미 해결됐다는 확인은 맞다(`contract-schema.md` `##### .harness/ 범위 조건` 절 실재)
- **범위**: 고치는 파일 넷은 러닝북 Phase 표 Phase 2 행 안이다. `contract-kaizen/SKILL.md` · `feedback-schema.yaml` ·
  `save-feedback.sh` · `sprint/SKILL.md` 를 Phase 4 로, 평가자 두 파일을 Phase 3 으로 넘긴 판단이 맞다. 공유 파일은 건드리지 않는다
- **외부 근거**: 새 URL 다섯 개가 전부 근거 파일에 있다(ER-01 모의본 0). 근거 파일의 보정 권고 둘 — 「zsh 일반 배열은 기본
  옵션에서 1 부터」 · 「0 이 나와도 통과가 아니라 스크립트 또는 입력 결함」 — 을 모의본 문구가 따랐다. 근거 파일이 스스로 밝힌
  한계(수치 · 끝맺음 · 멈춤에 직접 근거 없음)를 계약이 그대로 옮긴 것도 맞다
- **판정 가능성**: 조건마다 실패 문장을 쓸 수 있다. 예: AR-02 「`contract-schema.md` 에 `##### 여러 주체가 한 가지에 커밋할 때` 제목이 없거나
  Diff-Scope 절 밖에 있거나, 그 절의 첫 bash 블록을 `f3test.sh` 에 넣었을 때 bash 나 zsh 중 하나라도 두 줄 OK 가 아니다」
- **조건끼리 충돌**: 모의본이 28 조건을 한꺼번에 통과했다. SK-03 (e) 의 새 Gotcha 와 AR-06 ④ 「옛 Gotcha 는 덧붙이기만」 도
  같이 선다. 부딪히는 곳은 없다. 다만 아래 2 번처럼 **조건은 다 통과하는데 문서 안에서 말이 어긋나는** 곳이 하나 있다

## 고칠 것

### 1. SK-02 · SK-03 · SK-05 · AR-03 — 핵심 문장을 지운 사본에서도 통과한다

러닝북 계약 규칙대로 모의본에서 핵심 문장 하나씩만 지운 사본을 만들어 해당 측정을 돌렸다. 다섯 곳 모두 요구값이 그대로 나왔다.

```text
지운 문장 (모의본 위치 · SC = contract-schema.md)                   측정 결과 → 판정
SK-02 (a)  SC §알려진 답 대조 「판정」 불릿 (다르면 봉인하지 않는다)   7 토큰 전부 1 이상 → PASS
SK-03 (a)  SC §조건 수 「기능 조건은 … 줄을 뺀 나머지다」 정의 문장   기능 조건=1 (표 머리줄) → PASS
SK-03 (b)  SKILL Step 2 「기능 조건은 자동 포함 여섯 줄 … 을 뺀 조건 줄이다」  0 · 1 · 1 · 1 → PASS
SK-05 (c)  SC §메타데이터 「시각 필드는 date 출력을 옮긴다 — 짐작해 적지 마라」   20:41:11=1 · date-invocation=1 → PASS
AR-03      SC preflight 소절 「이 스프린트가 소유한 줄의 이름을 적고 …」 불릿   소유한 줄=1 (둘째 불릿) → PASS
```

아래 토큰을 더하면 편집 전 0 · 모의본 1 · 그 문장만 지운 사본 0 으로 갈린다(셋 다 실측).

- **SK-02** (a) 토큰 목록에 `다르면 봉인하지 않는다` 를 더한다 — 「… `미실측 오라클 봉인 금지` · `다르면 봉인하지 않는다` 가 각각
  1 건 이상」. 측정 `tok "$(sect "$T/SC" '#### 알려진 답 대조')" … '다르면 봉인하지 않는다'` (8 값). 봉인 전 실측: 편집 전 0 · 모의본 1 ·
  판정 불릿을 지운 사본 0
- **SK-03** (a) 에 `` `N/A (사유)` 줄을 뺀 `` 1 이상, (b) 에 같은 토큰 1 을 더한다. 측정 (a) 는 기존 `tok` 에 인자 하나를 더해 8 값,
  (b) 는 5 값(`0 · 1 · 1 · 1 · 1`). 봉인 전 실측: 편집 전 0 · 0, 모의본 1 · 1, `contract-schema.md` 정의 문장을 지운 사본 (a) 0, SKILL 정의 문장을
  지운 사본 (b) 0. `자동 포함 여섯 줄` 은 `contract-schema.md` 의 다른 문장(옛 표가 11 줄이던 이유)에도 있어 가르지 못한다
- **SK-05** (c) 에 `짐작해 적지 마라` 1 이상을 더한다 (3 값). 봉인 전 실측: 편집 전 0 · 모의본 1 · 핵심 문장을 지운 사본 0
- **AR-03** 4 토큰의 `소유한 줄` 을 `소유한 줄의 이름` 으로 바꾼다. 봉인 전 실측: 편집 전 0 · 모의본 1 · 첫 불릿을 지운 사본 0
- 나머지 토큰 조건(SK-01 · SK-04 · SK-06 · SK-07 · AR-01 · AR-02 · AR-04)도 같은 방식으로 핵심 문장 하나를 지운 사본에서 0 이
  나오는지 돌리고, `회귀 게이트` 절에 「문장 삭제 대조」 표로 남긴다. 검토자가 문구 배치로 따져 본 바로는 SK-01 (a)(b) · SK-04 ·
  SK-07 · AR-01 (a) 의 `커밋하고 나면 빈 출력` · AR-04 의 `1 부터` 는 한 줄에만 있어 갈릴 것으로 보이지만, 규칙은 돌린 결과를 요구한다

### 2. AR-01 — 표준형 (1) 상태 전제 정의가 옛 두 선택지로 남는다

모의본 `contract-schema.md:571` 은 편집 뒤에도
「**(1) 상태 전제** (`Given: 커밋 직전 working tree` 또는 `Given: 스테이징 완료 후`)」다. 바로 아래 예시는 정의에 없는
`Given: 이 스프린트의 커밋이 끝난 뒤` 를 쓰고, 그 아래 새 문단은 정의에 있는 두 전제가 커밋 뒤 빈 출력이라고 말한다. 가이드 쪽 표
1 행은 모의본이 세 선택지로 고쳤으니 두 문서도 어긋난다. 이 초안이 고치려는 결함(메타 이슈 F2)이 정의 줄에 그대로 남는 셈이다.

- 조건 문구 (a) 끝에 더한다: 「… 옛 예시 `git diff --name-only HEAD -- app/lib` 0 · `**(1) 상태 전제**` 정의 줄에
  `Given: 이 스프린트의 커밋이 끝난 뒤` 1」
- 측정 (a) 에 더한다: `sect "$T/SC" '#### Diff-Scope Oracle 표준형' | grep -F '**(1) 상태 전제**' | grep -cF 'Given: 이 스프린트의 커밋이 끝난 뒤'` 1
- 봉인 전 실측: 편집 전 0 · **지금 모의본 0** · 아래 치환을 더한 모의본 1
- `mock.py` 의 `contract-schema.md` 쪽에 치환 하나를 더한다(옛 → 새, 각 한 줄). 이 치환을 얹은 사본에서 AR-01 (a) 기존 세 값은
  1 · 2 · 0 그대로, ER-02 0 · AP-01 0 · DG-02 새 경고 0 이다

```text
옛: **(1) 상태 전제** (`Given: 커밋 직전 working tree` 또는 `Given: 스테이징 완료 후`) ·
새: **(1) 상태 전제** (`Given: 이 스프린트의 커밋이 끝난 뒤` · `Given: 커밋 직전 working tree` · `Given: 스테이징 완료 후` 중 하나 — 평가 시점에 다시 잴 수 있는 것) ·
```

### 3. AR-02 — Gotchas 셋째 줄을 재는 조건이 없다

배경 표는 메타 이슈 F1 을 「`SKILL.md` 쪽 안내만 더한다」로, 개선안은 Gotchas 셋째 줄(여러 주체 커밋 · `verify_seal` · `mine` ·
`unsigned_on`)로 적었다. 그런데 SK-01 (b) 는 첫째 줄, SK-03 (e) 는 둘째 줄만 재고 AR-06 ④ 는 옛 줄 보존만 잰다. 셋째 줄을 빼먹어도
28 조건이 전부 통과한다.

- AR-02 에 (c) 를 더한다: 「(c) `SKILL.md` Gotchas 구간에 `verify_seal` 과 `unsigned_on` 을 함께 담은 줄이 1 개 이상」
- 측정: `sect "$T/SK" '## Gotchas' | grep -F 'verify_seal' | grep -cF 'unsigned_on'` 1 이상
- 봉인 전 실측: 편집 전 0 · 모의본 1 (검토자 실측). 조건 줄은 늘지 않으므로 `conditions: 28` · 기능 조건 19 는 그대로다
- 모의본의 그 줄과 `contract-schema.md` 새 소절에 「실측(2026-08-13): 한 Phase 계약이 **이 때문에** 세 번 깨졌다」는 사실보다 크다. 핸드오프
  `.harness/.meta/phase4-handoff-to-contract.md` 를 보면 세 번 가운데 첫 번은 자기 개정 파일 경로가 열거에 없어서였고, 남의 커밋
  때문은 두 번이다(F1 · F3 절). `contract-schema.md` 쪽은 「범위 조건이 세 번 깨졌고, 그중 두 번은 오케스트레이터가 감사 기록 커밋을 그 구간에
  넣어서였다」, Gotcha 쪽은 「세 번 깨졌고 그중 두 번이 남의 커밋 때문이었다」로 고친다(조건 영향 없음 — 검토자 사본에서 AR-02 (a)
  7 토큰 · AR-06 ④ 0 그대로)

### 4. SK-06 — 셸만 없는 세션을 놓치고, 대체 경로가 있는 도구를 가르지 않는다

「셸 실행 도구와 파일 쓰기 도구가 없는 세션이면 여기서 멈춘다」는 둘 다 없을 때만 걸린다. 파일 쓰기 도구는 있고 셸이 없는 세션은
이 규칙에 안 걸린 채 계속 가서, 측정 명령을 한 번도 못 돌린 계약을 쓴다 — F12 가 막으려는 바로 그 실패다. 문단 스스로 댄 이유
(「확인 단계는 전부 명령 출력으로 판정하므로 대체 경로가 없다」)도 셸에만 해당한다. 반대로 파일 쓰기 도구만 없으면 셸로 파일을 쓰면
되니 멈출 이유가 없다. 근거 파일 §2 F21 · §4-7 · §5 가 「필수 도구이고 대체 경로가 없을 때만 멈추고, 대체 경로가 있으면 멈추지
말라」고 따로 짚은 부분이다.

- 조건 문구: 「`SKILL.md` Step 0 구간에 `셸 실행 도구가 없는 세션` 1 · `파일 쓰기 도구만 없으면` 1 · `네 칸` · `재검증 명령` ·
  `skill-design-guide.md` 각 1 이상」
- 측정: `tok "$(sect "$T/SK" '### 0. CONTRACT_ROOT')" '셸 실행 도구가 없는 세션' '파일 쓰기 도구만 없으면' '네 칸' '재검증 명령' 'skill-design-guide.md'`
  1 · 1 · 1 이상 · 1 이상 · 1 이상
- `mock.py` 문단 첫 두 문장 → 「**셸 실행 도구가 없는 세션이면 파일 쓰기 도구가 있어도 여기서 멈춘다 (2026-09-24 추가).** 이 스킬의
  확인 단계(Step 0 · 0.5 · 6.2 · 6.5 · 6.6 · 6.7)는 전부 명령 출력으로 판정하므로 대체 경로가 없다. 파일 쓰기 도구만 없으면 멈추지
  말고 셸로 파일을 쓴다.」 나머지(네 칸 · 재검증 명령 · 실측)는 그대로
- 봉인 전 실측: 편집 전 0 · 0 · 0 · 0 · 0, 위 문구를 얹은 모의본 1 · 1 · 1 · 1 · 1 (검토자 실측)
- 배경 표 `F21` 행과 개선안 요지 「셸 실행 도구와 파일 쓰기 도구가 없는 세션이면 …」 도 같이 고친다

### 5. ER-03 · Counterpart 표 — 평가자 쪽 세 자리가 빠졌다

`Given: 스테이징 완료 후` · `--cached` · parity 를 레포 전체에서 다시 찾았다(`.harness/` · `docs/kaizen/` · 네 대상 파일 제외).
표에 없는 곳이 셋 나온다. 모두 Phase 3 범위라 고칠 수는 없고, 넘김으로 적어야 한다.

```text
harness/docs/guides/qa-evaluation-guide.md:1785   평가자가 계약 수정 제안을 쓰는 예시가 「`Given: 스테이징 완료 후` 를 붙이고 `--cached` 를 쓸 것」
                                                  — AR-01 이 「커밋 뒤에 늘 빈 집합을 잰다」고 고치는 옛 규칙을 평가자에게 가르친다
harness/agents/qa-evaluator.md:590 · qa-evaluation-guide.md:743   상태 전제 확인이 `커밋 직전 working tree` · `스테이징 완료 후` · 브랜치 비교만
                                                  나열 (표에는 :590 이 「4 요소」로만 올라 있고 ER-03 토큰에는 :1217 만 있다)
harness/docs/guides/qa-evaluation-guide.md:1862   Parity Table 머리 「9 개」 · 15 행까지만 있고 16 행(알려진 답 대조) 없음
                                                  — contract-kaizen Gotcha 「Cross-Surface Parity 전파 누락 금지」는 이런 빈 곳을 해당 Phase 에 넘김으로 적게 한다
```

- `GAP 분석` 절 Counterpart 표에 세 행을 더한다 (처리: 「Phase 3 — 넘김 (ER-03)」)
- ER-03 조건 문구의 경로 목록에 `harness/agents/qa-evaluator.md:590` · `harness/docs/guides/qa-evaluation-guide.md:1785` ·
  `harness/docs/guides/qa-evaluation-guide.md:1862` 셋을 더해 「경로 9 개」로 고친다
- 측정의 `for t in …` 목록에 같은 세 문자열을 더하고 「17 값」을 「20 값」으로 고친다. `범위 경계` 절의 「커버리지 해소: ER-03 —
  산문의 넘김 경로 6 개와 키 11 개 … 17 문자열」도 「경로 9 개 … 20 문자열」로 맞춘다
- 봉인 전 실측 문구: 20 문자열을 담은 가짜 notes 로 20 값 전부 1, 한 줄을 지우면 그 값 0

### 6. DG-06 — `scope-isolation` 예외 분기에 재는 명령이 없다

「다른 Phase 커밋 때문에 `scope-isolation` 이 FAIL 이면 `--verbose` 위반 커밋 목록에 서명 줄 커밋이 없을 때 PASS」 는 명령이
없어 평가자가 즉석에서 방법을 골라야 한다. `doc-contracts` 쪽 예외는 명령이 있다. 같은 모양으로 맞춘다.

- 측정 절 끝에 더한다: 「`scope-isolation` 이 `FAIL` 일 때만 —
  `python3 scripts/validate-post-kaizen.py --since "$B" --verbose > "$T/vpk.txt" 2>&1` 뒤
  `awk '/ scope-isolation: /{f=1;next} /^\[ /{f=0} f&&/^ +[0-9a-f]{7,40}$/{print $1}' "$T/vpk.txt" | grep -c .` 이 1 이상(목록을 실제로 읽었다) ·
  `awk '/ scope-isolation: /{f=1;next} /^\[ /{f=0} f&&/^ +[0-9a-f]{7,40}$/{print $1}' "$T/vpk.txt" | while read -r c; do git log -1 --format=%B "$c" | grep -qxF "$SIG" && echo "$c"; done | grep -c .` 0」
- 첫 값을 함께 거는 이유: 출력 형식이 바뀌어 목록을 하나도 못 읽으면 둘째 값이 조용히 0 이 된다
- 봉인 전 실측(검토자): 위반 목록에 `51a22b4` · `43fc9ee` 두 줄을 넣은 가짜 출력에서, `SIG` 를 Phase 1 서명 줄로 두면 읽은 수 2 ·
  서명 커밋 1 (양성 대조), `C.UTF-8` · `C` 두 로케일에서 같다. 지금 실제 출력(PASS)에서는 읽은 수 0 — 이 분기를 쓰지 않는 경우다
- 검사 스크립트는 위반 커밋을 앞 8 자리로 찍는다(`scripts/validate-post-kaizen.py` `check_scope_isolation`). 위 정규식은 7~40 자리를 받는다

## 고치면 좋지만 막지는 않는 것

- `범위 경계` 절 「그 밖에는 새 셸 블록을 넣지 않는다 (AP-03)」 — AP-03 은 언어 힌트 없는 펜스만 재고 새 셸 블록 수는 재지 않는다.
  괄호를 지우거나, 재려면 `added | grep -c '^+```bash$'` 가 3 (모의본 실측 3)
- SK-07 문구의 「최근 계약 피드백 31 건 중 19 건」은 움직이는 창이다. 검토 시점에 같은 방식(수정 시각 순 최근 40 건)으로 다시 세니
  `nfr_coverage` 20/31 이었다(그 사이 피드백 파일이 늘었다). 문서에 넣을 때는 「2026-09-24 21 시 기준」 처럼 잰 시점을 붙인다
- `nfr_coverage` 는 이름이 「덮었다」로 읽혀 true 뜻을 고정해도 다시 섞이기 쉽다. 이름은 호환 때문에 그대로 두더라도 Step 7 문항 끝에
  「(true = 빠졌다)」를 붙여 두면 덜 틀린다
- contract-kaizen Gotcha 「`contract-schema.md` 를 바꾸면 PR 본문에 반드시 적는다」 — notes 에 「`contract-schema.md` v5.4 → v5.5, PR 본문에 적을 것」
  한 줄을 남겨 Final 이 옮기게 한다
- Phase 4 로 넘기는 네 자리(`/sprint` 두 곳 · Step 9 문구 · `feedback-schema.yaml` · contract-kaizen Step 2)는 러닝북의 Phase 4 입력 목록에
  phase2-notes 가 없다. notes 에 「Phase 4 가 읽을 것」 소제목을 두고, 오케스트레이터가 Phase 4 를 부를 때 이 파일을 넘기게 적는다
- DG-02 준비 단계: 계약은 「스크래치 폴더에 `npm install`」 이라고만 적었다. SK-01 이 요구하는 준비 단계 실측을 이 계약에도 적용하려면
  QA 가 쓸 수 있는 설치 위치(초안이 쓴 `…/scratchpad/p1build/node_modules`)와 `markdownlint-cli2 --version` 출력
  (`v0.23.2 (markdownlint v0.41.1)`, 검토자 실측)을 `회귀 게이트` 절에 한 줄 적는다

## 다시 낼 때 확인할 것

- 위 1~6 반영 뒤 `mock.py` 를 다시 돌려 28 조건 전부 요구값, 문장 삭제 대조 표, AR-01 (1) 줄 치환 전 0 · 치환 뒤 1 을 `회귀 게이트`
  절에 남긴다
- 조건 줄 수는 변하지 않는다(28 · 기능 조건 19). 계약 파일을 다시 저장한 뒤 Step 6.2 · 6.5 를 다시 돌린다

VERDICT: CHANGES

## 2 회차

- 대상: 같은 계약 초안의 검토 반영판 (604 줄, 조건 28 줄 · 기능 조건 19, 내용 sha256 `09532b62…`), 모의본은
  `p2draft2/mock.py` (1 회차 검토자가 쓴 스크래치 `p2review/` 와 따로 `p2review2/` 에서 다시 만들었다)
- 검토 시각: 2026-09-24 22:0x ~ 22:14 · 작업 폴더 `HEAD` = `43fc9ee` 그대로 (네 대상 파일은 시작 커밋 `76cfb37` 과 같다)
- 레포 파일은 이 절을 덧붙인 것 하나만 썼다. 임시 사본과 시험 스크립트는
  `/private/tmp/claude-501/-Users-jackson-Hub-10-Dev-claude-plugins/de8c7935-a5b6-4df5-9106-fafa73c288a0/scratchpad/p2review2/` 에 있다.
  검토 전후로 `git status --short` 가 같다

### 2 회차 결론

한 곳만 더 고치면 된다(CHANGES). 1 회차에서 막은 여섯 건은 전부 반영됐고, 다시 돌려 봐도 계약에 적힌 값이 그대로 나온다.
새로 생긴 결함은 하나다 — 1 회차 5 번을 반영하면서 ER-03 의 넘김 경로 토큰 가운데 줄 번호 없는
`harness/docs/guides/qa-evaluation-guide.md` 가 늘 1 이상이 되어, `:12` · `:746` · `:1915` · `:742-743` 넘김을 notes 에서 빠뜨려도
통과한다. 러닝북 계약 규칙(그 문장만 지운 사본에서 실패해야 한다)에 걸리는 것이라 막는다. 토큰 하나를 넷으로 바꾸는 기계적인
수정이고, 나머지 27 조건은 다시 볼 필요가 없다.

### 1 회차 반영 확인

| 1 회차 | 반영 | 확인한 방법 |
| --- | --- | --- |
| 1 문장 삭제 대조 | 됐다 | SK-02 (a) `다르면 봉인하지 않는다` · SK-03 (a)(b) `` `N/A (사유)` 줄을 뺀 `` · SK-05 (c) `짐작해 적지 마라` · AR-03 `소유한 줄의 이름` · `마지막 단계(카이젠이면 Final)` 가 조건과 측정에 들어갔다. 계약의 30 건 표와 별도로 17 건을 내 손으로 지워 돌렸다 — 전부 모의본 1 → 지운 사본 0 (아래) |
| 2 AR-01 정의 줄 | 됐다 | 조건 (a) 넷째 값과 `mock.py` 치환 `SC-f2-def`. 모의본 1, 새 선택지만 옛 문구로 되돌린 사본 0 |
| 3 AR-02 (c) · 「세 번」 문구 | 됐다 | 모의본 1, 그 Gotcha 줄만 지운 사본 0. 새 문구 「세 번 깨졌고 그중 두 번은 오케스트레이터가 감사 기록 커밋을 …」 을 핸드오프 `.harness/.meta/phase4-handoff-to-contract.md` F1 · F3 절과 다시 대조했다 — 첫 번은 자기 개정 파일 경로 누락, 둘째 · 셋째가 감사 기록 커밋이다. 맞다 |
| 4 SK-06 | 됐다 | 두 토큰 각각 모의본 1 → 지운 사본 0. 근거 파일 §4-7 「필수 도구이고 대체 경로가 없으면 … 중단 / 대체 경로가 있으면 즉시 중단하지 말고」 와 뜻이 맞다. 네 칸 이름도 `skill-design-guide.md:302-306` 과 같다 |
| 5 ER-03 · 반대편 목록 | 됐다, 그러나 새 결함 | 세 행과 세 경로가 들어갔다. 인용한 줄 번호 여덟 개(`qa-evaluation-guide.md` `:12` `:742-743` `:746` `:1785` `:1862` `:1915`, `qa-evaluator.md` `:590` `:1217`)를 지금 파일에서 다시 읽었다 — 전부 맞다. 결함은 아래 「고칠 것」 |
| 6 DG-06 예외 분기 | 됐다 | 가짜 출력이 아니라 **실제 검사 출력**으로 돌렸다: 복제 저장소에 `harness/skills/` 와 `tone-kit/skills/` 를 한 커밋에 섞은 서명 없는 커밋을 넣자 `[ FAIL  ] ✗ scope-isolation: 1 cross-phase commits` · 다음 줄 `수정:` · 그다음 들여쓴 줄 `473b9f8d`. 읽은 수 1 · 서명 커밋 0 (`C.UTF-8` · `C` 같음). 이 Phase 서명 줄을 단 섞인 커밋을 더하면 읽은 수 2 · 서명 커밋 1 |

막지 않는 제안 여섯 가운데 넷은 계약 본문에, 둘(PR 본문 한 줄 · `## Phase 4 가 읽을 것`)은 `범위 경계` 넘김 줄에 BUILD 할 일로
들어갔다. DG-02 준비 단계도 적혔다 — 그 자리의 `markdownlint-cli2 --version` 첫 줄이 `v0.23.2 (markdownlint v0.41.1)` 로 나왔다.

### 2 회차에 다시 돌려 본 것

- **전체 예행 (따로 만든 복제본)**: 작업 폴더를 스크래치에 복제(`p2review2/rh`)해 `43fc9ee` 위에 서명 줄 계약 커밋 · 모의본 네 파일
  커밋 · 서명 없는 남의 커밋(`.harness/` 한 파일, 본문에 서명 글자 인용) · 서명 줄 notes 커밋 · `end_sha` 커밋을 쌓았다. 계약 본문의
  코드 블록 다섯 개를 기계로 떼어 내고(`cd` 줄만 바꿈) 28 조건 측정을 조건 문구대로 옮긴 묶음(`meas.sh`)을 돌렸다 — 28 조건 전부
  요구값, 값이 `회귀 게이트` 절 표와 하나하나 같다. 예: SK-03 (d) bash `16` · zsh `16`, SK-07 (a) `1 · 0 · 25 · 0`, AR-01 (a)
  `1 · 2 · 0 · 1`, AR-02 (b) bash · zsh 둘 다 두 줄 OK, AR-06 ① 0 · ② 0 · 4 · ③ `SEAL_OK 51 · SEAL_ABSENT 11` · 이 Phase 몫 0 ·
  ④ 0 (42 → 45 줄), DG-02 네 파일 `new_warnings=0`, DG-07 `3 · 19 · 2 · 2 · 2 · 2`, 더한 줄의 bash 펜스 3
- DG-05 · DG-06 도 같은 복제본에서: V1~V10 모두 OK · `check-stale-values.py` 종료 코드 0 · 네 경로 0,
  `scope-isolation` PASS (7 commits) · `doc-contracts` PASS · `docs-site-regen` FAIL (판정에서 빼는 줄)
- **편집 전 값**: 공통 정의의 `END` 자리에 `$B` 를 넣어 같은 묶음을 돌렸다 — 새 문구 전부 0 · 옛 문구 전부 1 이상, DG-02 편집 전
  경고 4 · 10 · 5 · 1 로 계약 표와 같다
- **문장 삭제 대조 17 건** (내가 고른 문자열, `mydel.py`): SK-01 (b) Gotcha · SK-02 (a) 판정 불릿 · SK-03 (a)(b) 정의 문장 ·
  SK-04 · SK-05 (c) · SK-06 두 문장 · SK-07 (a) · AR-01 (a) 첫째 · 넷째 값 · AR-02 (a) `조용히 빠진다` · AR-02 (c) · AR-03 세 불릿 ·
  AR-04 `1 부터` — 17 건 모두 모의본 1 → 지운 사본 0
- **sprint-contract Step 6.2 · 6.5** (현재 레포 판 `SKILL.md` 명령을 그대로): 조건 28 = `conditions: 28`, 제목은 허용 목록 안,
  조건 체크박스는 조건 절에만, `[미실측]` 0 건. 커버리지 검출기(`contract-schema.md` §측정 커버리지 표기)는 `UNCOVERED` 9 건
  (SK-01 · SK-02 · SK-03 · SK-05 · SK-06 · ER-03 · AR-02 · AR-04 · AR-05) — 전부 `범위 경계` 의 `커버리지 해소:` 줄이 받는다.
  모의본 6.2 둘째 명령으로 이 계약을 세면 bash · zsh 둘 다 19
- **시각 기록**: 계약 머리 `created: "2026-09-24 21:20"` 은 초안 에이전트 세션 기록의 `date '+%Y-%m-%d %H:%M'` 출력
  `2026-09-24 21:20` (12:20:34Z) 과 같다 — 이 계약이 넣으려는 규칙을 스스로 지켰다. 배경 7 번의 `21:49 기준` 20/31 과
  `회귀 게이트` 절의 `21:59 다시 잼` 도 세션 기록에 그 시각의 측정 출력이 있다

### 2 회차에 고칠 것

#### ER-03 — 줄 번호 없는 가이드 경로 토큰이 다른 줄에 늘 받쳐진다

1 회차 5 번을 반영해 `harness/docs/guides/qa-evaluation-guide.md:1785` · `:1862` 토큰이 들어오면서, 원래 있던 줄 번호 없는
`harness/docs/guides/qa-evaluation-guide.md` 토큰은 그 두 줄이 있기만 하면 1 이상이 된다(`grep -cF` 는 부분 문자열을 센다).
그래서 반대편 표 3 행(`:12` `contract-schema.md` v5.3 표기 · `:746` 표준형 4 요소 · `:1915` Parity with)과 4 행의 `:742-743`(상태 전제 선택지) 넘김을
notes 에서 통째로 빼도 20 값이 전부 1 이상이다. 초안도 이 사실을 적어 두었다(「앞 경로 … 는 다른 줄이 받쳐 1 이상 유지」).
넘김이 빠지면 Phase 3 이 버전 표기와 4 → 5 요소를 고칠 단서가 notes 에 없다.

```text
실측 (p2review2 — 예행 notes 에서 가이드 :12 · :746 · :1915 줄만 뺀 사본)
                              qa-evaluation-guide.md   :1785   :1862
예행 notes                     3                         1       1
그 줄만 뺀 사본                 2                         1       1      ← 여전히 전부 1 이상 = 통과
```

- 조건 문구: 경로 목록의 `harness/docs/guides/qa-evaluation-guide.md` 하나를 넷으로 바꾼다 —
  `harness/docs/guides/qa-evaluation-guide.md:12` · `harness/docs/guides/qa-evaluation-guide.md:742` ·
  `harness/docs/guides/qa-evaluation-guide.md:746` · `harness/docs/guides/qa-evaluation-guide.md:1915`. 「경로 9 개」 → 「경로 12 개」
- 측정: `for t in …` 목록에서 같은 자리를 같은 네 문자열로 바꾸고 「20 값」 → 「23 값」
- `범위 경계` 절 「커버리지 해소: ER-03 — 산문의 넘김 경로 9 개 … 20 문자열」 → 「12 개 … 23 문자열」, `회귀 게이트` 절 표
  `[ER-03]` 의 「20 값」 → 「23 값」
- 봉인 전 실측 문구: 23 문자열을 담은 가짜 notes 로 23 값 전부 1 이상, `:746` 이 든 줄을 지우면 그 값 0 (검토자 실측: 네 토큰을
  `경로:줄` 로 붙여 쓴 notes 에서 각각 1)
- notes 를 쓸 BUILD 에게 한 줄: 반대편 표처럼 `` `harness/docs/guides/qa-evaluation-guide.md` (`:12` · `:746` · `:1915`) `` 로 몰아 쓰면
  새 토큰이 전부 0 이다(검토자 실측). 줄마다 `경로:줄` 을 붙여 쓴다. `:742` 는 `…:742-743` 으로 써도 잡힌다
- 조건 줄 수는 그대로다(28 · 기능 조건 19). 고친 뒤 Step 6.2 · 6.5 와 커버리지 검출기를 다시 돌린다 — `UNCOVERED` ER-03 은 기존
  해소 줄이 받는다

### 2 회차 — 고치면 좋지만 막지는 않는 것

- SK-06 모의본 문단은 멈출 때 `skill-design-guide.md` §3.7 네 칸을 채우라고 하는데, 그 첫 칸 「막는 것」 은 「실행한 명령과 그 실패
  출력」 이다(`skill-design-guide.md:303`). 셸이 없는 세션은 명령을 실행할 수 없으니 그 칸을 채울 수 없다. 「셸이 없으면 막는 것 칸에
  이 세션에서 쓸 수 있는 도구 목록을 붙인다」 한 마디를 문단에 더하면 규칙끼리 부딪히지 않는다 (조건 영향 없음 — SK-06 토큰 다섯은 그대로)
- `harness/agents/qa-evaluator.md:590` 은 반대편 표 1 행(표준형 4 요소)과 4 행(상태 전제 선택지)이 같이 쓴다 — 파일의 한 줄에 두
  문제가 다 있어서다. 토큰으로는 둘을 가를 수 없으니 notes 의 그 줄에 두 내용을 함께 적게 한다

VERDICT: CHANGES
