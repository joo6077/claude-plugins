---
slug: h2-flipper-rear-exhaust-abs-fan-screw
created: "2026-09-26 19:45"
---

# h2-flipper-rear-exhaust-abs-fan-screw 개정

**조건은 바꾸지 않았다 — 조건 줄 개정 0 건.** 봉인 뒤 조건 줄도 산문도 고치지 않았다.
봉인 검증은 QA·교차 진단 뒤에도 `SEAL_OK` (`sha256:e686afa5b607ccd7`, 봉인 커밋 판과 일치).

- 봉인 커밋: `17074fb` (계약 파일 1 개)
- 구현: 레포 파일 0 개. 산출물 9 파일은 전부 레포 밖 `<OUT>` 이라 git 에 없다
- QA 판정: APPROVE · 21/21 통과 · N/A 5 건 · FAIL 0 건
- 교차 진단: APPROVE 는 옳음 (21 조건 전부 독립 재현). 지적 3 건 중 2 건 확인 · 1 건 미재현

아래는 전부 **`amend_direction: unchanged`** 다 — 통과 집합이 바뀌지 않는다. 조건도 측정도
그대로 두고 기록을 바로잡거나 못 잰 축을 드러낸다. `consent: unanchored` 이지만 `unchanged`
이므로 PASS 근거로 쓸 수 있다.

## A-01 · 팬 측정 스크립트가 보조 팬까지 셌다 (확인됨)

전제 2 는 「**부품 냉각** 팬」을 재라고 적었는데 스크립트가 그 제한을 구현하지 않아 `M106 P2`
(보조) · `P3` (챔버) · `P6` 를 함께 셌다. 교차 진단이 짚었고 실측으로 확인했다.

```text
M106 종류별   P 없음 41215 · P1 1 · P2 4 · P3 3 · P6 2
전부 세면      명령 41225 · 40%p 급변  6 회
부품 팬만      명령 41216 · 40%p 급변  2 회
→ 보조 팬 9 줄이 급변을 4 회 부풀렸다
```

**판정은 뒤집히지 않는다** — SC-03 의 기준이 100 회 미만이라 6 회든 2 회든 통과다. 평균 팬
(SC-02) 은 압출 이동에 걸린 값만 쓰므로 보조 팬 줄에 영향받지 않는다 (18.2 % 그대로).

다만 **기준이 빡빡한 조건에서 같은 버그가 나면 판정이 뒤집힌다.** 다음에 팬을 재는 조건을 쓸 때는
`M106` 에서 `P` 가 없거나 `P1` 인 줄만 세도록 측정 규약에 못 박는다. H2S 는 부품 팬을 `P` 없이
쓰고 `P1` 도 같은 팬을 가리킨다 (실측: `P1` 1 줄은 준비 동작).

## A-02 · DG-02 의 N/A 사유가 부정확하다 (판정 유지)

사유를 「레포 파일을 하나도 고치지 않으므로 편집기 진단 대상 변경이 0 개다」로 적었다.
**부정확하다** — 계약 파일과 QA 피드백 파일 자체가 레포 마크다운이고 이 스프린트가 만든 것이다.
`git status` 가 셋을 보여준다 (계약 1 · 피드백 1 · 멈춰둔 다른 계약 1).

실측으로 대신 확인했다.

```text
바뀐 레포 .md 3 개 · markdownlint (MD013 끔) → 0 건
이미 합쳐진 다른 피드백 파일   37 · 31 · 32 건
```

**새 경고 0 건이라 판정은 유지된다.** 사유만 틀렸다.

**교차 진단의 「40 건」은 재현되지 않았다.** 같은 설정으로 같은 세 파일을 재니 0 건이다. 교차
진단이 잰 대상이 달랐을 가능성이 있으나 그쪽 명령을 확인할 수 없어 여기서는 내 측정값을 적는다 —
재현 안 된 수치를 기록으로 남기지 않는다.

## A-03 · N/A 5 건을 명령·출력 없이 선언했다 (앞 스프린트 교훈을 또 어겼다)

`AP-00` · `RE-01` · `DG-01` · `DG-02` · `DG-03` 다섯 개를 「교집합 0 개」라는 **추론만으로**
선언했고, 실제로 돌린 명령과 출력을 계약에도 남기지 않았다. QA 는 다섯 개를 「사유 확인됨」으로
받았고, 증거 집계가 21 건이라 빠진 다섯이 드러나지도 않았다.

**앞 스프린트 개정 A-02 가 바로 이것을 적어 뒀다** — 「다음 계약에서는 N/A 사유를 「그건 검사
대상이 아니다」로 쓰지 말고 **돌려서 0 건임을 보인 뒤** N/A 로 내린다.」 한 스프린트 뒤에,
**그 A-02 를 이름으로 인용하는 바로 그 DG-02 문구에서 다시 어겼다.**

교차 진단이 대신 다섯 개를 돌려 닫았다.

```text
DG-01  bash -n scripts/release.sh                   exit 0 · release.sh 변경 0 줄
DG-03  bash scripts/release.sh 2>&1 || true         사용법 16 줄, 정상
AP-00  AP-01·AP-02 패턴을 바뀐 3 파일에 · AP-03      패턴 0 건 · 14 plugins 14 OK exit 0
RE-01  킷 파일 변경 0 건                             문서에 박힌 코드도 안 바뀜
DG-02  markdownlint (MD013 끔)                       0 건 (내 재측정)
```

**글로 남긴 교훈은 한 스프린트를 못 버틴다.** 이 항목은 다음 harness 작업으로 넘긴다 —
`sprint-contract` Step 6.5 가 「N/A 인데 측정 출력이 안 붙은 조건」을 이미 있는 `[미실측]`
검사와 같은 모양으로 막아야 한다. 그리고 QA 쪽은 N/A 에도 PASS 와 같은 근거 한 줄을 요구해야
한다. 이번 스프린트 범위 밖이라 사용자 판단으로 넘긴다.

## A-04 · 오르카는 한 번도 자르지 않았다 (계약대로다)

전제 1 이 자르기를 **뱀부 3mf 1 회**로 못 박았으므로 계약 위반이 아니다. 다만 `SC-02` · `SC-03` ·
`SC-04` · `SC-05` · `DG-04` 가 전부 뱀부 근거이고, 오르카 산출물은 키 값 · 옵션 목록 · zip 내용 ·
3mf 설정으로만 확인됐다. 오르카 사용자 프로파일이 0 개라 실사용 경로가 아니어서 그대로 둔다 —
오르카로 실제 뽑을 일이 생기면 그때 1 회 자른다.
