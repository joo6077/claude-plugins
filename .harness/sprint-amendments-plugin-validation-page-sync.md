# plugin-validation-page-sync 개정

계약 `sprint-contract-plugin-validation-page-sync.md` (봉인 `sha256:0c84c3f3266af6ec`, 봉인 커밋 2a190d7)
의 조건 줄은 고치지 않았다. 바뀐 것은 여기에만 적는다.

## A-01 — AR-01 변경 파일 목록에 `docs/index.html` 1 개 추가

**앵커**: AR-01 — "변경 파일이 정확히 2 개로 한정된다", 범위 경계 — "`docs/index.html` 은 이미 등록돼 있어 건드리지 않는다"

**무엇이 달라졌나**: AR-01 기대 집합에 `docs/index.html` 을 더한다 (2 → 3). 범위 경계의 "건드리지 않는다" 는
"`<head>` 에 아이콘 선언 한 줄만 더한다" 로 바뀐다. AR-02 (등록 1 · 아이콘 키 1) 는 그대로다.

**왜** — Iteration 1 QA 가 DG-04 하나로 REJECT 했다. 조건은 "콘솔 에러 0" 인데, 문서 사이트 첫 화면
`docs/index.html` 이 아이콘을 선언하지 않아 브라우저가 `/favicon.ico` 를 요청하고 404 가 1 건 난다.
평가자가 부모 세션 기록의 도구 결과로 확인했다 — 에러는 항목을 누르기 **전** 첫 화면에서 났고, 누른 뒤
새로 생긴 에러는 0 건이다. 그러니 계약의 두 문장(DG-04 "에러 0" · 범위 경계 "index.html 불가침")이 서로
부딪혀 이 범위 안에서는 통과가 불가능했다. 계약을 쓸 때 첫 화면의 콘솔을 재 보지 않은 작성자 결함이다.

**로컬 서버만의 문제가 아니다.** 배포 사이트에서도 같다 (2026-09-24 실측):

```text
https://joo6077.github.io/favicon.ico               → 404
https://joo6077.github.io/claude-plugins/favicon.ico → 404
https://joo6077.github.io/claude-plugins/            → 200
docs/index.html 의 rel="icon" 선언                   → 0 건
```

방문자마다 콘솔에 404 가 뜨므로, 측정을 좁히지 않고 원인을 고친다. `<link rel="icon" href="data:,">`
한 줄은 브라우저가 아이콘을 따로 요청하지 않게 한다. 바깥 리소스를 부르지 않는다.

**amend_direction**: `relaxing added=1 removed=0` — 원 기대 집합 2 개 → 개정 3 개. 자기신고가 아니라
두 목록을 `comm` 으로 비교해 계산했다 (`comm -13` 출력 `docs/index.html`).

**consent**: `anchored` — 세션 기록의 `AskUserQuestion` 질문 · 답 쌍 (선택지로 받은 동의).

| 항목 | 값 |
| --- | --- |
| 질문 머리 | `DG-04 처리` |
| 질문 시각 | `2026-09-24T05:08:58.846Z` |
| 답 시각 | `2026-09-24T05:17:53.792Z` (KST 14:17:53) |
| 세션 | `f5b7f3a5-c03d-452b-b44b-fc3d15dcd1a0` |
| 작업폴더 | `/Users/jackson/Hub/10_Dev/claude-plugins` |
| 고른 답 | "index.html 에 아이콘 한 줄 추가 (추천)" |

제시한 선택지는 셋이었다 — index.html 에 아이콘 한 줄 추가 / 측정에서 favicon 404 만 제외(배포 사이트의
404 는 남음) / 이번엔 합치지 않음.

## A-02 — Iteration 1 뒤 부모 교차 진단이 짚은 것 (조건 판정에 영향 없음)

판정을 뒤집을 근거는 없었다. 대신 조건이 못 잡은 틀린 글 넷을 짚었고, 이 스프린트 파일 안이라 고쳤다.
**amend_direction**: `unchanged` — 통과 집합을 바꾸지 않는다.

1. **킷 수를 숫자로 새로 박았다.** 출력 예시 아래 "전부 통과하면 `Total: 14 plugins, 14 OK` 처럼" 을 두 파일에
   새로 적었다. 계약 설계 결정("개수 표기는 뺀다")과 ER-04 취지에 어긋난다. ER-04 의 식
   `[0-9]+ ?개 (킷|카이젠)` 은 영어 "14 plugins" 를 못 잡아 통과했다. `Total: N plugins, N OK` 로 고쳤다
2. **기준 문서 §3 V2 "예외" 에 틀린 킷 목록이 남았다.** "`templates/` 없는 킷: … (harness, flutter-toolkit,
   design-kit, backend-kit, infra-kit …)" — 앞 셋은 `templates/` 가 4 · 2 · 8 개다. 이 스프린트가 §6 표에서
   바로잡은 사실과 반대로 말했다. 계약 사전 점검이 §6 표(`:562-567`)만 짚었고, ER-05(a) 는 세 구간에서
   이름 뒤 `/` · `"` 만 세어 쉼표로 나열한 이 줄을 못 봤다. 목록을 빼고 "§6 참조" 로 고쳤다
3. **출력 예시의 경고 줄이 스크립트가 낼 수 없는 모양이었다** (390dea8 전부터). `WARN "새 화면 추가" —
   react-screen, flutter-screen` 은 킷 안 중복 형식인데 flutter-screen 은 react-kit 에 없다. 킷 사이 중복의 실제
   형식(`scripts/validate-plugin.py:421`)인 `— react-kit / flutter-toolkit (cross-kit)` 으로 두 파일을 고쳤다.
   페이지는 V3 FAIL 줄도 두 줄로 꺾어 적었는데 실제 출력은 한 줄이라 한 줄로 되돌렸다
4. **V9 나쁜 예 주석이 부정확했다.** "인자와 함께 부르면 `$1` 이 인자 낱말로 바뀐다" — 공식 규칙상 `$N` 은
   N 번째 인자(0 부터)라 인자 하나로는 `$1` 이 그대로 남는다. "인자를 둘 넘기면 `$1` 이 둘째 인자가 된다" 로 고쳤다

교차 진단이 제대로 작동함을 확인한 것: 26 PASS 중 0 기대 조건(SK-01(b) · ER-05(b) · AR-05 · AP-01 · RE-02)은
양성 대조로 1 이상이 나왔다. 새로 적은 수치(V9 "6 파일 23 곳", V10 "84 줄 9 파일", 킷별 파싱 · SKIP 수,
출력 예시 열 맞춤, 변경 이력 날짜, 새 링크 3 개 HTTP 200)는 실제와 맞았다.

## A-03 — Iteration 2 뒤 부모 교차 진단이 짚은 것 (조건 판정에 영향 없음)

판정을 뒤집을 근거는 없었다. **amend_direction**: `unchanged`.

1. **A-02 3 번에서 고친 경고 예시가 또 틀렸다.** 형식(`scripts/validate-plugin.py:421`)은 맞았지만 고른 짝
   `react-kit / flutter-toolkit` 은 스크립트가 경고를 내지 않는 조합이다. 두 킷 모두 고유 단어 표
   `KIT_CONTEXT_TOKENS` 에 있어(`:69`, 표에 든 킷은 harness · flutter-toolkit · design-kit · backend-kit · infra-kit ·
   rust-kit · react-kit · tone-kit) 킷 사이 겹침을 "같은 개념 다른 프레임워크" 로 보고 넘긴다 — 교차 진단 실측으로
   두 킷은 키워드 37 개가 겹쳐도 WARN 0 건이다. 같은 두 문서가 바로 이 넘김을 V4 절에서 설명하므로 예시가 본문과
   어긋났다. 표에 없는 planning-kit 과 짝지어 `— react-kit / planning-kit (cross-kit)` 으로 고쳤다 (교차 진단이
   메모리 안 대조 시험으로 이 줄이 실제로 나오는 것을 확인했다)
2. **기준 문서 V9 문장의 느슨한 표현** — "인자와 함께 호출하면 `$0` · `$1` 이 인자 낱말로 바뀌어" 는 A-02 4 번이
   페이지에서 고친 것과 같은 느슨함이다. "그 순번(0 부터)에 들어온 인자 낱말로" 로 고쳤다

교차 진단이 확인한 것: `headless:false` 측정은 계약 대체 수단 범위 안이다 (평가자에게 브라우저 도구가 없고,
기본 헤드리스는 이 결함을 못 본다 — 창 없는 `channel:'chromium'` 으로도 넣기 전 404 1 건 · 넣은 뒤 0 건 재현).
아이콘 선언은 HTML 규격(WHATWG)상 아이콘 링크가 있으면 기본 `/favicon.ico` 요청을 하지 않는다. 사파리 동작은 확인하지 못했다.

## DG-04 를 다시 잴 때 빠지는 함정 두 개 (측정 방법 안내 — 조건은 그대로)

부모가 A-01 뒤에 다시 재다가 두 번 헛짚었다. 둘 다 "에러 0" 을 잘못 내거나 "에러 1" 을 잘못 낸다.

1. **헤드리스 브라우저는 favicon 을 아예 요청하지 않는다.** 계약이 허용한 대체 수단(`node` 로 도는 Playwright
   스크립트)을 헤드리스로 돌리면 고치기 **전** `index.html` 로도 에러 0 · 실패 요청 0 이 나온다 — 390dea8 판을
   임시 사본으로 띄워 실측했다. 이 결함에 대해서는 헤드리스 측정이 볼 수 없다. MCP 브라우저는 요청한다
   (04:54 · 05:22 두 번 404)
2. **브라우저 캐시가 옛 `index.html` 을 내준다.** 같은 MCP 브라우저로 다시 열면 전송량 0 · 아이콘 선언 없음인
   캐시 판을 써서 404 가 또 뜬다. 주소에 쿼리를 붙여(`/docs/index.html?v=<커밋>`) 새로 받게 하고,
   `document.querySelector('link[rel="icon"]')` 와 `performance.getEntriesByType('navigation')[0].transferSize` 로
   새 판인지 먼저 확인한다

새로 받은 판(전송 89,676 바이트, 아이콘 선언 있음)에서 항목을 누르면 iframe 제목 "플러그인 검증 가이드" ·
V 배지 10 개 · 콘솔 에러 0 이었다 (부모 세션 기록 05:23 의 `mcp__playwright__browser_*` 호출).

## 다음 스프린트로 남기는 것

1. **ER-04 · ER-05(a) 식이 좁다** — 한국어 "N개 킷" 만 잡고 "N plugins" 는 못 잡는다. 이름 뒤 `/` · `"` 만
   세어 쉼표 나열을 놓친다. 이번엔 교차 진단이 사람 눈으로 잡았다
2. **AR-03 이 큰따옴표 `<script src>` · `<link href>` 만 본다** — 작은따옴표 · `@import` · `//` 로 시작하는
   주소를 놓친다. 지금 페이지에는 해당이 0 건이다
3. **DG-02 는 도구가 죽어도 규칙 줄 0 개로 통과로 읽힌다** — 실행 성공 여부(종료 코드 · 검사 파일 수)를 함께 재야 한다
4. **SK-06 은 `V숫자 이름` 줄 머리만 잰다** — 상세 줄(FAIL · WARN)의 모양은 못 잡는다 (A-02 3 번 · A-03 1 번이 그 사례 —
   두 번 연속 사람 눈으로만 잡혔다)
5. **출력 예시의 `Total: 2 plugins` 는 명령줄로 만들 수 없는 조합이다** — 한 킷만 돌리거나 전체를 돌리거나 둘 중
   하나이고, 킷 사이 경고는 대상 킷이 둘 이상일 때만 난다. 예시를 "전체 실행 출력에서 두 킷만 뽑은 것" 으로 밝히거나
   실제 전체 실행에서 잘라 쓰는 편이 정확하다 (Iteration 3 교차 진단)
6. **리포트의 양성 대조 기록** — SC-01 · SC-03 · DG-02 는 계약에도 양성 대조가 없다. 다음 계약에서 도구가 실패하면
   0 이 아닌 값이 나오는지를 조건에 적을 것
