# Handoff — 2026-09-06 01:30 KST

> 다음 세션의 Claude가 읽을 것. 맨 위부터 읽고 **Resume Prompt** 섹션을 실행하라.

## Context

사용자 실측 보고("출력 표면이 지글거리고 듬성듬성 결손")를 추적해 bambu-kit 의 원인 4 종을 고치고,
회전체 seam default 를 `random` 에서 `vase + Smooth Spiral` 로 전환했다. 스프린트 2 건 완료,
`v0.7.0` 릴리스 + PR #28 머지까지 끝났다. 남은 것은 (1) `DG-04` — 플러그인 캐시가 아직 `0.6.0`
이라 신정책으로 완주 실행을 못 했다 (2) harness 코어 계약 결함 3 건 — 조사 완료, 미착수.

## Git State

- **Branch**: `main` — `origin/main` 과 동기 (ahead 0)
- **Uncommitted**: clean
- **Last commit**: `141d2a8` Merge PR #28 (release: bambu-kit v0.7.0)
- 이번 세션 커밋: `07573ee` · `f3c73e6` · `b173e9b`(release) · 머지 `141d2a8`
- 태그 `bambu-kit/v0.7.0` push 완료, main 도달 가능. CI 3 종 통과.

## Done

- [x] 원인 규명 — 생성 번들 42 개 전수 대조. 유량비 중앙값 5.1x / 최대 9.2x, filament 키를
      소재 부모값(0.4/1) 대신 제네릭(0.8/2)으로 덮어씀, `seam_slope_gap` 을 Bambu 기본 0 대신 10%
- [x] 스프린트 `bambu-kit` (24 조건) — 유량비 게이트 신설 · 소재 부모값 조회 강제 ·
      건조 하드게이트를 관측 신호 감별 트리로 교체 · `user-preferences.md` 신규. QA 22/24
- [x] 스프린트 `bambu-seam-policy` (27 조건) — **QA APPROVE 27/27, status done** (`f3c73e6`)
      seam 정책 v4 전환 · spiral 키 3 종 문서화 · scarf 길이 상한 · 소재 12 종 표 ·
      버전 런타임 조회 + 프리셋 온전성 검사
- [x] Bambu Studio `02.06.00.51` → `02.08.02.61` (Homebrew cask 전환). 앵커값 10/10 동일 확인
- [x] `print.timelapse = 0` (H2S + spiral vase 에서 seam 재생성, issue #9166)
- [x] `bambu-kit v0.7.0` 릴리스 + PR #28 머지

## In Progress

### 1. DG-04 — 스프린트 `bambu-kit` 마지막 조건 (status: active, 22/24)

- **어디까지**: 릴리스까지 완료. `.harness/sprint-contract-bambu-kit.md:118` 의 `DG-04`
  ("수정된 스킬을 실제 1 회 실행해 process + filament JSON 생성까지 에러 0 개") 만 미충족.
- **블로커**: 플러그인 캐시가 `~/.claude/plugins/cache/joo6077-plugins/bambu-kit/0.6.0` 그대로다.
  `installed_plugins.json` 의 `installPath` 도 `0.6.0`. **구정책(random default + 건조
  하드게이트)이 살아 있어서 지금 실행하면 신정책 검증이 아니라 거짓 증거가 된다.**
- **다음 스텝**: 사용자가 Claude Code 재시작 또는 `/plugin` 으로 캐시를 `0.7.0` 으로 갱신한 뒤
  아래를 확인하고 실행:
  ```bash
  ls ~/.claude/plugins/cache/joo6077-plugins/bambu-kit    # 0.7.0 이어야 함
  grep -c '유량비 게이트' ~/.claude/plugins/cache/joo6077-plugins/bambu-kit/0.7.0/skills/bambu-print-profile/SKILL.md  # >0
  ```
  그다음 `/bambu-print-profile /Users/jackson/Downloads/H2S_superlube_applicator_single_color.3mf`
  (⌀10.19 PETG 원통 — 사용자가 "단면상 골 파임" 을 보고한 그 모델. 신정책 검증 겸용)
- **주의**: 캐시 디렉토리를 손으로 복사해 채우지 마라. 배포 경로 우회 검증은 증거 가치가 없다.

### 2. harness 코어 계약 결함 3 건 (미착수, 조사 완료)

- **어디까지**: 서브에이전트 전수 조사 완료. 계약 미작성.
- **결함 A — RE-02 ↔ 신규 파일 요구 조건 충돌 (3 회 반복)**
  정본 `harness/skills/sprint-contract/SKILL.md:496-509`. `harness/references/contract-schema.md:717-723`
  의 문구가 **정본과 다르다**. 카브아웃 제안은 `.harness/sprint-amendments-bambu-seam-policy.md`
  의 `AM-01` (여전히 `consent: pending`, 미적용).
- **결함 B — 계약에 리터럴 환경값을 박는 패턴 (3 회 반복). 근본 원인.**
  QA 가 붙이는 결함 태그 5 종(`qa-evaluator.md:812`)과 계약 작성 preflight 태그
  6 종(`contract-schema.md:693-702`)의 **교집합이 `측정-중복` 하나뿐**이다. QA 가 3 회 붙인
  `측정-상태-모호` 가 작성 단계로 되먹여질 자리가 없다. "리터럴 환경값 금지" 규칙은 레포에 없고
  `Given:` 은 diff-scope 와 상태의존 명령에만 한정된다 (`contract-design-guide.md:566-600`).
- **결함 C — markdown 전용 킷 DG-02 오라클 부재 (3 회 → `[미검증:INVALID]` 승급)**
  `contract-schema.md:728-733` 의 DG-02 는 정본과 **의미가 다르다**(analyze 에러 vs IDE diagnostics).
  `commands.lint` 는 어떤 DG 조건과도 연결돼 있지 않다(`harness/README.md:86-102`).
  "마커는 `[미검증]` 통일, N/A 금지"(`qa-evaluation-guide.md:1015`)와 "빈 카테고리 `XX-00: N/A`
  허용"(`sprint-contract/SKILL.md:584`)이 이미 충돌. 이식 가능한 선례는
  `qa-evaluation-guide.md:1497-1509` (안티패턴 스택 정합성의 `N/A (사유)` 표기).
- **다음 스텝**: `harness:sprint-contract` 로 계약부터. 필수 수정 8 파일 —
  `harness/skills/sprint-contract/SKILL.md` · `harness/references/contract-schema.md` ·
  `harness/docs/guides/contract-design-guide.md` · `harness/docs/guides/qa-evaluation-guide.md` ·
  `harness/agents/qa-evaluator.md` · `harness/README.md` · `harness/templates/project.yaml` ·
  `.harness/project.yaml`. 조건부 6 그룹(init 스킬 · red-flags · validate.sh · eval fixture 5 ·
  fixture project.yaml 4).
- **주의**: harness 는 모든 킷이 의존하는 코어다. 기존 계약은 봉인(write-once)이라 수정 불가 —
  신규 계약부터 적용된다. **결함 B 를 먼저 고쳐라** — 나머지 둘의 재발 방지 장치다.

## Known Issues / Blockers

- 플러그인 캐시 갱신은 세션에서 강제 불가 (사용자 조작 필요)
- `AM-01` (relaxing amendment) `consent: pending` — RE-02 가 독립 검증으로 PASS 해서
  이번 스프린트엔 불필요해졌으나, 평가자가 "보일러플레이트에 영구 반영" 을 권고했다
- issue #9166 (H2S + Smooth Spiral + timelapse) 는 업스트림 미해결.
  `farthest_point_timelapse=1` 이 2.8 에서 H2S 에 추가돼 완화됐을 가능성 있으나 `[미확인]`

## QA / Build State

- `bambu-seam-policy` — **APPROVE 27/27** (iteration 3). `bambu-kit` — 22/24, DG-04 미충족
- `python3 scripts/validate-plugin.py bambu-kit` → V1~V8 OK, exit 0
- CI (PR #28): Harness Integration / Plugin Validation / Playwright Visual 3 종 통과

## Resume Prompt

```text
`.harness/handoff/2026-09-06-0130.md` 를 먼저 읽고, "In Progress" 섹션의 다음 스텝부터 이어서 실행해줘.

먼저 플러그인 캐시가 0.7.0 으로 갱신됐는지 확인하고:
- 갱신됐으면 → DG-04 (superlube 모델로 완주 실행) 부터
- 아직이면 → harness 코어 결함 3 건, 결함 B(되먹임 루프 단절)부터 계약 끊고 진행

주의: 캐시를 손으로 복사해 채우지 마라. 봉인된 계약의 조건 문구를 고치지 마라 (사이드카 사용).
```

## References

- 리서치: Codex run A(습기 감별진단) · B(유량/압력 일관성) · D(seam 해결 기법) · E(소재별 seam) · F(버전)
- 서브에이전트: H2S 프로파일 diff `v02.06.00.51`↔`v02.08.02.61` · spiral vase 키 소스 추적 · harness 결함 표면 전수
- 실측 사례 로그: `/Users/jackson/Hub/60_3D Print/Settings/*/notes.md`
- Studio 백업: `/Users/jackson/Hub/60_3D Print/_bambu-backup-2026-09-05` (프리셋 88 개)
- 롤백용 dmg: `~/Downloads/Bambu_Studio_mac-v02.06.00.51-*.dmg`
