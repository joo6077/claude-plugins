# Handoff — 2026-09-08 12:29 KST

> 다음 세션의 Claude가 읽을 것. 맨 위부터 읽고 **Resume Prompt** 를 실행하라.
> 직전 핸드오프 `2026-09-08-0100.md` 는 `feat/howto-kit` 브랜치에만 있다 (다른 세션이 자기 커밋에 담았다).

## Context

`2026-09-08-0100.md` 의 Queued 항목 **"bambu-kit surface-first 가 부품 형상을 구분하지 않는 구조 결함"** 을
계약부터 끊어 완료했다. 래티스 출력 사진은 오지 않았으므로 In Progress(실물 A/B)는 그대로 대기다.
공유 워크트리는 다른 세션의 `feat/howto-kit` 이 점유·편집 중이라 **`EnterWorktree` 로 격리 워크트리**를 만들어
거기서 작업했다 — 브랜치 전환 없음, 남의 브랜치에 커밋 없음.

## Git State

- **Worktree**: `/Users/jackson/Hub/10_Dev/claude-plugins/.claude/worktrees/bambu-surface-first-geometry`
- **Branch**: `worktree-bambu-surface-first-geometry` (`origin/main` `4fb1382` 기준, 원격 없음)
- **Local commits ahead of origin/main**: 1 개 (+ 이 핸드오프 커밋)
  - `1587bae` feat(bambu-kit): surface-first 에 형상 클래스 축 planar/thin — 래티스 회귀 수정
- **Uncommitted**: clean
- **push**: 안 함. 사용자가 명시해야 push · PR 한다

## Done (이번 세션)

- [x] **계약 `bambu-kit-surface-first-geometry-class`** — 26 조건 봉인(`sha256:65d829d758d1fc9e`), 슬러그 `SLUG_AUTO`,
      커버리지 검출기 0 건, 교차 진단 → 사이드카 narrowing 5 건(AM-01~05), 피드백 `verify-feedback.sh` PASS
- [x] **`surface-recipes.md` §2.7 형상 클래스 축** — `_geometry_class` `planar`|`thin`, 판정 = 3mf 단면 루프 둘레
      `<30mm` 비율의 3 높이(25/50/75 %) 중앙값 `>=0.5`. `thin` 은 §3 속도 하향 미적용, 냉각은 **확인 후** filament
      `overhang_fan_threshold 10%` 1 키 (`user-preferences.md` §1 "팬 자동 X" 는 그대로 참)
- [x] **`SKILL.md`** — Phase 1.0 `bambu-kit geometry-class probe`(stdlib, 3mf components/변환 해석, 3 높이 절단) ·
      Phase 3 라우팅 표 · `:1031` 냉각 위임 예외 · 튜닝 목록 `outer_wall_speed` planar 한정 · Gotcha 2 항 ·
      실측 사례 행 · Phase 4.3 게이트에 **설치본 도출 키 스코프 검사**(시스템 프로파일 + Bambu 저장 user preset,
      `META` 제외, 미지 키는 `[미검증]`) + **`_geometry_class` 검사**(허용값 · 결측 · thin 인데 외벽 하향 → FAIL)
- [x] **`bambu-fields-baseline.md` §10.5** — 냉각 키 5 종 filament 스코프 정본 (process 0 / filament 146~1218,
      앱 `02.08.02.61` · 번들 `02.08.00.06`, 소재별 실효값 표)
- [x] **파생 HTML 3** 동기화 — `surface-recipes.html` 의 stale 값 2 건(PETG `20-35`→`50-70`, `resolution` `0.006-0.010`→기본값 유지) 정정 포함
- [x] **`bambu-kit/evals/gate-fixtures/` 7 종** — 게이트 SC/ER 조건 + 음성 대조용
- [x] **QA APPROVE 25/26** (DG-02 `[미검증:ENV]` — IDE 패널 접근 도구 부재). 음성 대조 6 종 전부 실측 뒤집힘 확인
- [x] 실물 3mf 분류 검증 — Side/Center/Rear/Large Container + Large Lid `thin`, Funnel + Small Lid `planar`.
      직전 세션 수작업 측정(20.59 / 175.47 / 391.21 mm)과 일치
- [x] 메모리 정리 — 해결된 enum 결함·DG-04 메모리 2 건 삭제, 신규 3 건
      (`project_bambu_geometry_class_sprint` · `feedback_worktree_guard_script_files` · `feedback_contract_section_scope_awk_anchor`)

## In Progress (이어서 할 것)

### 래티스 통 출력 결함 — 실물 A/B 대기 (변경 없음)

- **어디까지**: 원인 확정(속도값 + 냉각 문턱) + `LatticePods-FANFIX.zip` 전달 완료. **출력 안 됨.**
- **다음 스텝**: 사용자가 Side Container LHS 1 개를 FANFIX filament 프리셋으로 출력 → 사진 판정.
  이번 킷 수정으로 **다음 생성부터는** thin 부품에 외벽 `30` 이 안 들어간다 — 재생성하면 process 도 바뀌므로
  A/B 단일 변수를 지키려면 이번 출력은 기존 process + FANFIX filament 그대로 간다.
- **주의**: `Bambu ABS Flow Rate Calibrated` 프리셋(`filament_flow_ratio 0.77805`) 바인딩 여부 출력 전 확인 필수

## Queued

- [ ] **3.36 mm 주기 측면 돌출** — 층시간 `8.6↔14.6s` 가 `slow_down_layer_time 12` 를 교차. 이번 킷 수정(thin 은 부모 속도
      `60` 유지)이 그 방향의 처방이지만 **실물 검증 전**. A/B 이후 재생성으로 확인
- [ ] **`overhang_1_4_speed = ["0","0","0"]`** 센티넬 — 2 차 출력에서
- [ ] **릴리스 판단** — bambu-kit `0.8.0` → `0.9.0` (minor: 정책 축 신설 + 게이트 확장). `bash scripts/release.sh bambu-kit minor`
      는 **main 에서** 돌린다 — 먼저 이 브랜치를 PR/머지해야 한다
- [ ] **격리 워크트리 처리** — 머지 후 `ExitWorktree` 또는 `git worktree remove .claude/worktrees/bambu-surface-first-geometry`
- [ ] **`harness-attribution-followup`** 계약 `status: active` (REJECT 13/15 기록 보존) — 정리 여부 사용자 판단
- [ ] STL 입력의 단면 측정 — probe 는 3mf 만 지원. STL 은 사용자에게 단면 성격을 묻는다 (SKILL.md Phase 1.0 명시)

## Known Issues / Blockers

- **다른 세션이 공유 워크트리에서 `feat/howto-kit` 작업 중** (`docs/howto-kit/` → `docs/howto/` 이름 변경 스테이징 관측).
  그 브랜치에 내 직전 핸드오프 + `docs/bambu-calibration/` 이 커밋 `97cde49` 로 들어가 있다 — main 도달은 그쪽 PR 에 달려 있다
- **EnterWorktree 세션의 Bash 가드**가 heredoc · 런타임 변수 · `cd` 가 든 명령을 거부한다. 스크립트 파일로 쓰고 절대경로로
  실행하라 (memory `feedback_worktree_guard_script_files`)
- `save-feedback.sh` 가 `CONTRACT_ROOT` 기준으로 `project_name` 을 워크트리 basename(`bambu-surface-first-geometry`)으로 기록한다.
  글로벌 피드백 집계 시 `claude-plugins` 와 분리돼 보인다 (`project_hash b3a3a520`)
- 게이트 스코프 검사는 시스템 프로파일이 설정하지 않는 키를 **user preset 에서만** 잡는다 (`brim_type` 이 그 사례). user preset 이
  없는 새 머신에서는 그런 키가 `[미검증]` 으로 뜬다 — FAIL 아님

## QA / Build State

- 계약 `bambu-kit-surface-first-geometry-class` **done** (APPROVE 25/26, SEAL_OK). 산출물 3 종 커밋됨
  (`sprint-contract` · `sprint-amendments` · `sprint-feedback`)
- `python3 scripts/validate-plugin.py bambu-kit` exit 0 · `python3 scripts/sync-docs.py --check-only bambu-kit` 동기화 상태
- 톤 게이트(tone-guide Step 5): K-02 킬러 패턴 0 · C-13 0 · N-08 0 · 어댑터 없음(코어만)
- 글로벌 피드백: contract `b3a3a520-2026-09-08T121212-1778a3e2-79499.yaml` · evaluator `b3a3a520-2026-09-08T122748-1778a3e2-36876.yaml`

## Resume Prompt

```text
`.harness/handoff/2026-09-08-1229.md` 를 먼저 읽고, "In Progress" 섹션의 다음 스텝부터 이어서 실행해줘.

주의:
- 작업 브랜치는 격리 워크트리 `.claude/worktrees/bambu-surface-first-geometry` 의 `worktree-bambu-surface-first-geometry` 다.
  공유 워크트리는 다른 세션이 `feat/howto-kit` 으로 쓰고 있다 — 브랜치 전환 금지, `git add -A` 금지.
- 래티스 출력 사진이 오면 그것부터 판정. 안 왔으면 재촉하지 말고 Queued 로 가라.
- 게이트·분류기는 SKILL.md 코드 블록을 awk 로 추출해 편집 없이 실행한다 (계약 §추출 규약). 픽스처는 bambu-kit/evals/gate-fixtures/.
- 설정 키를 말하기 전에 설치본에서 실존·스코프를 확인해라 — 이제 Phase 4.3 게이트가 스코프를 잡지만, 문서에 쓰는 값은 여전히 손이다.
```

## References

- 계약 · 사이드카 · QA: `.harness/sprint-contract-bambu-kit-surface-first-geometry-class.md` ·
  `.harness/sprint-amendments-…` · `.harness/sprint-feedback-…`
- 정책 정본: `bambu-kit/skills/bambu-print-profile/references/surface-recipes.md` §2.7 ·
  키 정본: `references/bambu-fields-baseline.md` §10.5
- 실물: `/Users/jackson/Hub/60_3D Print/Settings/ams-2-pro-lattice-dry-pods/` (`notes.md` §1.2.2 · `LatticePods-FANFIX.zip`)
- 검증 스크립트(세션 scratchpad, 휘발): `run_gate_tests.py` · `verify_static.sh` · `contract-gates.sh` — 같은 실행은
  계약 §추출 규약 + 픽스처로 재현 가능
- 직전 핸드오프: `feat/howto-kit` 브랜치의 `.harness/handoff/2026-09-08-0100.md`
