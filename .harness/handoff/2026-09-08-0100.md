# Handoff — 2026-09-08 01:00 KST

> 다음 세션의 Claude가 읽을 것. 맨 위부터 읽고 **Resume Prompt** 를 실행하라.

## Context

세션이 두 덩어리다. (1) 레포 작업 — bambu-kit enum 오염 수정 + harness 코어 결함 3 건 수정,
계약 5 건 · QA 6 회 · 릴리스 2 건까지 완료. (2) 실물 3D 프린팅 진단 — 그 릴리스한 킷으로
생성한 프로파일의 출력물에 결함이 나와, flow / PA / Z / 워핑 / 습기를 차례로 배제하고
**원인이 내가 넣은 속도값과 냉각 문턱**임을 워크플로우 25 에이전트로 확정했다. 프로파일은
만들어 전달했고 **실물 검증만 남았다.**

## Git State

- **Branch**: `main` — `origin/main` 과 동기 (ahead 0)
- **Last commit**: `4fb1382` Merge PR #34 (release: harness v0.8.0)
- **Uncommitted (내 것)**: `docs/bambu-calibration/` (신규)
- **Uncommitted (다른 세션 것 — 건드리지 마라)**:
  `.harness/sprint-contract-howto-kit-design-brief.md` · `.harness/sprint-feedback-howto-kit-design-brief.md`
  · `docs/howto-kit/` · `result.json`

⚠️ **이 레포는 워크트리를 여러 Claude 세션이 공유한다.** 이번 세션에서 다른 세션이 내 작업을
자기 커밋 3 개에 쓸어담았고(`e73429f` `3cd7dfe` `38254cc`), 브랜치를 checkout 해서 워킹트리가
한 번 통째로 되돌아갔다. `git add -A` 금지. 귀속은 mtime + diff 주제로 판정하라.
자세한 건 memory `feedback_shared_worktree_concurrent_session`.

## Done

- [x] **bambu-kit enum 오염 수정** — `ironing_type` 이 OrcaSlicer 이름(`topmost_only` 등)이라
      Bambu 가 조용히 무시하던 결함. `top_surface_pattern` 도 같은 오염. 50 건 치환 +
      Phase 4.3 게이트에 `ENUM_ALLOW` 6 키 추가. **QA APPROVE 22/22**
- [x] **harness 코어 결함 3 건** — QA 태그와 계약 preflight 태그가 교집합 1 개인 별개 어휘라
      되먹임이 끊겨 있던 것을 **통합 10 종 SSOT** 로 합침. RE/DG 문구 정본 정합, `N/A (사유)` ↔
      `[미검증]` 경계 명문화. **QA APPROVE 21/22** (iteration 2)
- [x] **anti_patterns 에 `command` 필드 신설** — `^```\s*$` 정규식이 오탐 292 건 / 실제 위반 0 건
      이었다. 여는·닫는 fence 가 동형이라 줄 단위 regex 로는 원리적으로 판정 불가.
      정규식 대신 도구 위임 경로를 스키마에 추가. **QA APPROVE 21/21**
- [x] **릴리스 2 건** — `bambu-kit/v0.8.0` (PR #33) · `harness/v0.8.0` (PR #34), 둘 다 머지 완료.
      태그 main 도달 가능 확인
- [x] **`docs/bambu-calibration/calibration-reference.md`** — Bambu Studio 캘리브레이션 6 종
      레퍼런스. 근거 등급([설치본]/[공식]/[Orca]/[추론]/[미검증]) 문장마다 표기.
      **글로벌 스킬 소스용** — 사용자가 다른 세션에서 스킬로 만들 예정

## In Progress

### 래티스 통 출력 결함 — 팬 문턱 수정, 실물 검증 대기

- **어디까지**: 원인 확정 + 프로파일 생성 + 전달 완료. **출력만 안 했다.**
- **전달물**: `/Users/jackson/Hub/60_3D Print/Settings/ams-2-pro-lattice-dry-pods/LatticePods-FANFIX.zip`
  (filament 1 + process 3). Phase 4.3 게이트 PASS
- **적용한 변경 — 딱 1 개**: `overhang_fan_threshold` `25%` → `10%` (**filament** 프리셋)
  - 사선 스트럿 미지지율 = `0.12·tanθ/0.42` → 20°=10.4% / 40°=24.0%. base 25% 문턱에 안 걸려
    `overhang_fan_speed=100` 이 구조적으로 발동 못 하고 있었다. 하향면의 42.4% 가 이 대역
  - 스코프 실측: filament 148 개 / **process 0 개** — process 에 넣으면 조용히 무시된다
  - `Bambu ABS-GF @base` 가 이미 `10%` 를 쓴다 (임의값 아님)
- **다음 스텝**: 사용자가 Side Container LHS 1 개를 이 프리셋으로 출력 → 사진 받아 판정.
  단일 변수 A/B 라 process 는 손대지 않았다
- **주의**: 효과는 "0→100" 이 아니라 **"약 55→100"** 이다 (`fan_cooling_layer_time=30` 상속 +
  실제 층시간 8.6~14.6s 라 팬이 이미 53~60% 로 돌고 있었다)

## Queued

- [ ] **3.36mm 주기 측면 돌출** — 이번 세트로 **안 고쳐진다.** 기구: 층시간이 `8.6↔14.6s` 로
      `slow_down_layer_time=12` 를 주기적으로 가로지름 + `layer_time_smoothing=0`.
      처방은 속도 원복 또는 12s 문턱 재배치. 팬 변경과 상충해 의도적으로 이번 사이클에서 제외
- [ ] **`overhang_1_4_speed = ["0","0","0"]`** (process) — 반박 단계가 남긴 revised 값.
      툴팁 *"0 speed means no slowing down ... and wall speed is used"* 의 센티넬.
      시스템 process 147 개 중 98 개가 이 센티넬 사용. **2 차 출력에서** 추가하라
- [ ] **bambu-kit 구조 결함** — `surface-first` 가 부품 형상을 구분하지 않는다. 큰 평면이든
      얇은 래티스든 똑같이 속도를 깎는데, 래티스는 병목이 유량이 아니라 **열 방출 시간**이라
      느릴수록 나빠진다. 그리고 "냉각은 base 에 위임" 정책 때문에 늘어난 체류를 팬으로 보상할
      경로가 막혀 있다. 계약 끊고 고칠 대상
- [ ] **`harness-attribution-followup`** 계약이 `status: active` (REJECT 13/15). 조건 2 개의
      오라클이 틀려서 **어떤 올바른 구현으로도 만족 불가**. v2 로 대체했고 기록 보존용으로 남김.
      정리할지 사용자 판단 필요

## Known Issues / Blockers

- **`Bambu ABS Flow Rate Calibrated` 프리셋의 `filament_flow_ratio = ["0.77805","nil","nil"]`**
  (base 0.95). 캘리브레이션이 오염된 값이고 실물 A/B 로 0.95 가 낫다고 확인됨.
  **래티스 출력에 이게 바인딩돼 있었으면 18% 과소압출만으로 증상 전부가 설명된다** — 출력 전 확인 필수
- 사용자가 확인해 준 배제 항목: 습기(80°C 24h + 출력 중 연속 건조, 환경 7% RH) · PA(k 0.019 정상)
  · Z 과이송(두께 2.44~2.48 vs 슬라이스 2.44) · 베드 기울기(양 끝 0.03mm 차)
- **내가 이 세션에서 낸 사실 오류 3 건** — 다음 세션도 조심하라:
  (a) `ironing_type` enum 을 Orca 이름으로 오독 (b) `bridge_speed` stock 을 25 로 보고(실효 **50**)
  (c) diff-scope 조건에 `git diff` 를 써서 **미추적 파일이 안 보임** — 3 회 연속 같은 실패

## QA / Build State

- 계약 5 건: `bambu-kit-enum-allowlist-gate` done · `harness-core-defects` done ·
  `harness-attribution-followup` **active(REJECT 보존)** · `harness-attribution-followup-v2` done ·
  `antipattern-command-field` done. **전부 `SEAL_OK`** (조건 문구 무변조)
- `python3 scripts/validate-plugin.py` → **exit 0** (13 킷 OK)
- 플러그인: `harness 0.8.0` · `bambu-kit 0.8.0`, 둘 다 main 머지 완료

## Resume Prompt

```text
`.harness/handoff/2026-09-08-0100.md` 를 먼저 읽고, "In Progress" 섹션의 다음 스텝부터 이어서 실행해줘.

주의:
- 이 레포는 워크트리를 다른 Claude 세션과 공유한다. `git add -A` 금지, 내 파일만 경로로 스테이징.
- 래티스 출력 결과 사진이 오면 그것부터 판정. 출력 안 했으면 재촉하지 말고 Queued 항목으로 가라.
- bambu-kit 의 surface-first 가 부품 형상을 구분하지 않는 구조 결함은 계약부터 끊어라.
- 설정 키를 말하기 전에 설치본(/Applications/BambuStudio.app)에서 실존·스코프를 확인해라.
  이 세션에서 enum 이름과 키 스코프를 각각 한 번씩 틀렸다.
```

## References

- 캘리브레이션 레퍼런스: `docs/bambu-calibration/calibration-reference.md`
- 워크플로우 결과 (25 에이전트, 12 키 검증+반박):
  `~/.claude/projects/-Users-jackson-Hub-10-Dev-claude-plugins/44c7700e-*/subagents/workflows/wf_3c6f1ffd-7cf/journal.jsonl`
- 프린트 산출물: `/Users/jackson/Hub/60_3D Print/Settings/ams-2-pro-lattice-dry-pods/`
- 사이드카: `.harness/sprint-amendments-harness-core-defects.md` ·
  `.harness/sprint-amendments-harness-attribution-followup.md`
- memory 신규 4 건: `feedback_shared_worktree_concurrent_session` ·
  `feedback_diff_scope_oracle_blind_spots` · `project_harness_unified_defect_tags` ·
  `project_bambu_kit_ironing_enum_defect`
